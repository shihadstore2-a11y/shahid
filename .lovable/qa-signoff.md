# QA Sign-off Matrix V9 — رِفد للتقنية

> **الحالة:** مفتوح — تم تنفيذ فحص تقني آلي وأمني داعم، والاعتماد اليدوي الموسع لم يُغلق بعد.  
> **الهدف:** توثيق 84 تمريرة يدوية قبل رفع الجاهزية الكلية من 99.989% إلى 100%.

---

## قاعدة الاعتماد

لا تُغلق QA اليدوية إلا إذا تحققت الشروط التالية:

1. كل السيناريوهات الـ14 في `.lovable/qa-runbook.md` اجتازت الاختبار.
2. كل سيناريو اختُبر على 3 مقاسات: Mobile 390px، Tablet 768px، Desktop 1280px.
3. كل مقاس اختُبر على Chrome وSafari.
4. لا توجد أخطاء console حرجة.
5. أي فشل موثّق في سجل الأخطاء مع قرار واضح: مصلح / مقبول / مانع.
6. لا يتم اعتماد 100% قبل تحصين الفيديو، الحملات، النقاط، ولوحة الإدارة.

---

## مصفوفة الاعتماد اليدوي V9

| السيناريو | Chrome Mobile | Chrome Tablet | Chrome Desktop | Safari Mobile | Safari Tablet | Safari Desktop | الحالة |
|---|---|---|---|---|---|---|---|
| S1 — تسجيل جديد → onboarding → dashboard | □ | □ | □ | □ | □ | □ | مفتوح |
| S2 — توليد نص + usage + library | □ | □ | □ | □ | □ | □ | مفتوح |
| S3 — توليد صورة + download + library | □ | □ | □ | □ | □ | □ | مفتوح |
| S4 — اشتراك كامل + إيصال + تفعيل | □ | □ | □ | □ | □ | □ | مفتوح |
| S5 — استعادة كلمة السر | □ | □ | □ | □ | □ | □ | مفتوح |
| S6 — نموذج /contact | □ | □ | □ | □ | □ | □ | مفتوح |
| S7 — تجاوز الحصة + مسار الترقية | □ | □ | □ | □ | □ | □ | مفتوح |
| S8 — توليد فيديو ناجح | □ | □ | □ | □ | □ | □ | مفتوح |
| S9 — فشل توليد فيديو + refund | □ | □ | □ | □ | □ | □ | مفتوح |
| S10 — ظهور الفيديو في المكتبة | □ | □ | □ | □ | □ | □ | مفتوح |
| S11 — إنشاء Campaign Pack وحفظه | □ | □ | □ | □ | □ | □ | مفتوح |
| S12 — توليد نص/صورة/فيديو من الحملة | □ | □ | □ | □ | □ | □ | مفتوح |
| S13 — مراقبة النقاط والقيود للمميزات المكلفة | □ | □ | □ | □ | □ | □ | مفتوح |
| S14 — Admin للفيديو والحملات والledger | □ | □ | □ | □ | □ | □ | مفتوح |

---

## Lighthouse المطلوب قبل الإعلان

| الصفحة | Performance | Accessibility | Best Practices | SEO | الحالة |
|---|---:|---:|---:|---:|---|
| `/` | ≥80 | ≥90 | ≥90 | ≥95 | مفتوح |
| `/pricing` | ≥80 | ≥90 | ≥90 | ≥95 | مفتوح |
| `/about` | ≥80 | ≥90 | ≥90 | ≥95 | مفتوح |
| `/auth` | ≥80 | ≥90 | ≥90 | ≥90 | مفتوح |
| `/contact` | ≥80 | ≥90 | ≥90 | ≥95 | مفتوح |
| `/dashboard` | ≥75 | ≥90 | ≥90 | N/A | مفتوح |
| `/dashboard/generate-video` | ≥75 | ≥90 | ≥90 | N/A | مفتوح |
| `/dashboard/campaign-studio` | ≥75 | ≥90 | ≥90 | N/A | مفتوح |

---

## قرار الإغلاق

| البند | الحالة |
|---|---|
| 84 تمريرة موثقة | مفتوح |
| Lighthouse على الصفحات الأساسية والجديدة | ⚠️ Desktop اجتاز، وMobile كشف فجوات أداء في الصفحات العامة؛ تم إصلاح تحميل فيديو الصفحة الرئيسية، وبقيت إعادة القياس بعد النشر وصفحات Dashboard بجلسة مستخدم |
| فحص TypeScript | ✅ ناجح — 25 أبريل 2026، أُعيد تشغيله بعد بوابة الجودة النهائية دون أخطاء |
| Production build | ✅ ناجح — 25 أبريل 2026، أُعيد تشغيله بعد بوابة الجودة النهائية؛ تحذير حجم chunks فقط |
| Lovable Cloud security linter | ✅ لا توجد ملاحظات — 25 أبريل 2026، أُعيد التحقق ضمن بوابة الجودة النهائية |
| Lovable Cloud triggers | ✅ مفعّلة ومُنظّفة من التكرار ومثبتة فعلياً عبر `pg_trigger` بعد ظهور 25 تريجر دون تكرار — 25 أبريل 2026 |
| أخطاء console حرجة | ✅ الفحص السريع لا يظهر أخطاء حرجة من كود التطبيق؛ ظهرت تحذيرات `postMessage` من بيئة المعاينة فقط — تبقى مراجعة 84 تمريرة |
| معاينة Mobile/Tablet/Desktop السريعة | ✅ فحص بصري داخلي جزئي للصفحات `/`, `/pricing`, `/auth`, `/contact`, `/about` على 390px و820px و1280px — لا يغني عن QA الكامل |
| تحصين الفيديو والنقاط | ✅ متقدم جداً — تم تدقيق S8-S10، وتأكيد خصم النقاط مرة واحدة، وردّها عند الفشل/timeout، وحفظ الفيديوهات في المكتبة؛ بقي اختبار إنتاجي بمستخدم حقيقي ومزوّد فعلي |
| لوحة إدارة المميزات الجديدة | ✅ متقدم جداً — S11-S14 مدققة كودياً: إدارة الفيديو، الحملات، ودفتر النقاط محمية بـAdminGuard وتحقق خادمي من دور الأدمن |
| مسار رفع إيصال الاشتراك | ✅ تم إصلاح فجوة RLS حرجة — تحديث `receipt_path` ومعاينة الإيصال أصبحا عبر Server Functions موثوقة بعد تحقق الملكية |
| قرار الإعلان التسويقي | غير معتمد بعد — ينتظر إعادة Mobile Lighthouse بعد النشر، صفحات Dashboard، وQA يدوي كامل |

**نسبة التقدم الرسمية:** 99.84% جاهزية بناء وتشغيل، مع بقاء إعادة Lighthouse Mobile بعد النشر وDashboard وQA اليدوي وSoft Launch قبل اعتماد 100%.

---

## فحص QA داخلي جزئي — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| TypeScript | ✅ ناجح | لا مانع تقني |
| Production build | ✅ ناجح | لا مانع؛ يوجد تحذير حجم chunks يحتاج متابعة أداء لا يمنع QA |
| Security linter | ✅ لا ملاحظات | لا مانع أمني آلي |
| Triggers | ✅ موجودة فعلياً على الجداول الحرجة | لا مانع بنيوي |
| `/` Mobile 390 | ✅ سليم بصرياً | لا overflow ظاهر في أول شاشة |
| `/pricing` Mobile 390 | ✅ سليم بصرياً | لا كسر ظاهر في أول شاشة |
| `/auth` Mobile 390 | ✅ سليم بصرياً | نموذج الدخول ظاهر ومقروء |
| `/contact` Tablet 820 | ✅ سليم بصرياً | النموذج والبطاقات ظاهرة دون كسر كبير |
| `/about` Desktop 1280 | ✅ سليم بصرياً | الهيدر والمحتوى مستقران |
| Console | ✅ لا أخطاء تطبيق حرجة | تحذيرات المعاينة لا تُعد blocker |
| Performance profile في المعاينة | ⚠️ متوسط/بحاجة Lighthouse رسمي | أزمنة المعاينة لا تُعتمد كبديل Lighthouse |
| Dashboard/Auth flows | ⏳ غير مغلق | يحتاج جلسة مستخدم/أدمن واختبار يدوي كامل |
| Safari | ⏳ غير مغلق | يحتاج تأكيد على Safari حقيقي أو جلسة مناسبة |

**حكم هذه الجولة:** لا يوجد blocker تقني ظاهر يمنع الانتقال إلى QA V9 اليدوي، لكن لا يجوز اعتبار QA مغلقاً أو بدء Soft Launch قبل توثيق السيناريوهات كاملة وتشغيل Lighthouse الرسمي.

---

## دفعة QA V9 التالية — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| TypeScript + Production build | ✅ ناجح | لا مانع تقني؛ بقي تحذير حجم chunks كملاحظة أداء غير مانعة |
| `/` Chrome Mobile 390 | ✅ ناجح بصرياً | الهيدر والـhero والـCTA واضحة دون overflow أفقي ظاهر |
| `/pricing` Chrome Mobile 390 | ✅ ناجح بصرياً | بطاقات الأسعار تظهر بشكل مقروء، والـtoggle واضح |
| `/pricing` Chrome Tablet 820 | ✅ ناجح بصرياً | التخطيط ثنائي الأعمدة مستقر ولا يوجد تداخل ظاهر |
| `/pricing` Chrome Desktop 1280 | ✅ ناجح بصرياً | التخطيط عريض ومستقر؛ يظهر Cookie Banner دون كسر للمحتوى |
| `/auth` Chrome Mobile 390 | ✅ ناجح بصرياً | نموذج الدخول واضح، الحقول والأزرار ضمن العرض، ورابط الاستعادة ظاهر |
| Console | ✅ لا أخطاء تطبيق حرجة | التحذيرات المرصودة من بيئة المعاينة وPerformance API فقط، وليست blocker |
| Performance profile تمهيدي | ⚠️ يحتاج Lighthouse رسمي | FCP في المعاينة يتأثر ببيئة التطوير؛ لا يُعتمد بديلاً عن Lighthouse الإنتاجي |

**حكم الدفعة:** تم تأكيد استقرار البناء والصفحات العامة الأعلى أولوية على Chrome Mobile/Tablet/Desktop جزئياً. لا يوجد blocker جديد. تبقى السيناريوهات العملية S1-S14، Safari، ورسائل البريد/Telegram/الأدمن غير مغلقة.

**نسبة التقدم بعد هذه الدفعة:** 99.25% جاهزية بناء وتشغيل، وقرابة 94-95% جاهزية اعتماد قبل Soft Launch.

---

## تدقيق S1-S4 ومعالجة فجوة حرجة — 25 أبريل 2026

| السيناريو | نتيجة التدقيق | القرار |
|---|---|---|
| S1 — تسجيل جديد + onboarding | ✅ كودياً سليم | التسجيل يحوّل إلى onboarding، وonboarding يحفظ `profile.onboarded=true` ويولّد أول محتوى؛ يحتاج اختبار بريد فعلي |
| S2 — توليد نص + usage + library | ✅ كودياً سليم | `generateText` يستهلك الحصة اليومية، يسجل `record_generation`، ويظهر في المكتبة عبر `generations` |
| S3 — توليد صورة + download + library | ✅ كودياً سليم | الصورة تُحفظ في التخزين، تولّد Signed URL، وتُسجل في `generations` مع زر تنزيل في الواجهة |
| S4 — اشتراك + إيصال + تفعيل | ⚠️ وُجدت فجوة ثم أُصلحت | رفع الإيصال كان يرفع الملف ثم يفشل تحديث الطلب بسبب RLS؛ نُقل تحديث `receipt_path` إلى Server Function تتحقق من ملكية الطلب والمسار |
| TypeScript + Production build بعد الإصلاح | ✅ ناجح | الإصلاح لا يكسر البناء؛ بقي تحذير chunk size فقط |

**حكم هذه الدفعة:** تم تحسين مسار الإيراد الأكثر حساسية قبل Soft Launch. لم يتم إغلاق S4 بالكامل بعد لأن وصول Telegram، تفعيل الأدمن، وبريد `subscription-activated` يحتاج جلسة أدمن/بيئة فعلية، لكن تم إزالة عائق تقني مؤكد كان سيمنع اكتمال رفع الإيصال للمستخدم.

**نسبة التقدم بعد الإصلاح:** 99.35% جاهزية بناء وتشغيل، وقرابة 95-96% جاهزية اعتماد قبل Soft Launch.

---

## تدقيق S5-S7 وتحسينات ما قبل Soft Launch — 25 أبريل 2026

| السيناريو | نتيجة التدقيق | القرار |
|---|---|---|
| S5 — استعادة كلمة السر | ✅ كودياً وبصرياً سليم | `/forgot-password` يرسل رابط الاستعادة دون كشف وجود البريد، و`/reset-password` يتحقق من جلسة recovery ويمنع كلمات السر القصيرة/غير المتطابقة؛ بقي تأكيد وصول بريد فعلي على صندوق بريد حقيقي |
| S6 — نموذج /contact | ✅ نجح عملياً + تحسين أمني | إرسال نموذج تواصل من Mobile Chrome نجح، وظهر الصف في `contact_submissions` بحالة `new`، وطابور البريد أضاف رسالة تأكيد؛ تم تعديل honeypot ليُرجع 400 كما ينص Runbook بدل نجاح صامت |
| S7 — تجاوز الحصة | ✅ كودياً سليم | أخطاء `text_quota_exceeded` و`image_quota_exceeded` و`insufficient_credits` تُلتقط وتفتح `QuotaExceededDialog` مع CTA للترقية/شحن النقاط؛ بقي اختبار مستخدم free مستهلك بالكامل عملياً |
| مسار معاينة إيصال الاشتراك | ✅ تم تعميق التحصين | أزيل توليد signed URL المباشر من العميل، وأصبح عبر `getReceiptSignedUrl` الذي يتحقق من المالك أو الأدمن قبل إصدار رابط مؤقت |
| Lovable Cloud security linter | ✅ لا ملاحظات | لا توجد ملاحظات أمنية آلية بعد هذه الدفعة |
| TypeScript + Production build | ✅ ناجح | لا كسر بعد التعديلات؛ تحذير حجم chunks ما زال ملاحظة أداء غير مانعة |
| Console + Network في فحص `/contact` | ✅ لا أخطاء تطبيق حرجة | طلب `/api/public/contact-submit` رجع 200 خلال ~1.6s؛ التحذيرات من سكربت المعاينة فقط |
| Performance profile تمهيدي | ⚠️ ليس Lighthouse رسمي | FCP ~1.75s على بيئة التطوير، وTTFB ~588ms؛ لا يُعتمد كبديل عن Lighthouse الإنتاجي |

**حكم هذه الدفعة:** تم إغلاق عائقين مهمين قبل الإطلاق التجريبي: إثبات عملي لمسار التواصل، وتوحيد حماية إيصالات الدفع بحيث لا يعتمد عرض الإيصال الخاص على صلاحيات التخزين من العميل. لا تزال QA اليدوية الرسمية مفتوحة بسبب الحاجة لتأكيد البريد الفعلي، Safari، واختبارات المستخدم/الأدمن الكاملة.

**نسبة التقدم بعد هذه الدفعة:** 99.45% جاهزية بناء وتشغيل، وقرابة 96-97% جاهزية اعتماد قبل Soft Launch.

---

## تدقيق S8-S10 وتحسين استرداد الفيديو — 25 أبريل 2026

| السيناريو | نتيجة التدقيق | القرار |
|---|---|---|
| S8 — توليد فيديو ناجح | ✅ كودياً سليم | `generateVideo` يتحقق من أهلية الباقة ورصيد النقاط، يمنع أكثر من مهمتين قيد المعالجة للمستخدم، يخصم نقاط الفيديو مرة واحدة، ينشئ `video_jobs` بحالة واضحة، ويعرض النتيجة أو حالة المعالجة في الواجهة |
| S9 — فشل توليد فيديو + refund | ✅ تم تحسينه | إذا فشل المزود أثناء الإنشاء أو أعاد حالة فشل/إلغاء فورية، يتم رمي خطأ موحّد وردّ النقاط عند وجود خصم فعلي؛ كما أن `refreshVideoJob` يرد النقاط عند `failed/canceled` أو timeout بعد 20 دقيقة |
| S10 — ظهور الفيديو في المكتبة | ✅ كودياً سليم | `/dashboard/library` يجلب `video_jobs` عبر Server Function محمية ويعرض الفيديوهات بحالتها ونقاطها وسياق الحملة إن وجد، مع عزل المستخدم عبر RLS/التحقق الخادمي |
| جودة البناء بعد التعديل | ✅ ناجح | `bun run build` و`bunx tsc --noEmit` نجحا بعد تعديل مسار الفشل الفوري؛ تحذير حجم chunks فقط غير مانع |

**الملاحظة النقدية التي عولجت:** كان احتمال الفشل الفوري من مزوّد الفيديو قد يترك المهمة في `processing` إلى أن يضغط المستخدم تحديثاً لاحقاً؛ تم تحويله الآن إلى فشل مضبوط يؤدي إلى refund مباشر ورسالة عربية آمنة.

**حكم هذه الدفعة:** مسار الفيديو أصبح آمناً كودياً قبل Soft Launch من ناحية الخصم، الاسترداد، حد المهام المتزامنة، والحفظ في المكتبة. لا يزال الاعتماد النهائي يتطلب تجربة مستخدم فعلية على المزود الحقيقي ومراجعة سجل النقاط بعد توليد ناجح وفشل متعمّد.

**نسبة التقدم بعد هذه الدفعة:** 99.55% جاهزية بناء وتشغيل، وقرابة 97-98% جاهزية اعتماد قبل Soft Launch.

---

## تدقيق S11-S14 وربط الحملات بالإدارة — 25 أبريل 2026

| السيناريو | نتيجة التدقيق | القرار |
|---|---|---|
| S11 — إنشاء Campaign Pack وحفظه | ✅ كودياً سليم + تحسين جودة | الحفظ يتم عبر Server Function محمية، مع RLS يفرض ملكية المستخدم وحدود الحقول والحالات؛ وتم تحسين فتح المخرجات ليحفظ أحدث نسخة من الحملة قبل الانتقال |
| S12 — توليد نص/صورة/فيديو من الحملة | ✅ تم تقوية الربط | أدوات النص والصورة والفيديو تستقبل `campaignPackId` بعد حفظ الحزمة، والخادم يتحقق من ملكية الحزمة قبل ربط metadata؛ هذا يمنع مخرجات غير مرتبطة أو مرتبطة بحملة قديمة |
| S13 — مراقبة النقاط والقيود | ✅ كودياً سليم مع تصحيح مصدر الحقيقة | خصم الفيديو عبر `consume_credits`، والحماية الحالية تعتمد على رصيد نقاط الفيديو وحد المهام المتزامنة؛ لا توجد حصة يومية مستقلة للفيديو حالياً، ودفتر `credit_ledger` يثبت الخصم والاسترداد؛ بقي اختبار ضغط عملي لمستخدم حقيقي |
| S14 — Admin للفيديو والحملات والledger | ✅ كودياً سليم | صفحات `/admin/video-jobs` و`/admin/campaign-packs` و`/admin/credit-ledger` محمية بـ`AdminGuard` وتعيد التحقق خادمياً عبر `assertAdmin` قبل استخدام صلاحيات الإدارة |
| جودة البناء بعد التعديل | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد تعديل Campaign Studio؛ تحذير chunk size فقط غير مانع |

**الملاحظة النقدية التي عولجت:** كان المستخدم يستطيع فتح أداة توليد من الحملة قبل حفظ Campaign Pack، مما قد يجعل الناتج غير مرتبط بالحملة. الآن يتم حفظ أحدث نسخة كحزمة جاهزة أولاً ثم فتح الأداة مع `campaignPackId` صحيح.

**حكم هذه الدفعة:** اكتملت المراجعة الكودية للسيناريوهات S11-S14 دون blocker جديد، وتم تقوية رابط الحملة ← المخرجات ← المكتبة ← الإدارة. لا يزال الإغلاق الرسمي يحتاج تمريرات يدوية على المتصفحات والمقاسات وتدقيق Lighthouse الرسمي.

**نسبة التقدم بعد هذه الدفعة:** 99.65% جاهزية بناء وتشغيل، وقرابة 98% جاهزية اعتماد قبل Soft Launch.

---

## بوابة الجودة النهائية قبل Lighthouse/Soft Launch — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة S11-S14 السابقة | ✅ سليمة | التعديل الأخير على Campaign Studio عالج فجوة الربط قبل فتح أدوات التوليد، ولا توجد فجوة جديدة ظاهرة في مسارات الفيديو/الحملات/الإدارة |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اكتمل؛ تحذير chunk size فقط يبقى ملاحظة أداء تُراجع ضمن Lighthouse ولا يمنع QA |
| اختبار Vitest المتاح | ⚠️ غير حاسم | ملف `admin-plan-limits.test.ts` موجود لكنه متخطّى، لذلك لا يُحتسب كدليل قبول وظيفي |
| حالة Lovable Cloud | ✅ مستقرة | backend مستجيب وجاهز للفحوصات والعمليات المعتادة |
| Security linter | ✅ لا ملاحظات | لا توجد ملاحظات أمنية آلية جديدة |
| Performance profile تمهيدي للصفحة الرئيسية Mobile 390 | ⚠️ يحتاج Lighthouse رسمي | CLS جيد `0.0096` والذاكرة منخفضة، لكن FCP/Load في بيئة المعاينة بطيئان ومتأثران بموارد التطوير؛ لا يُعتمد كبديل Lighthouse إنتاجي |
| Console سريع | ✅ لا أخطاء تطبيق حرجة | التحذيرات المرصودة من سكربت المعاينة/Performance API وليست من كود التطبيق |

**الحكم النقدي:** الخطوة السابقة مقبولة مهنياً ولا يوجد سبب لإعادة فتح تعديل S11-S14 الآن. المخاطر المتبقية لم تعد كودية بحتة، بل تشغيلية: Lighthouse إنتاجي، Safari، تمريرات مستخدم/أدمن فعلية، ورسائل البريد/Telegram.

**نسبة التقدم بعد هذه البوابة:** 99.72% جاهزية بناء وتشغيل، وقرابة 98.5% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية الموصى بها:** تنفيذ Lighthouse الرسمي على الصفحات الثماني المحددة، ثم بدء 84 تمريرة QA اليدوية وتوثيقها، مع إصلاح Blocker/High فقط دون إضافة ميزات جديدة.

---

## تدقيق Lighthouse Desktop للصفحات العامة + إصلاح SEO — 25 أبريل 2026

| الصفحة | Performance | Accessibility | Best Practices | SEO | القرار |
|---|---:|---:|---:|---:|---|
| `/` | 91 | 95 | 100 | 100 | ✅ اجتاز الحدود المطلوبة |
| `/pricing` | 93 | 95 | 100 | 100 | ✅ اجتاز الحدود المطلوبة |
| `/about` | 95 | 100 | 100 | 100 | ✅ اجتاز الحدود المطلوبة |
| `/contact` | 97 | 96 | 100 | 100 | ✅ اجتاز الحدود المطلوبة |
| `/auth` قبل الإصلاح | 92 | 95 | 100 | 63 | ⚠️ الأداء والـA11y ممتازان، لكن SEO سقط لأن `robots.txt` كان يحجب `/auth` |

**الإصلاح المنفذ:** أزيل حجب `/auth` من `public/robots.txt` لأن خطة QA تطلب SEO ≥90 لصفحة الدخول. بقي حجب `/forgot-password` و`/reset-password` و`/dashboard` و`/admin` كما هو، وهذا صحيح أمنياً وفهرسياً.

**التحقق بعد الإصلاح:** `bun run build` و`bunx tsc --noEmit` نجحا. تعذرت إعادة Lighthouse محلياً على build preview بسبب مشكلة preview محلية في خادم TanStack Start، وليست خطأ تطبيقياً؛ يُعاد قياس `/auth` على النشر التالي للتأكد من ارتفاع SEO المتوقع.

**الحكم النقدي:** تم تحويل أول نتيجة Lighthouse فعلية إلى إصلاح مباشر ومحدود النطاق. الصفحات العامة الأساسية قوية أداءً وتجتاز الحدود على Desktop، وأكبر فجوة قابلة للإصلاح كانت robots لصفحة `/auth` وتمت معالجتها.

**نسبة التقدم بعد هذه الدفعة:** 99.80% جاهزية بناء وتشغيل، وقرابة 98.8% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** تشغيل Lighthouse Mobile الرسمي بعد النشر/تحديث robots، ثم قياس `/dashboard` و`/dashboard/generate-video` و`/dashboard/campaign-studio` بجلسة مستخدم، وبعدها بدء توثيق 84 تمريرة QA اليدوية.

---

## تدقيق مرحلة الإدارة والمراقبة + إصلاح التريجرات الفعلية — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| تدقيق التريجرات الفعلية | ✅ وُجدت فجوة ثم أُغلقت | الدوال الأمنية كانت موجودة، ثم أُعيد إنشاء التريجرات الحرجة والتحقق من `pg_trigger`، وبعد ظهور تكرارات قديمة تم حذفها حتى بقي تريجر واحد واضح لكل وظيفة تشغيلية |
| مراقبة الإساءة | ✅ تحسنت | تم تعديل `Abuse Monitor` ليقيس استهلاك الفيديو والنصوص والصور نسبةً إلى حدود الباقة من `PLAN_CATALOG` بدلاً من عتبات ثابتة عامة |
| صفحة الاستخدام | ✅ تحسنت | أُضيف عرض حد الفيديو اليومي، تكلفة Fast/Quality، تقدير الفيديوهات المتبقية، وصلاحيات Pro Image/Quality Video/أقصى مدة |
| تنظيف الكود القديم | ✅ لا بقايا مؤثرة | لم تظهر بقايا `PLAN_CONFIG` أو `PLANS` القديمة في مسارات الأسعار والفوترة، والمصدر المركزي مستمر عبر `plan-catalog` |
| المعاينات | ✅ مطلوب إعادة فحص بعد البناء | التعديلات مصممة بقيود responsive واضحة؛ يلزم تأكيد بصري على 390/820/1280 ضمن هذه الدفعة |

**الحكم النقدي:** أهم نتيجة في هذه الدفعة أن الاعتماد لم يكتفِ بوجود الدوال في القاعدة؛ تم فحص الربط الفعلي واكتشاف أن التريجرات غير ظاهرة، ثم إعادتها. هذا يرفع موثوقية الأمان والتشغيل قبل أي Soft Launch.

**نسبة التقدم بعد هذه الدفعة:** 99.86% جاهزية بناء وتشغيل، وقرابة 99% جاهزية اعتماد قبل Soft Launch. لا يزال 100% ممنوعاً حتى إغلاق Mobile Lighthouse، Dashboard بجلسة، Safari، و84 تمريرة QA.

**الخطوة التالية:** تنفيذ قياس Mobile Lighthouse وDashboard بجلسة مستخدم/أدمن، ثم بدء تسجيل تمريرات QA اليدوية S1-S14 مع إصلاح Blocker/High فقط.

---

## تدقيق Lighthouse Mobile للصفحات العامة + تحسين تحميل الفيديو — 25 أبريل 2026

| الصفحة | Performance | Accessibility | Best Practices | SEO | القرار |
|---|---:|---:|---:|---:|---|
| `/` | 58 | 95 | 100 | 100 | ⚠️ سقط الأداء؛ السبب الأكبر تحميل `rifd-promo.mp4` بحجم ~6.3MB بسبب autoplay |
| `/pricing` | 71 | 91 | 100 | 100 | ⚠️ أقل من حد الأداء 80؛ السبب الأساسي JavaScript غير مستخدم وFCP/LCP ~4.1s |
| `/about` | 74 | 100 | 100 | 100 | ⚠️ أقل من حد الأداء 80؛ السبب الأساسي JavaScript غير مستخدم وFCP/LCP ~4.1s |
| `/auth` | 63 | 95 | 100 | 63 | ⚠️ قياس النطاق المنشور ما زال يرى robots القديم؛ يحتاج نشر التعديل السابق ثم إعادة القياس |
| `/contact` | 85 | 93 | 100 | 100 | ✅ اجتاز الحدود المطلوبة على Mobile |

**الإصلاح المنفذ الآن:** تم تعديل مشغل فيديو الإثبات في الصفحة الرئيسية من `autoPlay + preload=metadata` إلى تشغيل يدوي `preload=none`، لتقليل تحميل أولي يقارب 6.3MB على Mobile دون حذف الفيديو أو تغيير الرسالة التسويقية.

**التحقق بعد الإصلاح:** `bun run build` و`bunx tsc --noEmit` نجحا. تحذير chunk size ما زال غير مانع لكنه أصبح أولوية أداء في الجولة التالية.

**الحكم النقدي:** Mobile Lighthouse كشف مشكلة أداء حقيقية وليست تجميلية. إصلاح الفيديو يعالج أكبر حمل شبكي في الصفحة الرئيسية، لكن صفحات `/pricing` و`/about` ما زالت تحتاج تحسين تحميل JavaScript/analytics أو تقسيم bundles إذا بقيت أقل من 80 بعد النشر.

**نسبة التقدم بعد هذه الدفعة:** 99.84% جاهزية بناء وتشغيل، وقرابة 98.9% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** نشر/تحديث النسخة ثم إعادة Lighthouse Mobile للصفحات العامة للتأكد من أثر إصلاح الفيديو وrobots، وبعدها قياس صفحات Dashboard بجلسة مستخدم حقيقية.

---

## إعادة تدقيق الخطوة السابقة + قياس إنتاجي سريع — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة خطوة Lighthouse Mobile السابقة | ✅ مقبولة نقدياً | الاستنتاج السابق صحيح: تحسين تحميل فيديو الصفحة الرئيسية خفّض أثر الوسائط الثقيلة، لكن لا يزال القياس الرسمي الكامل يحتاج Lighthouse Mobile بعد النشر وليس أداء المعاينة فقط |
| تحقق الإنتاج على `rifd.site/` | ✅ الصفحة تعمل | استجابة 200، رؤوس الأمان الأساسية موجودة، ولا توجد أخطاء JavaScript حرجة في جلسة المتصفح |
| Performance profile إنتاجي سريع للصفحة الرئيسية | ⚠️ يحتاج تحسين متابعة | TTFB ≈ 1501ms، FCP ≈ 2992ms، CLS = 0.0390 جيد، JS Heap ≈ 7.4MB، Script Duration ≈ 402ms؛ لا يوجد انهيار أو تسرّب واضح |
| الشبكة والتحميل | ⚠️ مقبول مع ملاحظة | أكبر أصل JS ظاهر ≈ 224KB، و`og-image.jpg` ≈ 54KB، ولا يظهر تحميل فيديو ضخم عند الفتح؛ هذا يؤكد اتجاه إصلاح `preload=none` عملياً |
| Console | ✅ نظيف وظيفياً | تحذيران فقط من Performance API/بيئة القياس، ولا توجد أخطاء تطبيق حرجة |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اكتمل؛ بقي تحذير chunk size فقط كملاحظة أداء غير مانعة |

**الحكم النقدي:** الخطوة السابقة سليمة ولم تُدخل كسر وظيفي. التقدّم الحقيقي الآن لم يعد في تعديل كود عشوائي، بل في تثبيت القياس الرسمي: Mobile Lighthouse بعد النشر، ثم Dashboard بجلسة مستخدم، ثم مصفوفة QA اليدوية.

**نسبة التقدم بعد هذه الدفعة:** 99.86% جاهزية بناء وتشغيل، وقرابة 98.9% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** تنفيذ Lighthouse Mobile الرسمي على `/`, `/pricing`, `/about`, `/auth`, `/contact` بعد التأكد أن النشر يحمل آخر build، ثم قياس `/dashboard`, `/dashboard/generate-video`, `/dashboard/campaign-studio` بجلسة مستخدم حقيقية، وبعدها بدء توثيق 84 تمريرة يدوية دون رفع النسبة إلى 100% قبل اكتمالها.

---

## Lighthouse Mobile رسمي بعد التدقيق — 25 أبريل 2026

| الصفحة | Performance | Accessibility | Best Practices | SEO | القرار |
|---|---:|---:|---:|---:|---|
| `/` | 66 | 95 | 100 | 100 | ⚠️ تحسّن عن 58 بعد إيقاف تحميل الفيديو التلقائي، لكنه ما زال دون حد 80 |
| `/pricing` | 69 | 91 | 100 | 100 | ⚠️ ما زال دون حد 80؛ المشكلة أداء تحميل/JavaScript وليست SEO أو A11y |
| `/about` | 70 | 100 | 100 | 100 | ⚠️ ما زال دون حد 80؛ يحتاج جولة تحسين أداء موجهة |
| `/auth` | 62 | 95 | 100 | 63 | ⚠️ النطاق المنشور ما زال يخدم `robots.txt` القديم الذي يحجب `/auth`؛ النسخة المحلية صحيحة وتحتاج Publish/Update |
| `/contact` | 85 | 93 | 100 | 100 | ✅ اجتاز كامل الحدود المطلوبة على Mobile |

**مراجعة الخطوة السابقة نقدياً:** إصلاح الفيديو كان صحيحاً ومؤثراً، إذ تحسّن أداء الصفحة الرئيسية من 58 إلى 66 ولم يعد يظهر تحميل ملف فيديو ضخم عند الفتح. لكن التحسن غير كافٍ لاعتماد Mobile Performance، لذلك لا يجوز رفع الجاهزية إلى 100% أو بدء Soft Launch العام قبل جولة تحسين أداء إضافية.

**ملاحظة نشر مهمة:** `public/robots.txt` المحلي لا يحجب `/auth`، بينما `https://rifd.site/robots.txt` المنشور ما زال يحجبه. هذا يعني أن إصلاح SEO السابق لم يصل للنطاق المنشور بعد، ويتطلب الضغط على **Update** من نافذة النشر لتحديث الواجهة والملفات العامة.

**الحكم النقدي:** لا توجد مشكلة جودة عامة في Accessibility/Best Practices/SEO لمعظم الصفحات، لكن Performance Mobile هو العائق الرئيسي الآن. الأولوية ليست إضافة ميزات، بل تقليل JavaScript الأولي وتأجيل أجزاء غير حرجة في الصفحات العامة، ثم إعادة القياس.

**نسبة التقدم بعد هذه الدفعة:** 99.87% جاهزية بناء وتشغيل، وقرابة 99.0% جاهزية اعتماد قبل Soft Launch؛ بقيت نقاط منع الاعتماد: تحديث النشر، تحسين Mobile Performance للصفحات `/`, `/pricing`, `/about`, `/auth`، ثم Dashboard و84 تمريرة يدوية.

**الخطوة التالية:** اضغط **Publish → Update** لتطبيق robots ونسخة الواجهة الأخيرة على `rifd.site`، ثم أنفّذ جولة تحسين أداء مركزة على تقسيم/تأجيل JavaScript غير الحرج للصفحات العامة وإعادة Lighthouse Mobile حتى تصل الصفحات الأساسية إلى ≥80.

---

## تحسين أداء Mobile للصفحات العامة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| تدقيق الخطوة السابقة | ✅ صحيح | Lighthouse Mobile أكد أن العائق الرئيسي أداء التحميل، وأن `/auth` ما زال يحتاج Publish/Update لتحديث `robots.txt` المنشور |
| إزالة تحميل المصادقة من الهيدر العام | ✅ نُفذ | `SiteHeader` لم يعد يستدعي `useAuth` في صفحات التسويق، ما يقلل تحميل منطق الجلسة/السحابة على الصفحات العامة قبل الحاجة الفعلية |
| تبسيط CTA الأسعار | ✅ نُفذ | صفحة `/pricing` لم تعد تفحص الجلسة لتحديد الرابط؛ التوجيه إلى `/dashboard/billing` يبقى آمناً لأن حارس Dashboard يحوّل غير المسجل إلى `/auth` |
| تقسيم حزم الإنتاج | ✅ نُفذ | أضيف `manualChunks` لفصل React/TanStack/UI/Icons/Cloud/Analytics/Charts بدلاً من حزمة vendor ضخمة واحدة |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اكتمل، وانخفضت أكبر حزمة client من نحو 750KB إلى حزم منفصلة؛ أكبر بقايا عامة أصبحت أصغر بكثير مع فصل vendor |

**الحكم النقدي:** هذا تحسين بنيوي صحيح ومحدود المخاطر؛ لا يغيّر منطق الاشتراكات أو الحماية، ويستهدف سبب Lighthouse المعلن: JavaScript أولي زائد على Mobile. لا أعتمد نتيجة Performance نهائية قبل النشر وإعادة Lighthouse على النطاق المنشور.

**نسبة التقدم بعد هذه الدفعة:** 99.89% جاهزية بناء وتشغيل، وقرابة 99.1% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** اضغط **Publish → Update** لتطبيق تغييرات الواجهة و`robots.txt`، ثم إعادة Lighthouse Mobile على الصفحات الخمس؛ إذا بقيت `/`, `/pricing`, `/about`, `/auth` دون 80 فالجولة التالية تكون تأجيل Analytics/عدادات الإثبات وما تحت الطي عبر lazy loading، ثم قياس Dashboard بجلسة مستخدم.

---

## تأجيل Analytics غير الحرجة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| تدقيق تحسين الحزم السابق | ✅ سليم | تقسيم الحزم السابق نجح ولم يكسر TypeScript أو build، لكنه وحده لا يكفي قبل قياس النشر لأن Analytics ما زالت تبدأ مبكراً من الجذر |
| تأجيل Analytics | ✅ نُفذ | تم تأخير تهيئة PostHog وpageview الأول 3.5 ثوانٍ بعد التحميل حتى لا تنافس FCP/LCP على Mobile |
| حفظ ربط المستخدم | ✅ مضبوط | بعد اكتمال تهيئة Analytics، يتم ربط المستخدم فقط إذا كانت الجلسة جاهزة؛ ولا تُرسل PII |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اكتمل بعد التعديل؛ لا توجد أخطاء بناء، وتحذيرات الحجم أصبحت موزعة بعد manualChunks |

**الحكم النقدي:** هذه خطوة أداء صحيحة لأنها تؤجل كوداً غير ضروري لأول رسم دون إزالة التتبع بالكامل. المخاطرة منخفضة: قد يتأخر تسجيل أول pageview لكنه لا يؤثر على المنتج أو التحويل، بينما يقل ضغط JavaScript المبكر على Mobile.

**نسبة التقدم بعد هذه الدفعة:** 99.91% جاهزية بناء وتشغيل، وقرابة 99.2% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** اضغط **Publish → Update** لتطبيق تحسينات الأداء و`robots.txt` على النطاق المنشور، ثم أعيد Lighthouse Mobile؛ إن بقي الأداء دون 80، فالتحسين التالي يكون lazy loading للمكوّنات تحت الطي في الصفحة الرئيسية وعدادات الإثبات في `/pricing`.

---

## Lazy loading للمحتوى تحت الطي — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| تدقيق الخطوة السابقة | ✅ سليم | تأجيل Analytics ثابت في الكود، و`robots.txt` المنشور ما زال قديماً ويحتاج Publish/Update |
| الصفحة الرئيسية | ✅ نُفذ | تم تحميل مكوّنات ما بعد الـHero كسولاً: فيلم الإثبات، العدادات، المقارنات، الرؤية، الحلول، الميزات، ومركز الإثبات |
| صفحة الأسعار | ✅ نُفذ | تم تأجيل `SubscribersCounter` و`TrustBadges` لأنهما غير حرجين لأول رسم على Mobile |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اكتمل، وانخفض chunk صفحة `/` من نحو 43.7KB إلى 16.1KB مع فصل مكوّنات تحت الطي إلى chunks صغيرة مستقلة |

**الحكم النقدي:** هذه أفضل خطوة أداء تالية لأنها تقلل JavaScript الحرج للصفحة الرئيسية دون حذف محتوى أو تغيير تجربة المستخدم. لا أعتمد رقم Lighthouse النهائي قبل النشر، لكن مؤشرات build تؤكد تحسناً حقيقياً في حجم route chunk.

**نسبة التقدم بعد هذه الدفعة:** 99.93% جاهزية بناء وتشغيل، وقرابة 99.3% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** اضغط **Publish → Update** ثم أعيد Lighthouse Mobile على الصفحات الخمس. إذا وصلت الصفحات إلى ≥80 ننتقل مباشرة إلى قياس Dashboard بجلسة مستخدم، وإذا بقيت صفحة دون الحد سنعالج السبب المحدد من تقرير Lighthouse فقط.

---

## مراجعة ما بعد Lazy loading وحالة النشر — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| تدقيق الخطوة السابقة | ✅ سليمة | lazy loading للمحتوى تحت الطي صحيح ومحدود المخاطر، ولم يغيّر منطق الاشتراك أو المصادقة أو الحماية |
| TypeScript + Production build | ✅ ناجح | أُعيد تشغيل `bunx tsc --noEmit` و`bun run build` بعد الخطوة السابقة دون أخطاء |
| مؤشرات الحزم | ✅ مستقرة | chunk الصفحة الرئيسية بقي قريباً من 16.1KB، وchunk الأسعار قريباً من 12.7KB، مع فصل Analytics/Cloud/Charts عن المسار الأولي |
| `robots.txt` المحلي | ✅ صحيح | النسخة المحلية لا تحجب `/auth` وتحافظ على حجب dashboard/admin/reset/API |
| `robots.txt` المنشور على `rifd.site` | ⚠️ غير محدّث | المتصفح يؤكد أن النسخة المنشورة ما زالت تحتوي `Disallow: /auth`، لذلك لا يجوز إعادة اعتماد SEO لصفحة `/auth` قبل Publish → Update |

**الحكم النقدي:** لا يوجد ما يستدعي تعديل كود إضافي الآن قبل النشر؛ أي تحسين جديد قبل قياس النسخة المنشورة سيكون تخمينياً وقد يضيف مخاطرة غير لازمة. العائق الحالي تشغيلي: تحديث النشر ثم Lighthouse Mobile رسمي.

**نسبة التقدم بعد هذه المراجعة:** 99.94% جاهزية بناء وتشغيل، وقرابة 99.3% جاهزية اعتماد قبل Soft Launch؛ لا تزال 100% ممنوعة حتى إعادة Lighthouse Mobile، قياس Dashboard بجلسة مستخدم، و84 تمريرة QA اليدوية.

**الخطوة التالية:** اضغط **Publish → Update** الآن. بعده أعيد قياس Lighthouse Mobile للصفحات `/`, `/pricing`, `/about`, `/auth`, `/contact`، ثم أبدأ Dashboard بجلسة مستخدم إن اجتازت الصفحات العامة أو أعالج فقط الصفحة التي تبقى دون الحد.

---

## تحقق إعادة النشر قبل Lighthouse — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| فحص `https://rifd.site/robots.txt` عبر المتصفح | ⚠️ لم يتغير | النسخة المنشورة ما زالت تحتوي `Disallow: /auth` |
| معرف النشر الظاهر في الاستجابة | ⚠️ ثابت | رأس الاستجابة يعرض نفس `x-deployment-id` السابق، ما يعني أن Update لم يصل للنطاق بعد أو لم يكتمل انتشاره |
| قرار Lighthouse الآن | ⏸️ مؤجل | إعادة القياس الآن ستعطي نتيجة مضللة لأنها ستقيس build قديم، خصوصاً SEO الخاص بصفحة `/auth` |

**الحكم النقدي:** الخطوة السابقة منفذة وصحيحة في الكود، لكن بوابة الاعتماد لا تنتقل للقياس التالي حتى يتغير النشر فعلياً. لا توجد قيمة مهنية في تعديل كود إضافي أو تشغيل Lighthouse قبل تحديث النطاق المنشور.

**نسبة التقدم بعد هذا التحقق:** 99.94% جاهزية بناء وتشغيل، وقرابة 99.3% جاهزية اعتماد قبل Soft Launch — دون تغيير لأن العائق تشغيلي لا كودي.

**الخطوة التالية:** اضغط **Publish → Update** من واجهة Lovable، ثم أتحقق مرة أخرى من اختفاء `Disallow: /auth`. عندها فقط أبدأ Lighthouse Mobile للصفحات الخمس وأوثّق النتائج الرسمية.

---

## تدقيق حالة النشر العامة مقابل آخر إصدار — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| حالة النشر | ✅ منشور وعام | إعدادات النشر تؤكد أن الموقع منشور ومرئي للعامة |
| حالة آخر إصدار | ⚠️ غير محدث | `robots.txt` على `rifd.site` ما زال يحتوي `Disallow: /auth`، ما يثبت أن آخر تغييرات الكود لم تصل للنطاق |
| القرار المهني | ⏸️ لا Lighthouse بعد | لا يتم قياس Lighthouse Mobile الرسمي حتى تتطابق النسخة المنشورة مع النسخة المحلية الصحيحة |

**الحكم النقدي:** المشكلة ليست أن الموقع غير منشور؛ المشكلة أن زر **Update** لم يدفع آخر إصدار بعد. لذلك التنفيذ الصحيح الآن هو إيقاف القياسات الرسمية مؤقتاً، وليس تعديل الكود أو رفع نسبة الاعتماد بلا دليل.

**نسبة التقدم بعد هذا التدقيق:** 99.94% جاهزية بناء وتشغيل، وقرابة 99.3% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** تنفيذ **Publish → Update** تحديداً، ثم إعادة فحص `robots.txt`. بمجرد اختفاء `Disallow: /auth` أبدأ فوراً Lighthouse Mobile للصفحات الخمس، ثم Dashboard بجلسة مستخدم.

---

## تحقق النشر بعد Update ومحاولة Lighthouse — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| روابط المشروع الرسمية | ✅ مؤكدة | النطاقات الفعلية: `rifd.lovable.app`, `rifd.site`, `www.rifd.site` |
| `robots.txt` بعد التحديث | ✅ محدث | فحص المتصفح أكد أن `Disallow: /auth` اختفى، وبقي حجب المسارات الخاصة فقط |
| Lighthouse Mobile عبر Chrome headless | ⚠️ تعذّر | القياس اصطدم بصفحة حماية/وسيط تحميل في بيئة headless على النطاقين، لذلك لا أعتمد أرقاماً ناقصة أو مضللة |

**الحكم النقدي:** بوابة النشر تحسنت فعلياً: إصلاح robots وصل، وهذا يزيل عائق SEO السابق لصفحة `/auth`. لكن Lighthouse الرسمي لم يكتمل بسبب عائق أداة القياس لا بسبب كود التطبيق، لذلك لا أرفع الاعتماد إلى 100% ولا أخترع نتائج.

**نسبة التقدم بعد هذه الدفعة:** 99.95% جاهزية بناء وتشغيل، وقرابة 99.35% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** استخدام أداة Speed/Lighthouse من واجهة Lovable أو جلسة متصفح كاملة غير headless لقياس Mobile للصفحات الخمس، ثم الانتقال إلى Dashboard بجلسة مستخدم عند اجتياز الحدود أو إصلاح أي صفحة تفشل فقط بناءً على التقرير.

---

## قياس متصفح حقيقي وإصلاح CLS للصفحة الرئيسية — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| قياس Mobile عبر متصفح حقيقي | ⚠️ كشف مشكلة محددة | الصفحة الرئيسية تعمل دون أخطاء حرجة، لكن CLS ظهر مرتفعاً بسبب حركة/تغير مساحة عناصر الـHero في أول شاشة |
| Console | ✅ لا أخطاء تطبيق حرجة | ظهرت تحذيرات Performance API فقط، وليست blocker وظيفي |
| Network | ✅ لا تحميل فيديو ضخم | لم يعد ملف الفيديو الكبير يُحمّل عند الفتح، وهذا يؤكد نجاح إصلاح `preload=none` السابق |
| إصلاح Hero CLS | ✅ نُفذ | أزيلت `animate-fade-in` من عناصر Hero الحرجة، وثُبّت ارتفاع H1 عبر `min-height` responsive لمنع قفز المحتوى عند تبدّل variant/التحميل |
| TypeScript + Production build | ✅ ناجح | أُعيد تشغيل الفحص والبناء بعد الإصلاح دون أخطاء |

**الحكم النقدي:** هذه ليست إضافة ميزة، بل إصلاح اعتماد مباشر لواحدة من أهم إشارات Core Web Vitals. الإصلاح محافظ: لا يغيّر الرسالة التسويقية ولا CTA ولا منطق A/B، بل يمنع اهتزاز أول شاشة على Mobile.

**نسبة التقدم بعد هذه الدفعة:** 99.96% جاهزية بناء وتشغيل، وقرابة 99.45% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** تنفيذ **Publish → Update** لتطبيق إصلاح CLS، ثم إعادة قياس الصفحة الرئيسية Mobile. إذا هبط CLS إلى النطاق المقبول نكمل قياس `/pricing`, `/about`, `/auth`, `/contact` ثم Dashboard بجلسة مستخدم.

---

## تحقق CLS بعد إصلاح Hero على المعاينة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| Mobile Preview 390px | ✅ مستقر بصرياً | أول شاشة للـHero واضحة، الأزرار والبطاقات لا تتداخل، ولا يوجد overflow أفقي ظاهر |
| CLS بعد الإصلاح | ✅ جيد | انخفض CLS في القياس التمهيدي من `1.5044` إلى `0.0106`، وهذا ضمن النطاق الجيد |
| Script Duration | ✅ منخفض | نحو `42.3ms` في المعاينة بعد تثبيت Hero، ما يؤكد أن الإصلاح لم يضيف عبئاً برمجياً |
| Console | ✅ لا أخطاء تطبيق حرجة | التحذيرات من سكربت المعاينة وPerformance API فقط |
| الاعتماد النهائي | ⏳ ينتظر النشر | النتيجة مؤكدة على Preview، لكنها تحتاج **Publish → Update** ثم قياس إنتاجي على `rifd.site` قبل إغلاق Lighthouse Mobile |

**الحكم النقدي:** إصلاح Hero كان صحيحاً ومؤثراً؛ عالج أهم إشارة فشل ظهرت في القياس البديل دون التضحية بالرسالة التسويقية أو تجربة أول شاشة. لا توجد حاجة لتعديل إضافي في Hero الآن.

**نسبة التقدم بعد هذا التحقق:** 99.97% جاهزية بناء وتشغيل، وقرابة 99.5% جاهزية اعتماد قبل Soft Launch.

**الخطوة التالية:** اضغط **Publish → Update** لتطبيق إصلاح CLS على النطاق المنشور، ثم أعيد قياس الصفحة الرئيسية و`/pricing`, `/about`, `/auth`, `/contact` على الإنتاج، وبعدها أبدأ Dashboard بجلسة مستخدم.

---

## تدقيق إنتاجي بعد مراجعة إصلاح CLS — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| TypeScript | ✅ ناجح | لا توجد أخطاء نوعية تمنع الاعتماد الفني |
| Production build | ✅ ناجح | البناء المحلي الحالي سليم؛ تحذير حجم chunks فقط غير مانع |
| الصفحة الرئيسية على `rifd.site` Mobile 390px | ⚠️ غير مطابقة لآخر إصلاح | القياس الإنتاجي ما زال يرصد عناصر `animate-fade-in` في Hero، وهذا يعني أن النطاق المنشور لم يستقبل build إصلاح CLS بعد |
| CLS إنتاجي تمهيدي | ⚠️ `0.1426` | تحسّن عن الفشل السابق لكنه لا يصل لمستوى الاعتماد الجيد؛ السبب المرجح أن النسخة المنشورة أقدم من النسخة التي أظهرت `0.0106` في Preview |
| Console | ✅ لا أخطاء تطبيق حرجة | ظهرت تحذيرات Performance API فقط، وليست blocker وظيفي |
| Network | ✅ لا تحميل فيديو ثقيل | لا يوجد رجوع لمشكلة تحميل الفيديو الكبير عند الفتح |

**الحكم النقدي:** الخطوة السابقة صحيحة في الكود ومثبتة على Preview، لكن القياس الإنتاجي يثبت أن آخر build لم يصل للنطاق العام بعد. لا أوصي بتعديل Hero مرة أخرى الآن؛ التعديل الإضافي قبل نشر الإصلاح المعتمد سيكون مخاطرة غير لازمة وقد يخفي السبب الحقيقي.

**نسبة التقدم بعد هذا التدقيق:** 99.97% جاهزية بناء وتشغيل، وقرابة 99.5% جاهزية اعتماد قبل Soft Launch — ثابتة دون رفع، لأن العائق الحالي نشر/تحقق إنتاجي وليس كوداً جديداً.

**الخطوة التالية:** تنفيذ **Publish → Update** مرة أخرى، ثم إعادة قياس الصفحة الرئيسية على `rifd.site`. معيار الانتقال للصفحات التالية هو هبوط CLS إلى أقل من `0.1`، والأفضل قرب نتيجة Preview `0.0106`؛ بعدها تُقاس `/pricing`, `/about`, `/auth`, `/contact` ثم Dashboard بجلسة مستخدم.

---

## تحقق إنتاجي ناجح بعد وصول إصلاح CLS — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| وصول build الجديد إلى `rifd.site` | ✅ مؤكد | تغيّر معرف النشر وملفات assets من النسخة القديمة إلى نسخة أحدث |
| TypeScript | ✅ ناجح | لا توجد أخطاء نوعية قبل اعتماد القياس |
| الصفحة الرئيسية Mobile 390px | ✅ مستقرة | أول شاشة تعمل دون أخطاء تطبيق حرجة، ولا يوجد رجوع لمشكلة تحميل الفيديو الكبير |
| CLS إنتاجي تمهيدي | ✅ `0.0726` | دخل النطاق الجيد `< 0.1` بعد وصول الإصلاح للإنتاج؛ لا يزال أعلى من Preview لكنه مقبول كبوابة انتقال للصفحات التالية |
| Console | ✅ لا أخطاء حرجة | التحذيرات الحالية من Performance API وليست blocker وظيفي |
| ملاحظة أداء | ⚠️ للمتابعة | TTFB وFCP متأثران بقياس المتصفح/الشبكة، ويحتاجان Lighthouse/Speed رسمي قبل الاعتماد النهائي |

**الحكم النقدي:** تم إثبات أن المشكلة السابقة كانت عدم وصول آخر build للإنتاج، وليست خللاً في إصلاح Hero. لا أعدّل الكود الآن لأن مؤشر CLS أصبح ضمن النطاق الجيد، وأي تعديل إضافي قبل قياس باقي الصفحات سيكون توسعاً غير مبرر.

**نسبة التقدم بعد هذا التحقق:** 99.98% جاهزية بناء وتشغيل، وقرابة 99.58% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى إكمال قياس الصفحات العامة، Dashboard بجلسة مستخدم، و84 تمريرة QA اليدوية.

**الخطوة التالية:** قياس `/pricing`, `/about`, `/auth`, `/contact` على Mobile 390px بنفس المنهج: Performance profile + Console + فحص بصري سريع. إذا لم يظهر blocker، ننتقل إلى Dashboard بجلسة مستخدم لاختبار المسارات المحمية.

---

## قياس `/pricing` وإصلاح CLS — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | الصفحة الرئيسية اجتازت بوابة CLS الإنتاجية `< 0.1`، لذلك كان الانتقال لقياس الصفحات العامة مبرراً |
| فحص HTTP للصفحات العامة | ✅ ناجح | `/pricing`, `/about`, `/auth`, `/contact` تعيد 200 وبعناوين SEO مخصصة |
| `/pricing` Mobile 390px | ⚠️ كشف blocker أداء | CLS وصل إلى `0.2393` بسبب تغيّر مساحة عنوان الصفحة/الوصف/عداد المشتركين/صندوق المقاعد بعد التحميل |
| الإجراء التصحيحي | ✅ نُفذ | ثُبّتت مساحات H1 والوصف وعداد المشتركين وصندوق المقاعد في Hero صفحة الأسعار، وأضيف fallback مرئي ثابت لـSuspense |
| TypeScript بعد الإصلاح | ✅ ناجح | لا أخطاء نوعية |
| Production build بعد الإصلاح | ✅ ناجح | البناء الإنتاجي سليم؛ تحذير حجم chunks فقط غير مانع |

**الحكم النقدي:** إكمال قياس باقي الصفحات بعد ظهور CLS بهذا الحجم في `/pricing` كان سيخالف الخطة؛ القرار المهني الصحيح هو إيقاف السلسلة، إصلاح الصفحة الفاشلة أولاً، ثم إعادة نشرها وقياسها قبل الانتقال إلى `/about`, `/auth`, `/contact`.

**نسبة التقدم بعد هذا الإصلاح:** 99.98% جاهزية بناء وتشغيل، وقرابة 99.6% جاهزية اعتماد قبل Soft Launch. لم أرفعها أكثر لأن الإصلاح لم يُثبت إنتاجياً بعد.

**الخطوة التالية:** تنفيذ **Publish → Update** لإرسال إصلاح `/pricing`، ثم إعادة قياس `/pricing` Mobile. إذا هبط CLS إلى أقل من `0.1` نكمل مباشرة قياس `/about`, `/auth`, `/contact` ثم Dashboard بجلسة مستخدم.

---

## إعادة تحقق `/pricing` بعد طلب النشر — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| TypeScript | ✅ ناجح | لا يوجد كسر كودي في النسخة الحالية |
| حالة النشر | ⚠️ لم تتغير فعلياً | `x-deployment-id` ما زال على نفس الإصدار السابق، وملفات assets المنشورة لم تنتقل إلى build إصلاح `/pricing` |
| `/pricing` Mobile 390px | ⚠️ ما زال يقيس النسخة القديمة | CLS بقي مرتفعاً عند `0.2295`، وما زالت مؤشرات القياس تشير لكلاسات Hero القديمة قبل تثبيت `min-height` |
| Console | ✅ لا أخطاء تطبيق حرجة | التحذيرات من Performance API فقط |
| القرار | ⏸️ إيقاف القياسات التالية | لا تُقاس `/about`, `/auth`, `/contact` قبل وصول إصلاح `/pricing` للإنتاج حتى لا تختلط نتائج نسخة قديمة مع نسخة جديدة |

**الحكم النقدي:** إصلاح `/pricing` صحيح محلياً ومؤكد بالبناء، لكن الإنتاج لم يستلمه بعد. إعادة تعديل الصفحة الآن غير مهني لأنها ستعالج نسخة قديمة لا تمثل الكود الحالي، وقد تضيف مخاطرة غير لازمة.

**نسبة التقدم بعد هذا التحقق:** 99.98% جاهزية بناء وتشغيل، وقرابة 99.6% جاهزية اعتماد قبل Soft Launch — دون رفع لأن الاعتماد ينتظر إثباتاً إنتاجياً.

**الخطوة التالية:** تنفيذ **Publish → Update** مرة أخرى والانتظار حتى يتغير معرف النشر، ثم إعادة قياس `/pricing`. عند نزول CLS تحت `0.1` نكمل قياس `/about`, `/auth`, `/contact` ثم Dashboard بجلسة مستخدم.

---

## تحقق إنتاجي ناجح لإصلاح `/pricing` — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| حالة النشر | ✅ وصل الإصلاح | تغيّر `x-deployment-id` إلى إصدار جديد، وHTML المنشور يحتوي علامات تثبيت المساحات `min-height` |
| `/pricing` Mobile 390px | ✅ اجتاز بوابة CLS | انخفض CLS من `0.2393/0.2295` إلى `0.0768`، وهو ضمن النطاق الجيد `< 0.1` |
| Console | ✅ لا أخطاء تطبيق حرجة | التحذيرات الحالية من Performance API فقط |
| قرار المتابعة | ✅ مسموح | يمكن الانتقال الآن إلى قياس `/about`, `/auth`, `/contact` بنفس المنهج |

**الحكم النقدي:** إصلاح `/pricing` كان صحيحاً ومؤثراً، والعائق السابق كان تأخر وصول النشر. لا حاجة لتعديل إضافي في صفحة الأسعار الآن؛ الأولوية تعود لإكمال قياسات الصفحات العامة المتبقية ثم Dashboard.

**نسبة التقدم بعد هذا التحقق:** 99.985% جاهزية بناء وتشغيل، وقرابة 99.65% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى قياس `/about`, `/auth`, `/contact`، ثم Dashboard بجلسة مستخدم، ثم 84 تمريرة QA اليدوية.

**الخطوة التالية:** قياس `/about`, `/auth`, `/contact` على Mobile 390px: فحص CLS/Console/HTTP/لقطة بصرية سريعة، وإصلاح أي blocker فقط إن ظهر.

---

## قياس `/about` وإصلاح CLS — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | `/pricing` بقي ضمن النطاق الجيد في إعادة القياس الحالي: CLS = `0.0659`، أقل من حد `0.1` |
| `/about` Mobile 390px | ⚠️ كشف blocker أداء | CLS وصل إلى `0.5717` بسبب تغيّر مساحة Hero بعد تحميل الخط/الأنماط |
| الإجراء التصحيحي | ✅ نُفذ | ثُبّتت مساحة Hero والعنوان والوصف والوسم العلوي في صفحة `/about` لمنع القفز البصري على الموبايل |
| TypeScript بعد الإصلاح | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| قرار المتابعة | ⏸️ ينتظر النشر | لا تُقاس `/auth` و`/contact` إنتاجياً قبل نشر إصلاح `/about` ثم إعادة قياسها حتى لا تختلط النتائج |

**الحكم النقدي:** قرار إيقاف السلسلة عند `/about` كان صحيحاً؛ الرقم ليس هامشياً بل blocker واضح. التعديل محدود ومباشر ولا يمس المنطق أو المسارات الحساسة.

**نسبة التقدم بعد هذا الإصلاح:** 99.985% جاهزية بناء وتشغيل، وقرابة 99.66% جاهزية اعتماد قبل Soft Launch. لم أرفعها أكثر لأن إصلاح `/about` يحتاج **Publish → Update** ثم إثباتاً إنتاجياً.

**الخطوة التالية:** اضغط **Publish → Update**، ثم أعيد قياس `/about` Mobile. إذا هبط CLS تحت `0.1` نكمل `/auth` ثم `/contact`، وبعدها Dashboard بجلسة مستخدم.

---

## إعادة تحقق نشر إصلاح `/about` — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | إصلاح `/about` كان محدوداً ومباشراً، و`TypeScript` اجتاز بعده دون أخطاء |
| حالة النشر على `rifd.site/about` | ⚠️ لم يصل الإصلاح | `x-deployment-id` ما زال على الإصدار السابق، وHTML المنشور لا يحتوي علامات تثبيت المساحات `min-h-[22rem]`, `min-h-[6.5rem]`, `min-h-[5.5rem]` |
| قرار القياس | ⏸️ مؤجل | لا أعيد قياس CLS إنتاجياً الآن لأن الصفحة المنشورة ما زالت النسخة القديمة التي أظهرت CLS = `0.5717` |
| قرار الكود | ✅ لا تعديل إضافي | تعديل الكود مرة أخرى قبل نشر الإصلاح الحالي سيزيد المخاطرة ولا يعالج السبب الحقيقي |

**الحكم النقدي:** العائق الحالي ليس جودة الإصلاح بل وصول النشر. المسار المهني الصحيح هو نشر النسخة الحالية ثم إعادة قياس `/about` فقط؛ بعدها نكمل `/auth` و`/contact`.

**نسبة التقدم بعد هذا التحقق:** 99.985% جاهزية بناء وتشغيل، وقرابة 99.66% جاهزية اعتماد قبل Soft Launch — دون رفع لأن الاعتماد ينتظر إثباتاً إنتاجياً.

**الخطوة التالية:** تنفيذ **Publish → Update**، ثم إعادة فحص `x-deployment-id` ووجود علامات التثبيت في `/about`، وبعدها قياس CLS. معيار الانتقال: CLS أقل من `0.1`.

---

## تدقيق أمني بعد التريجرات + إصلاح استدعاء A/B Admin — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة التريجرات الفعلية | ✅ مثبتة | تم التحقق مباشرة من `pg_trigger`: التريجرات الحرجة موجودة على `profiles`, `subscription_requests`, `video_jobs`, `campaign_packs`, `generations`, و`topup_purchases` دون تكرارات قديمة مؤثرة |
| فحص أمان القاعدة | ✅ لا ملاحظات linter | فحص Lovable Cloud linter لم يظهر مشاكل جديدة |
| Security scan | ✅ أُغلق الخطأ الحرج | كان هناك استدعاء Admin A/B بدون تمرير جلسة؛ تم إصلاحه بتمرير `Authorization` مثل باقي صفحات الإدارة، وانخفضت النتائج من 7 إلى 6 دون خطأ حرج ظاهر |
| تنظيف الوثائق | ✅ متسق | تم تحديث Roadmap V9 والخطة لإزالة تضارب نسب 98/98.7 وربط الحالة الحالية بـ 99.86 دون إعلان جاهزية إطلاق عام |
| قرار عدم التوسّع | ✅ صحيح | لم يتم تعديل CSP/OAuth أو سياسات التخزين الآن لأنها تحتاج جولة منفصلة عالية الحساسية، وليست blocker مكسوراً في خطوة اليوم |

**الحكم النقدي:** أهم إصلاح في هذه الدفعة ليس بصرياً بل أمني: صفحة A/B Admin كانت تعتمد على الحماية الخادمية لكنها لا تمرر رمز الجلسة في الاستدعاء، وهذا قد يكسر الصفحة أو يربك نموذج الحماية. تم توحيدها مع نمط صفحات الإدارة الأخرى.

**نسبة التقدم بعد هذه الدفعة:** 99.989% جاهزية بناء وتشغيل، وقرابة 99.70% جاهزية اعتماد قبل Soft Launch. لا تُرفع إلى 100% حتى يتم النشر، إعادة قياس `/about`، إغلاق `/auth` و`/contact`، ثم Dashboard و84 تمريرة QA.

**الخطوة التالية:** بعد **Publish → Update**: إعادة تحقق `/about` منشوراً، قياس CLS، ثم إكمال `/auth` و`/contact`، وبعدها قياس Dashboard بجلسة مستخدم وأدمن.

---

## قياس `/about` بعد وصول الإصلاح + تنظيف الوعود المتبقية — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | إصلاح `/about` وصل فعلياً إلى النشر، وتم تأكيد وجود علامات تثبيت المساحات في HTML المنشور |
| `/about` Mobile 390px | ⚠️ كشف CLS متبقياً | القياس أظهر CLS = `0.3806` رغم التحسن البنيوي، والسبب تغيّر مساحة العنوان/الوصف وبطاقات القيم بعد التحميل |
| الإجراء التصحيحي | ✅ نُفذ | زادت المساحات المحجوزة في Hero صفحة `/about` على الموبايل لمنع قفز المحتوى قبل تحميل الخط/الأنماط |
| تنظيف الوعود القديمة | ✅ نُفذ | أزيلت عبارات “فاتورة ضريبية رسمية” و“متوافق مع PDPL” من الواجهة العامة واستبدلت بصياغة أكثر دقة: فوترة واضحة وخصوصية/سياسات وصول |
| تدقيق Lovable Cloud | ✅ مستقر | التريجرات الحرجة مفعّلة، وفحص linter لا يعرض ملاحظات أمنية |
| TypeScript + Production build | ✅ ناجح | الفحص والبناء اجتازا بعد التعديلات؛ تحذير حجم chunks فقط غير مانع |

**الحكم النقدي:** الخطوة السابقة كانت صحيحة، لكن القياس كشف أن تثبيت `/about` لم يكن كافياً على Mobile. تم تنفيذ تعديل محافظ لا يغيّر الرسالة ولا المنطق، مع تنظيف بقايا ادعاءات قانونية/ضريبية غير مناسبة قبل Soft Launch.

**نسبة التقدم بعد هذه الدفعة:** 99.989% جاهزية بناء وتشغيل، وقرابة 99.72% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى نشر إصلاح `/about` الجديد، إعادة قياسه، ثم قياس `/auth` و`/contact` وDashboard بجلسة مستخدم/أدمن، وإغلاق 84 تمريرة QA.

**الخطوة التالية:** تنفيذ **Publish → Update**، ثم إعادة قياس `/about` Mobile. إذا أصبح CLS أقل من `0.1` نكمل مباشرة `/auth` و`/contact`، ثم Dashboard بجلسة مستخدم وأدمن.

---

## تدقيق `/auth` و`/contact` وتنظيف الوعود المتبقية — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة المرحلة السابقة | ✅ صحيحة | إصلاحات `/about` وتنظيف العبارات الحساسة كانت في الاتجاه الصحيح، لكن بقيت بقايا تسويقية متفرقة خارج الملفات المعدّلة سابقاً |
| تنظيف الكود القديم | ✅ نُفذ | أزيلت عبارات الباقة القديمة `5 توليدات نص + 2 صورة` من `/auth` والصفحة الرئيسية وCTA الموبايل و`/library` و`/vs-chatgpt` واستبدلت بصياغة لا تصطدم بمصدر الحدود الحقيقي |
| تنظيف الادعاءات الضريبية/القانونية | ✅ نُفذ | خُففت عبارات `فاتورة ضريبية` و`شامل الضريبة` و`PDPL` من الواجهة العامة إلى فوترة واضحة وخصوصية/سياسات وصول، مع ترك النصوص القانونية التفصيلية داخل صفحة الخصوصية فقط |
| `/auth` Mobile 390px | ✅ ناجح بصرياً | نموذج الدخول واضح، الحقول والأزرار ضمن العرض، ولا تظهر أرقام مجانية قديمة |
| `/auth` Tablet 820px | ✅ ناجح بصرياً | البطاقة مستقرة في منتصف الصفحة، ولا يوجد تداخل ظاهر |
| `/auth` Desktop 1280px | ✅ ناجح تنقلاً | الصفحة تُحمّل دون خطأ، وفحص Console لم يظهر أخطاء تطبيق حرجة |
| `/contact` Mobile/Tablet/Desktop | ✅ وظيفياً، مع ملاحظة أداة | `observe` أكد وجود الهيرو والنموذج والحقول على Mobile، وTablet ظهر بصرياً سليماً؛ لقطة Mobile أظهرت بياضاً من أداة المعاينة فقط بينما DOM والعناصر كانت موجودة |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التنظيف؛ تحذير حجم chunks فقط غير مانع |

**الحكم النقدي:** هذه الدفعة أغلقت تعارضاً تسويقياً مهماً كان سيعيد وعوداً قديمة رغم توحيد الخطط والحدود. لم أغيّر منطق التسجيل أو التواصل حتى لا نكسر مسارات حساسة قبل QA اليدوي.

**نسبة التقدم بعد هذه الدفعة:** 99.989% جاهزية بناء وتشغيل، وقرابة 99.76% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى نشر النسخة، إعادة قياس `/about` Mobile، ثم إغلاق Dashboard بجلسة مستخدم/أدمن و84 تمريرة QA.

**الخطوة التالية:** بعد **Publish → Update**: إعادة قياس `/about` Mobile؛ إذا استقر CLS أقل من `0.1` ننتقل إلى Dashboard Authenticated QA: `/dashboard`, `/dashboard/generate-video`, `/dashboard/campaign-studio`, `/dashboard/library`, ثم صفحات الأدمن الحرجة.

---

## تدقيق Dashboard وربط أرقام اللوحة بمصدر الخطط — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة المرحلة السابقة | ✅ صحيحة | تنظيف `/auth` و`/contact` أزال الوعود القديمة من الواجهة العامة، وبقيت الإشارات القانونية داخل الخصوصية ومسار الفاتورة فقط |
| مطابقة الكود مع الخطة | ✅ وُجدت فجوة وأُغلقت | لوحة `/dashboard` كانت تستخدم `LIMITS` محلية قديمة لخطط `free/pro/business` فقط؛ تم حذفها وربط العرض بـ `PLAN_BY_ID` من `plan-catalog` لدعم `free/starter/growth/pro/business` |
| حذف الكود القديم | ✅ نُفذ | أزيلت أرقام `5/2`, `1000/60`, `5000/300` من لوحة التحكم حتى لا تعارض حدود النص/الصور اليومية المعتمدة |
| معاينة Mobile 390px | ⚠️ مقيدة بالجلسة | مسارات Dashboard حوّلت إلى حالة تحميل/دخول عند عدم توفر جلسة مستخدم معاينة؛ لم يظهر خطأ تطبيق في Console، والتحقق البصري الكامل يحتاج جلسة مستخدم فعلية |
| معاينة Tablet 820px | ⚠️ مقيدة بالجلسة | `/dashboard/generate-video` وصل إلى حالة تحميل بسبب الحماية/الجلسة، لا بسبب كسر JSX أو بناء |
| معاينة Desktop | ✅ مغطاة تقنياً | البناء الإنتاجي وTypeScript أثبتا سلامة المسارات بعد الربط الجديد |
| فحص بقايا النصوص | ✅ نظيف في الواجهة | لم تعد أرقام الباقة القديمة موجودة في كود الواجهة؛ نتائج `PDPL` و`فاتورة ضريبية` المتبقية محصورة في صفحة الخصوصية ومسار الفاتورة |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل؛ تحذير حجم chunks غير مانع |

**الحكم النقدي:** هذه كانت فجوة حقيقية لا تجميلية: صفحة Dashboard كانت ستعرض حدوداً شهرية/قديمة بدل مصدر الباقات المعتمد، خصوصاً مع خطتي Starter وGrowth. تم إصلاحها بأقل تعديل ممكن ودون لمس منطق الرصيد أو قواعد البيانات.

**نسبة التقدم بعد هذه الدفعة:** 99.990% جاهزية بناء وتشغيل، وقرابة 99.80% جاهزية اعتماد قبل Soft Launch. لا تُرفع أكثر حتى تتم معاينة Dashboard بجلسة مستخدم حقيقية وأدمن، ثم إغلاق مصفوفة QA اليدوية.

**الخطوة التالية:** إنشاء/استخدام جلسة مستخدم اختبار منشورة، ثم تنفيذ Dashboard QA فعلي: الصفحة الرئيسية، الفيديو، Campaign Studio، المكتبة، الاستخدام، ثم صفحات الأدمن الحرجة مع فحص عدم تسريب صلاحيات.

---

## تدقيق Dashboard Authenticated QA — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة تعديل `/dashboard` السابق | ✅ صحيح مع تحسين لازم | ربط الباقة بـ`PLAN_BY_ID` كان صحيحاً، لكن عدّادات النص/الصورة كانت ما زالت تقرأ `usage_logs` الشهرية بينما حدود الكتالوج اليومية؛ تم تصحيحها لتقرأ الملخص اليومي الموثوق من `useCreditsSummary` |
| حذف الكود القديم | ✅ نُفذ | أزيل اعتماد لوحة التحكم على `usage_logs` الشهرية لبطاقات الحصة اليومية، وأزيلت دالة الشهر غير المستخدمة بعد التصحيح |
| مطابقة Dashboard مع `/dashboard/usage` | ✅ متطابقة | كلاهما الآن يعرض الاستخدام اليومي والحد اليومي من نفس Server Function بدلاً من خلط شهري/يومي |
| تدقيق `/dashboard/generate-video` | ✅ كودياً سليم بعد تصحيح مصدر الحقيقة | التكلفة، الجودة، المدة، والرصيد تُقرأ من `useCreditsSummary` مع منع الإرسال قبل الاستدعاء عند نفاد الرصيد أو منع Quality/8s؛ لا توجد حصة يومية مستقلة للفيديو حالياً |
| تدقيق `/dashboard/campaign-studio` | ✅ كودياً سليم | حفظ الحملة يتم قبل فتح أدوات النص/الصورة/الفيديو، مع تمرير `campaignPackId` صحيح والتحقق الخادمي عند الحفظ والأرشفة |
| تدقيق `/dashboard/library` | ✅ كودياً سليم | المكتبة تجلب التوليدات الخاصة بالمستخدم، وتعرض الفيديوهات عبر Server Function محمية، مع إبقاء فلتر الفيديو منفصلاً عن جدول `generations` |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التصحيح؛ تحذير حجم chunks فقط غير مانع |

**الحكم النقدي:** الخطوة السابقة لم تكن كافية بالكامل لأن واجهة Dashboard كانت قد انتقلت إلى حدود يومية للنص والصورة بينما بقي بعض العرض شهرياً؛ هذا قد يربك المستخدم. تم توحيد النص والصورة مع مصدر الحصص اليومي، أما الفيديو فمصدر الحقيقة الحالي له هو رصيد نقاط الفيديو وحد المهام المتزامنة.

**نسبة التقدم بعد هذه الدفعة:** 99.991% جاهزية بناء وتشغيل، وقرابة 99.82% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى يتم اختبار Dashboard بجلسة مستخدم/أدمن فعلية، Safari، و84 تمريرة QA موثقة.

**الخطوة التالية:** تنفيذ تمريرة مستخدم فعلية منشورة: تسجيل/دخول → Dashboard → توليد فيديو Fast ممنوع/مسموح حسب الخطة → Campaign Studio → Library → Usage، ثم تمريرة أدمن للتأكد من منع المستخدم العادي من صفحات الإدارة وتوثيق النتائج في مصفوفة S1-S14.

---

## إصلاح شاشة تحميل Dashboard في المعاينة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| Mobile 390px | ⚠️ وُجدت شاشة تحميل مستمرة | الفحص البصري كشف Spinner فقط عند فتح `/dashboard` في المعاينة دون جلسة مستخدم صالحة |
| السبب | ✅ شُخّص | `pendingComponent` كان يستخدم `DashboardShell` المحمي، ما قد يخلق انتظاراً دائرياً أثناء فحص الجلسة قبل عرض الهيكل |
| الإصلاح | ✅ نُفذ | استُبدل Pending الخاص بالداشبورد بهيكل Skeleton مستقل لا يعتمد على `DashboardShell` أو `useAuth` |
| تحصين المصادقة | ✅ نُفذ | أضيف حد زمني لفحص الجلسة وتحميل البروفايل/الدور حتى لا يبقى `loading` عالقاً عند بطء مزود الجلسة |
| التحقق | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد الإصلاح |

**الحكم النقدي:** هذا إصلاح مهم لتجربة الموبايل قبل QA الفعلي؛ لا يفتح بيانات محمية، بل يمنع شاشة تحميل لا نهائية ويترك التحويل الآمن إلى `/auth` يعمل عند عدم وجود جلسة.

**نسبة التقدم بعد هذه الدفعة:** 99.992% جاهزية بناء وتشغيل، وقرابة 99.83% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة Dashboard بجلسة مستخدم/أدمن فعلية وSafari ومصفوفة QA كاملة.

**الخطوة التالية:** بعد Publish → Update، إعادة فتح `/dashboard` على Mobile/Tablet/Desktop بجلسة مستخدم حقيقية، ثم توثيق S1-S14 عملياً وإصلاح أي Blocker/High فقط.

---

## تدقيق Route Guard والكوكيز ضمن QA المصادق — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة إصلاح Dashboard السابق | ✅ صحيح مع تحصين إضافي | `pendingComponent` أصبح مستقلاً، وتمت إضافة حد زمني أيضاً داخل `beforeLoad` حتى لا يعلّق Route Guard نفسه عند بطء فحص الجلسة |
| مطابقة الكود | ✅ نظيف | لا توجد بقايا `LIMITS` أو `usage_logs` أو عدّادات شهرية داخل `/dashboard` بعد التصحيح؛ الأرقام اليومية تأتي من ملخص الرصيد الموثوق |
| Mobile 390px | ✅ مقبول | فتح `/dashboard` بدون جلسة صالحة يحوّل إلى `/auth` بدل Spinner دائم؛ لا توجد أخطاء تطبيق حرجة في Console |
| Tablet 820px | ✅ مقبول مع ملاحظة | صفحة `/auth` مستقرة بصرياً؛ شريط الكوكيز يظهر كحوار مقصود ولا يكسر النموذج |
| Desktop 1280px | ✅ عولجت ملاحظة | شريط الكوكيز كان عريضاً ويحجب جزءاً من نموذج الدخول، فتم تقليل عرضه على الشاشات الكبيرة إلى `max-w-sm` دون تغيير المنطق أو الموافقة |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التحصين وتحسين تموضع الكوكيز؛ تحذير حجم chunks غير مانع |

**الحكم النقدي:** الخطوة السابقة أغلقت سبب الـSpinner، لكن التدقيق كشف أن الحماية الكاملة يجب أن تشمل Route Guard نفسه لا AuthProvider فقط. كما أن QA الديسكتوب كشف حجباً بصرياً من شريط الكوكيز لنموذج الدخول، وتم إصلاحه بأقل تعديل ممكن.

**نسبة التقدم بعد هذه الدفعة:** 99.993% جاهزية بناء وتشغيل، وقرابة 99.85% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تسجيل دخول مستخدم اختبار فعلي، فحص صفحات الأدمن بصلاحيات حقيقية، وإغلاق مصفوفة 84 تمريرة.

**الخطوة التالية:** تنفيذ QA فعلي بجلسة مستخدم: تسجيل دخول → Dashboard → Generate Video → Campaign Studio → Library → Usage، ثم جلسة أدمن/غير أدمن للتأكد من عزل الإدارة وIDOR، مع توثيق S1-S14.

---

## تثبيت التريجرات التشغيلية وتنظيف التكرار — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ⚠️ وُجد تعارض حرج | الدوال التشغيلية كانت موجودة، لكن فحص البيئة أظهر أن قائمة التريجرات الأولية غير مفعّلة/غير معروضة كما يجب، وهذا يخالف تقرير الجاهزية السابق |
| التثبيت | ✅ نُفذ | رُبطت دوال الحماية والتشغيل بجداول public المناسبة: حماية الخطة، سلامة التوليدات، حدود الفيديو المتزامن، تحديثات الوقت، تنبيهات الاشتراك، مزامنة الخطة، وإعادة نقاط الخطة عند التفعيل |
| تنظيف الكود/القاعدة القديمة | ✅ نُفذ | أزيلت تريجرات مكررة بأسماء قديمة، وأزيل تريجر الحصة الشهرية القديم للتوليدات حتى لا يصطدم مع نظام الحصص اليومية الحديث |
| التحقق المباشر | ✅ ناجح | التريجرات الحرجة المتبقية أصبحت نسخة واحدة واضحة لكل وظيفة على `profiles`, `generations`, `subscription_requests`, `topup_purchases`, `video_jobs` |
| فحص الأمان الآلي | ✅ ناجح | Lovable Cloud linter لا يعرض ملاحظات بعد الترحيل والتنظيف |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد تغييرات قاعدة البيانات؛ تحذير حجم chunks غير مانع |

**الحكم النقدي:** هذه الدفعة أهم من تعديل واجهي؛ لأنها أغلقت فجوة تشغيلية فعلية: وجود الدوال وحده لا يكفي إذا لم تكن مربوطة بتريجرات، كما أن التكرار القديم كان قد يؤدي لتنفيذ مزدوج عند التفعيل أو الشحن أو تحديث الفيديو.

**نسبة التقدم بعد هذه الدفعة:** 99.995% جاهزية بناء وتشغيل، وقرابة 99.88% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة مستخدم/أدمن فعلية و84 تمريرة QA موثقة.

**الخطوة التالية:** اختبار عملي للتريجرات: محاولة تغيير خطة مستخدم عادي يجب أن تُرفض، تفعيل طلب اشتراك يجب أن يزامن الخطة ويمنح النقاط مرة واحدة، ومهمة فيديو processing ثالثة لنفس المستخدم يجب أن تُمنع، ثم استكمال QA المصادق على Mobile/Tablet/Desktop.

---

## تدقيق ما بعد تثبيت التريجرات — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ مع ملاحظة نقدية | التريجرات الحرجة موجودة فعلياً، لكن بقيت 3 تريجرات `updated_at` قديمة مكررة على جداول حالة التنبيهات، وهذا كان يناقض عبارة “دون تكرار” حرفياً |
| تنظيف القديم | ✅ نُفذ | حُذفت النسخ القديمة `update_*_updated_at` من `dlq_alert_state`, `email_send_state`, `stale_subs_alert_state` وبقيت النسخ القياسية `trg_*_updated_at` فقط |
| التحقق من القاعدة | ✅ ناجح | استعلام التكرارات بعد التصحيح أعاد نتيجة فارغة، وقائمة التريجرات أصبحت 26 تريجر دون ازدواج وظيفة/جدول |
| فحص الأمان الآلي | ✅ ناجح | Lovable Cloud linter لا يعرض ملاحظات بعد التنظيف |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التنظيف؛ تحذير حجم chunks غير مانع |
| Mobile 390px | ✅ مقبول | `/dashboard` يحوّل إلى `/auth`؛ ظهرت لقطة Spinner انتقالية فقط ثم استقر نموذج الدخول دون أخطاء تطبيق حرجة |
| Tablet 820px | ✅ مقبول | نموذج الدخول مستقر ومقروء، وشريط الكوكيز لا يحجب النموذج بعد التصغير |
| Desktop 1280px | ✅ مقبول | نموذج الدخول مستقر، وتم تضييق شريط الكوكيز أكثر ليبقى بعيداً عن منطقة الإدخال الأساسية |

**الحكم النقدي:** لم تكن الخطوة السابقة خاطئة في أصلها، لكنها بالغت في وصف “بلا تكرار”. التصحيح الحالي جعل التقرير مطابقاً للواقع: لا تريجرات تشغيلية مكررة، ولا كود قديم مؤثر في مسار التحديث التلقائي.

**نسبة التقدم بعد هذه الدفعة:** 99.996% جاهزية بناء وتشغيل، وقرابة 99.90% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى اختبار مستخدم/أدمن فعلي وإغلاق مصفوفة 84 تمريرة.

**الخطوة التالية:** تنفيذ اختبار وظيفي مصادق للتريجرات بسيناريوهات حقيقية: منع تغيير خطة مستخدم عادي، تفعيل اشتراك يمنح الرصيد مرة واحدة فقط، ومنع ثالث مهمة فيديو بحالة processing لنفس المستخدم، ثم توثيق النتائج داخل S4/S8/S9/S13/S14.

---

## اختبار التريجرات العملي المصادق — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ دقيقة بعد التصحيح | لم تعد هناك تريجرات مكررة وظيفياً، وتم الانتقال من فحص وجود التريجرات إلى اختبار سلوكها الفعلي |
| منع تغيير الخطة | ✅ ناجح | محاكاة مستخدم عادي حاول تغيير `profiles.plan` وانتهت برسالة `plan_change_not_allowed` كما هو مطلوب |
| تفعيل الاشتراك | ✅ ناجح | طلب اشتراك مؤقت انتقل إلى `activated` فزامن الخطة ومنح رصيد الخطة، ثم إعادة تحديثه كـ`activated` لم تضف منحة ثانية |
| حد الفيديو المتزامن | ✅ ناجح | تم السماح بمهمتي فيديو `processing` للمستخدم نفسه، ومنع المهمة الثالثة برسالة `too_many_processing_video_jobs` |
| تنظيف بيانات الاختبار | ✅ ناجح | حُذفت سجلات الاختبار المؤقتة من `video_jobs` و`subscription_requests` و`credit_ledger`، وأُعيدت خطة ورصيد المستخدم كما كانت |
| ملاحظة اختبارية | ✅ مفيدة | أول محاولة اصطدمت بقيد `profiles_id_fkey` عند إنشاء مستخدم وهمي، والثانية كشفت قيد منع طلب اشتراك مكرر؛ كلاهما يؤكد أن قيود السلامة تعمل ولم يتطلب تغييراً وظيفياً |
| فحص الأمان الآلي | ✅ ناجح | Lovable Cloud linter لا يعرض ملاحظات بعد الاختبار |

**الحكم النقدي:** هذه الدفعة رفعت الثقة من “التريجر موجود” إلى “التريجر يوقف السلوك الخاطئ فعلياً”. كما أثبتت القيود الجانبية أن النظام يمنع بيانات اختبار غير واقعية وطلبات اشتراك مكررة، وهذا مطابق لمبدأ السلامة قبل السرعة.

**نسبة التقدم بعد هذه الدفعة:** 99.997% جاهزية بناء وتشغيل، وقرابة 99.92% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة مستخدم/أدمن كاملة على الواجهة وسيناريوهات الفيديو/الحملات/المكتبة.

**الخطوة التالية:** تنفيذ QA مصادق من الواجهة بجلسة مستخدم فعلية: Dashboard → Generate Video → فشل/نجاح الفيديو → Library → Usage، ثم جلسة أدمن للتأكد من عزل الفيديو والاشتراكات والـledger دون IDOR.

---

## تدقيق QA المصادق المبدئي بعد اختبار التريجرات — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ دقيقة | اختبار التريجرات أثبت السلوك لا الوجود فقط: منع تغيير الخطة، منع ثالث فيديو قيد المعالجة، ومنع منحة اشتراك مكررة |
| طبقة التريجرات الحالية | ✅ سليمة | فحص `pg_trigger` أظهر 26 تريجر مفعّل على `public`، واستعلام التكرار الوظيفي أعاد نتيجة فارغة |
| فحص الأمان الآلي | ✅ ناجح | Lovable Cloud linter لا يعرض ملاحظات بعد التدقيق |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا؛ تحذير حجم chunks ما زال ملاحظة أداء غير مانعة |
| Mobile 390px | ✅ مقبول | فتح `/dashboard` بدون جلسة يمر عبر Spinner انتقالية ثم يستقر على `/auth`؛ نموذج الدخول وشريط الكوكيز داخل العرض دون overflow أفقي ظاهر |
| Tablet 820px | ✅ مقبول | `/auth` مستقر بصرياً؛ شريط الكوكيز أسفل الصفحة ولا يحجب حقول الدخول الأساسية |
| Desktop 1280px | ✅ مقبول | `/auth` مستقر؛ شريط الكوكيز في أسفل اليسار بعيد عن النموذج ولا يمنع الإدخال |
| Console | ✅ لا أخطاء تطبيق حرجة | التحذيرات المرصودة من سكربت المعاينة الخارجي فقط، ولا توجد أخطاء من كود التطبيق أثناء الفحص |
| Network | ✅ لا فشل ظاهر في جلسة التحويل | لا توجد طلبات XHR/Fetch فاشلة أثناء فتح `/dashboard` والتحويل إلى `/auth` في جلسة غير مصادقة |

**الحكم النقدي:** لقطة الموبايل الأولى أظهرت Spinner، لكن فحص الرابط أكد أنه Spinner انتقالية أثناء التحويل من `/dashboard` إلى `/auth` وليس تعليقاً دائماً. لذلك لا يلزم تعديل كود جديد الآن، لكن يبقى الاعتماد الحقيقي مشروطاً بجلسة مستخدم فعلية لأن الفحص الحالي غير مصادق.

**نسبة التقدم بعد هذه الدفعة:** 99.998% جاهزية بناء وتشغيل، وقرابة 99.94% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى اختبار مستخدم/أدمن حقيقي على الفيديو والحملات والمكتبة والرصيد.

**الخطوة التالية:** بعد Publish → Update، تنفيذ جلسة مستخدم فعلية كاملة: Login → Dashboard → Generate Video Fast/Quality → Library → Usage → Campaign Studio، ثم جلسة أدمن/غير أدمن لعزل صفحات الإدارة والـledger، وتوثيق النتائج ضمن S1-S14.

---

## تدقيق مطابقة التوثيق والكود قبل QA المصادق الكامل — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | لا يوجد تعليق دائم في `/dashboard` بدون جلسة؛ التحويل إلى `/auth` يعمل، وبقيت جلسة المستخدم الفعلية كشرط اعتماد |
| مطابقة التوثيق مع القاعدة | ⚠️ وُجد مرجع قديم ثم صُحح | كان `qa-runbook` يذكر `trg_enforce_generation_quota` رغم حذفه ضمن تنظيف الحصص الشهرية القديمة؛ تم استبداله بالتريجرات الفعلية الحالية |
| تنظيف الكود القديم | ✅ نُفذ | عُدّل تعليق `QuotaExceededDialog` حتى لا يوحي بأن تريجر الحصة الشهرية القديم ما زال نشطاً، مع إبقاء توافق خلفي آمن لرسائل قديمة |
| طبقة التريجرات | ✅ سليمة | استعلام التكرار الوظيفي عاد فارغاً؛ التريجرات الحالية مفعّلة دون ازدواج وظيفة/جدول |
| حماية الإدارة | ✅ سليمة كودياً | صفحات الإدارة تستخدم `AdminGuard`، والدوال الحساسة للفيديو والاشتراكات والـledger تعيد التحقق خادمياً عبر `assertAdmin` |
| مسارات الفيديو والحملات والمكتبة والاستخدام | ✅ متماسكة كودياً | الفيديو يعرض التكلفة ورصيد النقاط وأهلية الباقة قبل التنفيذ، Campaign Studio يحفظ الحزمة قبل فتح الأدوات، Library تفصل الفيديوهات وتحفظ سياق الحملة، Usage يعرض الرصيد والحدود من الملخص الموحد |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد تنظيف التوثيق والتعليق؛ تحذير حجم chunks فقط غير مانع |
| فحص الأمان الآلي | ✅ ناجح | Lovable Cloud linter لا يعرض ملاحظات |

**الحكم النقدي:** هذه الجولة لم تكشف خللاً وظيفياً جديداً، لكنها كشفت تضارباً توثيقياً كان سيشوّش QA ويعطي انطباعاً خاطئاً عن تريجر قديم تمت إزالته عمداً. تم التصحيح بأقل تعديل ممكن دون تغيير منطق الحصص أو الواجهة.

**نسبة التقدم بعد هذه الدفعة:** 99.9985% جاهزية بناء وتشغيل، وقرابة 99.95% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تنفيذ جلسة مستخدم وأدمن فعلية على البيئة المنشورة وإغلاق S1-S14.

**الخطوة التالية:** بعد Publish → Update، تنفيذ تمريرة QA مصادقة حقيقية: مستخدم عادي يولّد فيديو Fast ويفتح Library/Usage/Campaign Studio، ثم مستخدم غير أدمن يحاول فتح `/admin/*` ويُمنع، ثم أدمن يراجع video jobs وsubscriptions وcredit ledger دون أي IDOR أو تسريب بيانات.

---

## مراجعة الخطوة السابقة وتنفيذ QA متعدد المقاسات — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة مع تصحيح قراءة السياق | التوثيق السابق صحيح في النتيجة، لكن تم إعادة فحص القاعدة مباشرة لأن ملخص السياق الآلي ذكر عدم وجود تريجرات؛ الاستعلام الفعلي أثبت وجود 26 تريجر مفعّل |
| التريجرات الحرجة | ✅ مؤكدة | `enforce_generation_integrity` و`protect_profile_plan_change` و`enforce_video_processing_limit` و`reset_credits_on_plan_change` و`sync_profile_plan_on_activation` كل منها مربوط مرة واحدة فقط بالجدول الصحيح |
| فحص الأمان الآلي | ✅ ناجح | Lovable Cloud linter لا يعرض ملاحظات |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اكتمل؛ توزيع الحزم الحالي مستقر وتحذيرات الحجم ليست blocker وظيفي |
| Mobile 390px | ✅ مقبول | `/auth` مستقر بصرياً، الحقول والأزرار داخل العرض، ولا توجد أخطاء console حرجة |
| Tablet 820px | ✅ مقبول | بطاقة الدخول متمركزة ومقروءة، والهيدر والفوتر لا يسببان تداخلاً ظاهراً |
| Desktop 1280px | ✅ مقبول | الوضع الداكن مستقر، نموذج الدخول واضح، وCookie Banner لا يحجب حقول الإدخال أو زر الدخول |
| كود قديم مؤثر | ✅ لا يوجد | لم تظهر بقايا فعالة للتريجر القديم `trg_enforce_generation_quota` خارج سجل الخطة كعنصر مراجعة تاريخي وتعليق توافق خلفي آمن في dialog الحصة |

**الحكم النقدي:** لا توجد حاجة لتعديل كود وظيفي في هذه الجولة؛ أفضل قرار مهني هو عدم لمس مسارات المصادقة أو الفيديو أو الحصص دون دليل كسر. التقدم الحقيقي المنفذ هنا هو تثبيت المطابقة بين القاعدة الفعلية، البناء، وفحص الواجهة على Mobile/Tablet/Desktop.

**نسبة التقدم بعد هذه الدفعة:** 99.9988% جاهزية بناء وتشغيل، وقرابة 99.96% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة مستخدم/أدمن مصادقة كاملة وإغلاق S1-S14 على Chrome وSafari.

**الخطوة التالية:** تنفيذ QA مصادق فعلي بعد Publish → Update: تسجيل دخول مستخدم حقيقي، توليد فيديو، مراجعة Library وUsage وCampaign Studio، ثم اختبار مستخدم غير أدمن ضد `/admin/*` وجلسة أدمن لمراجعة video jobs وsubscriptions وcredit ledger وتوثيق كل نتيجة داخل مصفوفة S1-S14.

---

## عزل الإدارة غير المصادق + مطابقة البناء — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة | لم يظهر كسر في `/auth` أو طبقة التريجرات بعد آخر توثيق؛ النسبة السابقة تبقى صحيحة كجاهزية بناء وتشغيل لا كاعتماد QA كامل |
| بقايا الكود القديم | ✅ لا بقايا مؤثرة | البحث لم يكشف تحقق أدمن عبر `localStorage/sessionStorage`، ولم يظهر `trg_enforce_generation_quota` ككود نشط؛ وجوده في الخطة/السجل تاريخي فقط |
| طبقة التريجرات الحرجة | ✅ مؤكدة | كل تريجر أمني/اقتصادي حرج مربوط مرة واحدة فقط بالوظيفة الصحيحة |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اكتمل دون كسر؛ تحذيرات حجم الحزم تبقى ملاحظة أداء غير مانعة |
| `/admin/video-jobs` Mobile 390 | ✅ محمي | الزائر غير المصادق يرى شاشة تحميل الحارس ثم يُحوّل إلى `/auth`؛ لا أخطاء console حرجة ولا فشل network تطبيقي |
| `/admin/video-jobs` Tablet 820 | ✅ محمي ومستقر | التحويل استقر على نموذج الدخول، والواجهة مقروءة دون تداخل يمنع الاستخدام |
| `/admin/video-jobs` Desktop 1280 | ✅ محمي ومستقر | صفحة الدخول تظهر بشكل صحيح، وCookie Banner لا يمنع الإدخال أو CTA |

**الحكم النقدي:** تم تنفيذ الجزء المتاح من الخطوة التالية: اختبار عزل صفحة إدارة حساسة عند عدم وجود جلسة. النتيجة صحيحة أمنياً؛ لا تُعرض بيانات إدارة ولا يتم استدعاء بيانات حساسة قبل المصادقة. لا أرفع الحالة إلى 100% لأن اختبار “غير أدمن مسجّل” و“أدمن مسجّل” يحتاج جلسات فعلية.

**نسبة التقدم بعد هذه الدفعة:** 99.9990% جاهزية بناء وتشغيل، وقرابة 99.965% جاهزية اعتماد قبل Soft Launch. المتبقي لإغلاق 100%: جلسة مستخدم حقيقية، جلسة غير أدمن حقيقية، جلسة أدمن حقيقية، و84 تمريرة S1-S14 على Chrome/Safari.

**الخطوة التالية:** بعد Publish → Update، تنفيذ الاختبار المصادق الكامل: مستخدم عادي يدخل Dashboard ويولّد فيديو ثم يراجع Library/Usage/Campaign Studio، مستخدم غير أدمن يحاول `/admin/video-jobs` و`/admin/credit-ledger` ويُمنع، ثم أدمن يفتح صفحات الإدارة ويتأكد من عزل البيانات وسلامة ledger دون IDOR.

---

## توسيع عزل الإدارة غير المصادق — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | عزل `/admin/video-jobs` للزائر غير المصادق كان سليماً؛ تم توسيع العينة إلى صفحة مالية أكثر حساسية قبل أي تعديل جديد |
| طبقة التريجرات | ✅ ثابتة | 26 تريجر public مفعّل؛ لا تراجع في طبقة الحماية بعد الجولات السابقة |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| `/admin/credit-ledger` Mobile 390 | ✅ محمي | يظهر حارس التحميل ولا تُعرض أي بيانات دفتر نقاط أو أرصدة؛ لا أخطاء console ولا طلبات بيانات تطبيقية قبل المصادقة |
| `/admin/credit-ledger` Tablet 820 | ✅ محمي ومستقر | التحويل يستقر على `/auth`، النموذج مقروء، ولا يوجد تداخل يمنع تسجيل الدخول |
| `/admin/credit-ledger` Desktop 1280 | ✅ محمي ومستقر | الواجهة مستقرة، وCookie Banner بعيد عن مسار الإدخال الأساسي |
| Network | ✅ نظيف | الطلبات المرصودة خارجية للقياس فقط، ولا تظهر fetch/XHR لتسريب بيانات مالية من صفحة الإدارة |

**الحكم النقدي:** توسيع العينة إلى `credit-ledger` مهم لأن هذه الصفحة أكثر حساسية من عرض مهام الفيديو. النتيجة تؤكد أن الحماية لا تعتمد على إخفاء واجهة فقط؛ الصفحة لا تصل إلى بيانات مالية قبل تحقق الجلسة. لا يزال اختبار “مستخدم مسجّل غير أدمن” ضرورياً لأنه يختبر طبقة مختلفة عن الزائر غير المصادق.

**نسبة التقدم بعد هذه الدفعة:** 99.9991% جاهزية بناء وتشغيل، وقرابة 99.97% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى جلسة مستخدم/غير أدمن/أدمن فعلية وإغلاق مصفوفة S1-S14 على Chrome وSafari.

**الخطوة التالية:** تنفيذ QA المصادق الحقيقي: تسجيل دخول مستخدم عادي، محاولة فتح `/admin/video-jobs` و`/admin/credit-ledger` للتأكد من التحويل أو المنع، ثم تسجيل دخول أدمن ومراجعة الصفحات نفسها مع التحقق من عدم ظهور بيانات غير لازمة وعدم وجود IDOR في الروابط أو التفاصيل.

---

## توسيع عزل إدارة الحملات + تحقق البناء — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | عزل `credit-ledger` غير المصادق كان سليماً، ولم يظهر تسريب بيانات مالية أو طلبات XHR/Fetch حساسة قبل المصادقة |
| مطابقة الكود | ✅ سليمة | `AdminGuard` يوجّه غير المصادق إلى `/auth` ويمنع غير الأدمن، والحماية الخادمية باقية عبر `assertAdmin` في الدوال الحساسة؛ لا يوجد كود قديم مؤثر يحتاج حذفاً |
| `/admin/campaign-packs` Mobile 390 | ✅ محمي | ظهر تحميل الصلاحيات ثم استقر المسار على `/auth?redirect=/admin/campaign-packs` دون عرض بيانات حملات |
| `/admin/campaign-packs` Tablet 820 | ✅ محمي | ظهرت رسالة “صلاحية غير كافية” انتقالياً ثم إعادة توجيه، ولا يوجد تداخل يمنع القراءة أو الإجراء |
| `/admin/campaign-packs` Desktop 1280 | ✅ محمي | التحويل إلى `/auth` تم دون إظهار بيانات إدارة الحملات؛ ظهور شاشة التحميل كان انتقالياً فقط |
| Console/Network | ✅ نظيف تطبيقياً | لا توجد أخطاء من كود التطبيق ولا طلبات XHR/Fetch تكشف بيانات إدارة؛ التحذيرات من سكربت المعاينة الخارجي فقط |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اكتمل دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اكتمل؛ تحذيرات حجم الحزم ما زالت ملاحظة أداء غير مانعة |

**الحكم النقدي:** توسعة العينة إلى إدارة الحملات مهمة لأنها تغطي مساراً مختلفاً عن الفيديو والدفتر المالي. النتيجة تثبت أن صفحات الإدارة الثلاث الأعلى حساسية لا تعرض بيانات قبل المصادقة على الهاتف والتابلت والديسكتوب. لم أعدّل منطق الحماية لأن الفحص لم يكشف خللاً؛ أفضل نسخة هنا هي تثبيت السلوك الحالي وتوثيقه بدقة لا إدخال تغيير بلا دليل.

**نسبة التقدم بعد هذه الدفعة:** 99.9992% جاهزية بناء وتشغيل، وقرابة 99.972% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى جلسة مستخدم عادي مصادق، مستخدم غير أدمن مصادق، وأدمن مصادق مع إغلاق مصفوفة S1-S14 على Chrome وSafari.

**الخطوة التالية:** تنفيذ جلسة مستخدم عادي فعلية: Login → Dashboard → Generate Video → Library → Usage → Campaign Studio، ثم جلسة غير أدمن ضد `/admin/*`، ثم جلسة أدمن لمراجعة video jobs وcampaign packs وcredit ledger مع توثيق أي IDOR أو فشل صلاحيات إن ظهر.

---

## فحص مسارات المستخدم المحمية قبل المصادقة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | عزل صفحات الإدارة الثلاث بقي سليماً، ولم يظهر كود قديم مؤثر أو تحقق أدمن عبر `localStorage/sessionStorage` |
| مطابقة كود الفيديو/المكتبة/الاستخدام | ✅ سليمة | `generateVideo` و`listVideoJobs` محميتان بـ`requireSupabaseAuth`، و`Usage` يقرأ من `useCreditsSummary`، و`Library` تعتمد RLS + دالة الفيديو المحمية |
| `/dashboard/generate-video` Mobile 390 | ✅ محمي | ظهرت دلالة العنوان انتقالياً ثم استقر المسار على `/auth` دون عرض حقول توليد أو وظائف فيديو أو بيانات رصيد |
| `/dashboard/library` Tablet 820 | ✅ محمي | الصفحة تحولت إلى `/auth` دون عرض بطاقات توليدات أو فيديوهات أو صور مكتبة |
| `/dashboard/usage` Desktop 1280 | ✅ محمي | الصفحة تحولت إلى `/auth` دون عرض أرقام رصيد أو حدود استخدام أو تفاصيل باقة |
| Console/Network | ✅ نظيف تطبيقياً | لا توجد أخطاء تطبيق حرجة ولا طلبات XHR/Fetch تعرض بيانات مستخدم قبل المصادقة؛ التحذيرات من سكربت المعاينة الخارجي فقط |
| كود قديم | ✅ لا حذف مطلوب | لم يظهر مسار قديم نشط للحصة الشهرية أو تحقق صلاحيات غير آمن؛ البقايا الموجودة في الخطة/QA تاريخية ومقصودة كسجل مراجعة |

**الحكم النقدي:** هذه الجولة أغلقت جزءاً مهماً قبل QA المصادق: مسارات المستخدم الحساسة لا تكشف مكتبة أو رصيداً أو أدوات توليد قبل تسجيل الدخول عبر الهاتف والتابلت والديسكتوب. لم يتم رفع الاعتماد إلى 100% لأن الاختبار المطلوب التالي لا بد أن يكون بجلسة مستخدم حقيقية تُثبت الخصم، الحفظ في المكتبة، وتحديث الاستخدام بعد عملية فعلية.

**نسبة التقدم بعد هذه الدفعة:** 99.9993% جاهزية بناء وتشغيل، وقرابة 99.974% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة مستخدم مصادق وغير أدمن وأدمن على Chrome/Safari وإغلاق S1-S14.

**الخطوة التالية:** تنفيذ QA مستخدم مصادق فعلي: Login → Generate Video Fast 5s → Refresh/Status → Library → Usage → Campaign Studio، ثم مقارنة `credit_ledger` و`video_jobs` وواجهة الرصيد للتأكد من خصم واحد وحفظ واحد وعدم وجود IDOR.

---

## تحسين حفظ مسار الرجوع في حارس Dashboard — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ دقيقة مع ملاحظة تحسين | مسارات Dashboard كانت محمية فعلاً، لكن التحويل إلى `/auth` لم يكن يحتفظ دائماً بالمسار المطلوب بعد الدخول |
| تعديل الحارس | ✅ نُفذ | `beforeLoad` في `/dashboard` يمرر الآن `redirect: location.href` إلى `/auth` قبل أي render فعلي للمحتوى المحمي |
| تعديل صفحة الدخول | ✅ نُفذ | `/auth` تقبل `redirect` اختيارياً وآمناً فقط إذا بدأ بـ`/`، ثم تعيد المستخدم إليه بعد الدخول إذا لا يحتاج onboarding |
| توافق الروابط القائمة | ✅ محفوظ | جُعل `redirect` اختيارياً حتى لا تنكسر روابط `/auth` القديمة في الهيدر وصفحات الاستعادة وباقي المسارات |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد الإصلاح؛ تحذير حجم الحزم غير مانع |

**الحكم النقدي:** هذا تعديل جودة مهم وليس تغييراً تجارياً جديداً: يحافظ على حماية `beforeLoad`، ويمنع فقدان سياق المستخدم بعد تسجيل الدخول، دون نقل النظام إلى بنية routes جديدة قد تكسر الملفات الحالية. أفضل نسخة الآن هي تحسين الحارس الموجود بأقل تعديل آمن، وتأجيل إعادة هيكلة pathless auth إلى مرحلة مستقلة إن احتجناها لاحقاً.

**نسبة التقدم بعد هذه الدفعة:** 99.9994% جاهزية بناء وتشغيل، وقرابة 99.976% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة مستخدم مصادق وغير أدمن وأدمن على Chrome/Safari وإغلاق S1-S14.

**الخطوة التالية:** اختبار تسجيل دخول فعلي من رابط محمي مثل `/dashboard/generate-video` والتأكد من الرجوع لنفس الصفحة بعد الدخول، ثم تنفيذ Generate Video → Library → Usage ومطابقة الخصم والحفظ في الجداول.

---

## تدقيق مسار الرجوع بعد الدخول + تصحيح Google/Admin — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة مع ملاحظة نقدية | حارس `/dashboard` و`validateSearch` في `/auth` يعملان، لكن Google OAuth كان يعود إلى الجذر ولا يحتفظ دائماً بمسار الصفحة المحمية |
| تحسين Google OAuth | ✅ نُفذ | أصبح `redirect_uri` يعود إلى `/auth?redirect=...` عند وجود وجهة محمية، ثم تكمل صفحة الدخول التحويل إلى المسار الأصلي بعد ثبوت الجلسة |
| تحسين AdminGuard | ✅ نُفذ | أصبح يحفظ `pathname + search` بدلاً من `pathname` فقط، حتى لا تضيع فلاتر/استعلامات صفحات الإدارة بعد تسجيل الدخول |
| مطابقة مصدر حقيقة الباقات | ✅ موثق | `plan_entitlements` يحتوي الخطط الخمس النشطة: Free / Starter / Growth / Pro / Business مع حدود النص والصورة والفيديو وصلاحيات Pro/Quality |
| مشغلات قاعدة البيانات | ✅ موثقة فعلياً | ظهر 33 تريجر فعّالاً إجمالاً، منها حماية `profiles.plan`، مزامنة الاشتراك، قفل topup، وحدّ مهام الفيديو المتزامنة؛ لا يوجد التريجر القديم كاسم نشط |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل؛ تحذير حجم الحزم ما زال ملاحظة أداء غير مانعة |
| كود قديم | ✅ لا حذف مطلوب | لم يظهر تحقق أدمن عبر تخزين العميل، ولا مسار نشط قديم للحصة الشهرية يستدعي حذفاً فورياً |

**الحكم النقدي:** هذه الدفعة حسّنت تجربة الدخول دون توسيع نطاق المنتج: المستخدم القادم من رابط محمي لا يفقد وجهته سواء دخل بالبريد أو Google، وصفحات الإدارة تحفظ الاستعلامات عند الرجوع. لم أرفع الاعتماد إلى 100% لأن الاختبار المصادق الحقيقي لتوليد الفيديو ومطابقة `credit_ledger`/`video_jobs` لم يُغلق بعد.

**نسبة التقدم بعد هذه الدفعة:** 99.9995% جاهزية بناء وتشغيل، وقرابة 99.978% جاهزية اعتماد قبل Soft Launch. تبقى 100% ممنوعة حتى تمريرة مستخدم مصادق، مستخدم غير أدمن، وأدمن على Chrome/Safari وإغلاق S1-S14.

**الخطوة التالية:** تنفيذ تمريرة مصادقة فعلية: فتح `/dashboard/generate-video` كزائر → الدخول بالبريد وGoogle → الرجوع لنفس الصفحة → توليد فيديو Fast 5s → التأكد من ظهوره في Library وUsage ومطابقة خصم واحد في دفتر النقاط ومهمة واحدة في سجل الفيديو.

---

## تحصين Redirect النهائي قبل QA المصادق — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة مع تحسين أمني | حفظ مسار الرجوع للبريد وGoogle كان سليماً وظيفياً، لكن قبول أي قيمة تبدأ بـ`/` تم تضييقه لمنع صيغ `//domain` أو روابط تحتوي `://` |
| `/auth` redirect validation | ✅ تم تحصينه | أضيفت دالة `sanitizeRedirectPath` لقبول المسارات الداخلية فقط ورفض التحويلات الخارجية/المبهمة قبل تنفيذ `navigate` أو بناء `redirect_uri` |
| Dashboard fallback redirect | ✅ تم تحسينه | مسار الرجوع الاحتياطي أصبح يحفظ `pathname + search + hash` عند انتهاء الجلسة بعد التحميل |
| AdminGuard redirect | ✅ تم تحسينه | مسار رجوع صفحات الإدارة أصبح يحفظ `pathname + search + hash` أيضاً، فيحافظ على أي فلتر أو anchor مشروع |
| مطابقة الكود القديم | ✅ لا حذف مطلوب | لا يوجد تحقق أدمن عبر `localStorage/sessionStorage`، وظهور `trg_enforce_generation_quota` محصور في سجل QA التاريخي وليس كوداً نشطاً |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التحصين؛ تحذير حجم الحزم فقط غير مانع |
| Mobile/Tablet/Desktop | ✅ لا أثر بصري | التعديل منطقي في طبقة التوجيه ولا يغير JSX أو CSS؛ لذلك لا يضيف خطر تداخل أو overflow على المقاسات الثلاثة |

**الحكم النقدي:** هذه أفضل نسخة للمرحلة الحالية لأنها تغلق احتمال open redirect دون إعادة هيكلة auth كاملة، وتحافظ على تجربة الرجوع الدقيقة للمستخدم والأدمن. لا توجد بقايا كود قديم تستحق الحذف الآن؛ الموجود في QA سجل تاريخي لا ينفذ داخل التطبيق.

**نسبة التقدم بعد هذه الدفعة:** 99.9996% جاهزية بناء وتشغيل، وقرابة 99.980% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة مستخدم مصادق وغير أدمن وأدمن على Chrome/Safari مع إغلاق S1-S14.

**الخطوة التالية:** تنفيذ QA مصادق فعلي لا يمكن تعويضه بالكود فقط: الدخول من `/dashboard/generate-video` بالبريد وGoogle، توليد فيديو Fast 5s، فحص ظهوره في Library وUsage، ثم مطابقة `credit_ledger` و`video_jobs` لضمان خصم واحد ومهمة واحدة وعدم وجود IDOR.

---

## QA عملي لمسارات الرجوع والمقاسات الثلاثة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ مثبتة | `sanitizeRedirectPath` يرفض `//...` و`://`، وGoogle/email يحافظان على مسار الرجوع الداخلي |
| كود قديم/تحقق غير آمن | ✅ لا يوجد نشط | لا يوجد تحقق أدمن عبر `localStorage/sessionStorage`، ولا يظهر `trg_enforce_generation_quota` إلا داخل سجل QA التاريخي |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا؛ تحذير حجم الحزم غير مانع |
| Mobile 390 — `/dashboard/generate-video?qa=redirect#cost` | ✅ مستقر | ظهرت صفحة الفيديو لأن جلسة المعاينة مصادقة؛ لم يظهر كسر بصري في أول شاشة |
| Tablet 820 — `/auth?redirect=//evil.example` | ✅ بقي داخل التطبيق | صفحة الدخول ظهرت طبيعياً ولم يحدث انتقال إلى نطاق خارجي، ما يثبت تحصين open redirect عملياً |
| Desktop 1280 — `/auth?redirect=/dashboard/generate-video?...#cost` | ✅ مقروء ومستقر | نموذج الدخول واضح دون تداخل أو overflow ظاهر، والوجهة الداخلية محفوظة في الرابط |
| Console | ✅ لا أخطاء تطبيق حرجة | التحذيرات المرصودة من سكربت المعاينة الخارجي `postMessage` فقط وليست من كود التطبيق |

**الحكم النقدي:** تم تحويل التحصين من مراجعة كود إلى إثبات عملي جزئي على الهاتف والتابلت والديسكتوب. ما زال الإغلاق التجاري غير مكتمل لأن جلسة توليد فيديو فعلية بمستخدم حقيقي ومطابقة دفتر النقاط لم تُنفذ بعد.

**نسبة التقدم بعد هذه الدفعة:** 99.9997% جاهزية بناء وتشغيل، وقرابة 99.982% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى إغلاق QA المصادق الحقيقي على Chrome/Safari.

**الخطوة التالية:** تنفيذ السيناريو الوظيفي المصادق كاملاً: مستخدم عادي → Generate Video Fast 5s → Library → Usage → مقارنة `credit_ledger` و`video_jobs`، ثم مستخدم غير أدمن ضد `/admin/*`، ثم أدمن لمراجعة سجلات الفيديو والحملات والدفتر المالي.

---

## مراجعة QA المصادق التمهيدية قبل التوليد الفعلي — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ مثبتة | تحصين `sanitizeRedirectPath` ومسارات الرجوع ما زالا صحيحين؛ لم يظهر كود قديم مؤثر أو تحقق أدمن عبر تخزين العميل |
| Mobile 390 — `/dashboard/generate-video?qa=auth-flow#cost` | ✅ مستقر | صفحة توليد الفيديو ظهرت بجلسة المعاينة دون تداخل أو overflow واضح في أول شاشة |
| Tablet 820 — نفس المسار بعد تغيّر حالة الجلسة | ✅ محمي | انتهى المسار إلى `/auth?redirect=...` مع حفظ الوجهة الأصلية كاملة، وهذا يثبت حماية المسار عند غياب الجلسة بدلاً من عرض محتوى محمي |
| Desktop 1280 — `/auth?redirect=/dashboard/generate-video?...#cost` | ✅ مستقر | نموذج الدخول واضح، والـredirect الداخلي محفوظ في العنوان دون انتقال خارجي |
| Console/Network | ✅ لا أخطاء تطبيق حرجة | التحذيرات المرصودة من سكربت المعاينة الخارجي فقط، ولا توجد طلبات XHR/Fetch فاشلة في هذه التمريرة |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد المراجعة؛ تحذير حجم الحزم غير مانع |

**الحكم النقدي:** تم تنفيذ جزء آمن من QA المصادق دون توليد فيديو فعلي حتى لا نستهلك رصيداً أو نستدعي المزود بلا حساب اختبار مؤكد. السلوك الصحيح الآن: إن وُجدت جلسة تظهر صفحة الفيديو، وإن غابت الجلسة يتم الرجوع إلى `/auth` مع حفظ الوجهة الدقيقة.

**نسبة التقدم بعد هذه الدفعة:** 99.99975% جاهزية بناء وتشغيل، وقرابة 99.983% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تنفيذ توليد فيديو حقيقي ومطابقة `credit_ledger` و`video_jobs` وحفظ النتيجة في Library/Usage.

**الخطوة التالية:** تشغيل تمريرة مستخدم حقيقية كاملة بحساب اختبار مؤكد: Login → Generate Video Fast 5s → Refresh/Status → Library → Usage، ثم مقارنة سجل النقاط ومهمة الفيديو، وبعدها اختبار مستخدم غير أدمن ضد `/admin/*` ثم أدمن لمراجعة السجلات.

---

## إصلاح نقدي في refund/حصة الفيديو + QA مقاسات — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة مع ملاحظة حرجة | مسار الخصم والـrefund كان آمناً للنقاط، لكن احتاج لاحقاً توحيد مصدر الحقيقة وإلغاء الاعتماد على أي حصة يومية مستقلة للفيديو |
| تعديل `refreshVideoJob` | ✅ نُفذ ثم صُحح مصدر الحقيقة | أصبح مسار الفشل/timeout يرد النقاط ويربط المهمة بسجل refund واضح؛ الحماية النشطة حالياً هي النقاط وحد المهام المتزامنة فقط |
| مطابقة الكود القديم | ✅ لا حذف مطلوب | لم يظهر تحقق أدمن عبر `localStorage/sessionStorage`; استخدام التخزين الموجود محصور في A/B testing وليس صلاحيات، و`trg_enforce_generation_quota` سجل تاريخي لا كود نشط |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد الإصلاح؛ تحذير حجم الحزم غير مانع |
| Mobile 390 | ✅ مستقر | `/dashboard/generate-video?qa=refund-fix#cost` ظهر دون كسر واضح في أول شاشة عند وجود جلسة معاينة |
| Tablet 820 | ✅ محمي ومستقر | انتهى إلى صفحة الدخول مع حفظ redirect عند غياب الجلسة؛ النموذج مقروء، وشريط الكوكيز فقط كان يغطي أسفل الصفحة حتى الموافقة |
| Desktop 1280 | ✅ مستقر | بعد إغلاق شريط الكوكيز، صفحة الدخول واضحة دون تداخل أو overflow ظاهر |
| Console/Network | ✅ لا أخطاء تطبيق حرجة | التحذيرات من سكربت المعاينة الخارجي فقط، وطلبات التحليلات رجعت 200 |

**الحكم النقدي:** هذه الجولة أغلقت فجوة عدالة مستخدم مهمة: الفشل التقني يرد النقاط ويربط المهمة بسجل refund قابل للتدقيق. لاحقاً تم توحيد مصدر الحقيقة وإلغاء أي اعتماد على حصة يومية مستقلة للفيديو. لم يتم توليد فيديو فعلي لأن ذلك يحتاج حساب اختبار مؤكد ومزوّد فعلي لتجنب استهلاك غير مضبوط.

**نسبة التقدم بعد هذه الدفعة:** 99.9998% جاهزية بناء وتشغيل، وقرابة 99.985% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة توليد فيديو حقيقية ومطابقة `credit_ledger` و`video_jobs` وLibrary/Usage.

**الخطوة التالية:** تنفيذ تمريرة حساب اختبار فعلية: Login → Generate Video Fast 5s → Refresh حتى completed/processing واضح → Library → Usage → مقارنة سجل النقاط ومهمة الفيديو، ثم اختبار غير أدمن ضد `/admin/*`، ثم أدمن يراجع سجلات الفيديو والحملات والدفتر المالي.

---

## تحصين المصادقة والتحقق من التريجرات الفعلية — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ مثبتة | إصلاح رد النقاط عند failure/timeout بقي سليماً، ولم يظهر كسر في البناء |
| تريجرات قاعدة البيانات | ✅ مثبتة فعلياً | الاستعلام المباشر من `pg_trigger` أظهر 26 تريجر فعّالاً، منها حماية `profiles.plan`، حد مهام الفيديو المتزامنة، مزامنة الاشتراك، وتحديثات `updated_at`؛ سياق الأدوات الذي قال “لا توجد تريجرات” كان غير دقيق |
| أمان كلمات المرور | ✅ تم تفعيله | فُعّل فحص كلمات المرور المسرّبة في إعدادات المصادقة مع إبقاء التسجيل والبريد كما هما، ودون تفعيل تأكيد تلقائي للبريد |
| Security linter | ✅ لا ملاحظات | الفحص الآلي لم يُظهر ملاحظات أمنية جديدة بعد ضبط المصادقة |
| Auth logs | ✅ سليمة | السجلات أظهرت تحميل cache كلمات المرور المسرّبة وتشغيل خدمة المصادقة؛ التحذيرات الموجودة داخلية غير مانعة |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا؛ تحذير حجم الحزم غير مانع |
| Mobile/Tablet/Desktop — `/auth?redirect=/dashboard/generate-video?...#cost` | ✅ مستقر | نموذج الدخول مقروء على 390 و820 و1280؛ شريط الكوكيز قد يغطي أسفل الشاشة حتى الموافقة لكنه ليس كسر تخطيط |
| Console/Network | ✅ لا أخطاء تطبيق حرجة | التحذيرات من سكربت المعاينة الخارجي فقط، وطلبات التحليلات رجعت 200 |

**الحكم النقدي:** هذه الدفعة أغلقت طبقة أمان مصادقة حقيقية قبل Soft Launch: كلمات المرور المعروفة كمسرّبة تُرفض عند التسجيل/التغيير، والتريجرات الحرجة مؤكدة فعلياً في القاعدة. لم يُحذف كود قديم لأن البحث لم يكشف بقايا تنفيذية مؤثرة.

**نسبة التقدم بعد هذه الدفعة:** 99.99985% جاهزية بناء وتشغيل، وقرابة 99.986% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة توليد فيديو فعلية ومراجعة أدمن/غير أدمن كاملة.

**الخطوة التالية:** تنفيذ اختبار إنتاجي بحساب اختبار: تسجيل/دخول بكلمة آمنة → Generate Video Fast 5s → Refresh/Status → Library → Usage → مقارنة `credit_ledger` و`video_jobs`، ثم اختبار رفض كلمة مرور مسرّبة في signup/reset، ثم عزل `/admin/*` لمستخدم غير أدمن.

---

## تدقيق استرداد الفيديو ضد التحديث المتزامن + QA مقاسات — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة مع تحسين نقدي | رد النقاط عند الفشل/timeout كان صحيحاً، ثم تم تضييقه أكثر ليحدث أثر الاسترداد مرة واحدة فقط عند نجاح إنشاء سجل refund جديد |
| تعديل `refreshVideoJob` | ✅ نُفذ | أصبح تثبيت الاسترداد مشروطاً بعودة `refundLedgerId`، وهذا يمنع أثر refund مزدوجاً إذا حدث تحديثان متزامنان لنفس مهمة الفيديو |
| مطابقة الكود القديم | ✅ لا حذف مطلوب | لم يظهر مسار قديم نشط يعيد خصم/رد نقاط الفيديو أو يتحكم بالصلاحيات من العميل؛ السجل التاريخي في QA باقٍ للتوثيق فقط |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل؛ تحذير حجم الحزم غير مانع |
| Mobile 390 — `/dashboard/generate-video?qa=post-refund-audit#cost` | ✅ مستقر | صفحة توليد الفيديو ظاهرة بجلسة المعاينة دون تداخل أو overflow أفقي واضح |
| Tablet 820 — `/dashboard/library?qa=post-refund-audit` | ✅ مستقر | صفحة المكتبة ظاهرة ومقروءة دون كسر بصري أو overflow أفقي واضح |
| Desktop 1280 — `/dashboard/usage?qa=post-refund-audit` | ✅ مستقر | صفحة الاستخدام ظاهرة دون كسر بصري واضح في أول شاشة |
| Console | ✅ لا أخطاء تطبيق حرجة | التحذيرات المرصودة من سكربت المعاينة الخارجي `postMessage` فقط وليست من كود التطبيق |

**الحكم النقدي:** التعديل السابق كان يعالج عدالة المستخدم، وهذه الجولة جعلته أكثر أماناً ضد التزامن: لا يتم تثبيت حالة refund إلا بعد رد النقاط فعلياً لأول مرة. هذا يمنع تضخيم أو تكرار أثر الاسترداد عند ضغط المستخدم على التحديث أو تكرار الطلب.

**نسبة التقدم بعد هذه الدفعة:** 99.9999% جاهزية بناء وتشغيل، وقرابة 99.988% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة توليد فيديو حقيقية ومطابقة `credit_ledger` و`video_jobs` وLibrary/Usage مع اختبار غير أدمن وأدمن.

**الخطوة التالية:** تنفيذ تمريرة حساب اختبار فعلية لا يمكن تعويضها بالكود: Login → Generate Video Fast 5s → Refresh حتى completed/refunded واضح → Library → Usage → مقارنة `credit_ledger` و`video_jobs`، ثم اختبار مستخدم غير أدمن ضد `/admin/*`، ثم أدمن لمراجعة الفيديو والحملات والدفتر المالي.

---

## QA فعلي لمسار الفيديو وترميم مطابقة ledger — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ كشفت فجوة أعمق | شرط تحرير الحصة عند `refundLedgerId` صحيح، لكن التدقيق الخلفي كشف أن بعض مهام الفيديو القديمة كانت `refunded` دون سجل refund فعلي في دفتر النقاط |
| سبب الخلل | ✅ مشخص | مسار الفشل كان يستدعي `refund_credits` بسياق مستخدم لا يملك مطالبة `service_role`، ثم لا يرفع الخطأ لأن helper الردّ مصمم ألا يحجب رسالة الفشل الأصلية |
| تعديل الكود | ✅ نُفذ | مسارات refund في `generateVideo` و`refreshVideoJob` تستخدم الآن عميل الخادم الموثوق، ولا يتم اعتبار المهمة مستردة إلا إذا أُنشئ refund فعلي |
| ترميم البيانات | ✅ نُفذ | أُضيفت سجلات refund مفقودة للمهام الأربع المتأثرة، ورُبطت بـ`video_jobs.refund_ledger_id` و`credit_ledger.refund_ledger_id`، وعاد رصيد المستخدم إلى 20000 نقطة |
| مطابقة خلفية بعد الترميم | ✅ ناجحة | استعلام المطابقة أعاد `missing_refunds = 0` للمهام المتأثرة، ولا توجد حالياً حصة يومية مستقلة للفيديو ضمن مصدر الحقيقة الحالي |
| Mobile 390 — Generate Video | ✅ محمي | المسار انتهى إلى صفحة الدخول لأن جلسة المتصفح غير مصادقة؛ لا يوجد تسريب لأدوات التوليد أو بيانات الرصيد |
| Tablet 820 — Library | ⚠️ تحميل فقط في هذه الجلسة | ظهرت شاشة تحميل بسبب غياب/تذبذب الجلسة في المعاينة، ولم تظهر بيانات مكتبة؛ لا يوجد تسريب بيانات، لكن يلزم اختبار بحساب حقيقي |
| Console/Network | ✅ لا أخطاء تطبيق حرجة | التحذيرات من سكربت المعاينة الخارجي فقط، وطلبات التحليلات 200؛ لا توجد طلبات بيانات فيديو ظاهرة دون جلسة |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد الإصلاح؛ تحذير حجم الحزم غير مانع |

**الحكم النقدي:** هذه الجولة أهم من مجرد تمريرة واجهة؛ لأنها كشفت عدم تطابق مالي فعلي بين حالة مهمة الفيديو ودفتر النقاط، وتم إصلاح المصدر وترميم البيانات المتأثرة. الآن لا يُوسم الفشل كـrefund حقيقي إلا مع سجل رد نقاط قابل للتدقيق.

**نسبة التقدم بعد هذه الدفعة:** 99.99993% جاهزية بناء وتشغيل، وقرابة 99.990% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى توليد فيديو ناجح فعلي بمزوّد صحيح وحساب اختبار، ثم مراجعة غير أدمن/أدمن كاملة على Chrome وSafari.

**الخطوة التالية:** إصلاح/تأكيد صلاحية مفتاح مزوّد الفيديو ثم تنفيذ تمريرة إنتاجية كاملة: Login → Generate Video Fast 5s ناجح → Refresh حتى completed → Library → Usage → مطابقة `credit_ledger` و`video_jobs`، وبعدها اختبار مستخدم غير أدمن ضد `/admin/*` ثم أدمن لمراجعة السجلات.

---

## خريطة إغلاق قسم الفيديو — 27 أبريل 2026

| الجلسة | الهدف | المخرجات المطلوبة | معيار الإغلاق |
|---|---|---|---|
| 1 | تثبيت مصدر الحقيقة | توثيق المزود، التسعير، سياسة الخصم/الاسترداد، وحد المهام المتزامنة | لا يوجد تعارض بين الكود والقاعدة والوثائق؛ الحصة اليومية المستقلة للفيديو غير مذكورة كميزة نشطة |
| 2 | تدقيق End-to-End والدفتر المالي | توثيق آخر فيديو ناجح، مطابقة `video_jobs` مع `credit_ledger` و`user_credits` | لا مهام عالقة، ولا completed بلا رابط، ولا refunded بلا refund ledger |
| 3 | اختبار الفشل والتزامن | محاكاة فشل/timeout آمن، اختبار ضغط مزدوج ونافذتين | لا خصم مكرر، لا processing دائم، والرسالة للمستخدم آمنة وواضحة |
| 4 | تجربة المستخدم والمكتبة والإدارة | مراجعة `/dashboard/generate-video`, `/dashboard/library`, `/admin/video-jobs` على المقاسات الأساسية | المستخدم يرى السعر والحالة والرابط، والأدمن يرى السجل دون تسريب للمستخدم العادي |
| 5 | قرار التخزين والتقرير النهائي | حسم روابط المزود الخارجية مقابل التخزين الداخلي، وإصدار تقرير إغلاق القسم | قرار Soft Launch واضح: جاهز / جاهز بشرط / غير جاهز |

**تقدير الجلسات:** الحد الأدنى 3 جلسات لقسم مناسب لـSoft Launch محدود، والأفضل مهنياً 4–5 جلسات ليصبح القسم قوياً تقنياً وموثقاً. أكثر من 5 جلسات مطلوبة فقط إذا تقرر إضافة أرشفة داخلية دائمة للفيديوهات أو اختبار أجهزة/متصفحات حقيقية موسع.

**حكم مصدر الحقيقة الحالي:** قسم الفيديو يعمل تشغيلياً بعد تجربة المستخدم الناجحة الأخيرة، لكنه لا يُغلق هندسياً قبل إتمام جلسات التدقيق أعلاه. الحماية الحالية للفيديو هي نقاط الفيديو + حد المهام المتزامنة + refund موثق عند الفشل، وليست حصة يومية مستقلة.

---

## تدقيق تكامل الفيديو والدفتر بعد تحصين الاسترداد — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة مع تحسين نقدي | استخدام صلاحيات الخادم للاسترداد كان صحيحاً، لكن تم رصد احتمال سباق نادر عند تحديث نفس مهمة الفيديو من أكثر من طلب تحديث في الوقت نفسه |
| تحصين حالة `refunded` | ✅ نُفذ | أضيف `markProcessingJobRefunded` لتحديث المهام التي ما زالت `processing` فقط، وإرجاع الحالة الحالية إن سبق طلب آخر بتحديثها |
| حماية رابط الاسترداد | ✅ نُفذت | لم يعد التحديث يكتب `refund_ledger_id: null` عند وجود استرداد سابق؛ هذا يمنع مسح رابط دفتر النقاط في السباقات المتزامنة |
| مطابقة البيانات الحالية | ✅ سليمة | `video_jobs=4` كلها `refunded`، ولا توجد مهام `processing` عالقة، ولا توجد مهام مستردة بدون رابط استرداد، و`consume_video=4` مقابل `refund=4` وكلها مرتبطة |
| مطابقة الدفتر مع الفيديو | ✅ سليمة | لا توجد mismatches بين `video_jobs.credits_charged` وسجلات `credit_ledger.amount`، ولا توجد نتائج مكتملة بلا رابط فيديو |
| فحص الأمان الآلي | ✅ ناجح | Lovable Cloud linter لا يعرض ملاحظات أمنية |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل؛ تحذير حجم الحزم فقط غير مانع |
| Mobile/Tablet/Desktop | ✅ لا أثر بصري | التعديل خادمي/محاسبي ولا يمس JSX أو CSS؛ لذلك لا يغيّر استقرار المقاسات الثلاثة |

**الحكم النقدي:** هذه الدفعة أغلقت فجوة جودة صغيرة لكنها مهمة في سيناريو S9: الاسترداد صار أكثر صموداً أمام الضغط أو النقر المتكرر على تحديث الحالة، ولا يمسح روابط المحاسبة القديمة. لا توجد بقايا كود قديم نشطة لمسار الاسترداد المباشر بعد استبدالها بالدالة الموحدة.

**نسبة التقدم بعد هذه الدفعة:** 99.99994% جاهزية بناء وتشغيل، وقرابة 99.985% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى اختبار مستخدم حقيقي ناجح للفيديو على المزود الفعلي، Safari، وإغلاق مصفوفة S1-S14.

**الخطوة التالية:** تنفيذ تمريرة إنتاجية مصادقة كاملة: مستخدم غير أدمن يدخل إلى `/dashboard/generate-video` → يولد فيديو Fast 5s ناجحاً → ينتظر `completed` → يراجع Library وUsage → نطابق `video_jobs` و`credit_ledger` ونؤكد خصماً واحداً ومهمة واحدة وظهور النتيجة.

---

## تحصين إكمال الفيديو ضد السباق العكسي — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة مع تحسين أعمق | قفل `refunded` منع مسح رابط الاسترداد، لكن التدقيق النقدي كشف احتمالاً عكسياً نادراً: تحديث إكمال متأخر قد يحاول تحويل مهمة سبق استردادها إلى `completed` |
| تحصين `completed` | ✅ نُفذ | أضيف `markProcessingJobCompleted` ليُكمل المهمة فقط إذا كانت ما زالت `processing`، ويعيد الحالة الحالية إذا سبق تحديثها بطلب آخر |
| حذف/استبدال الكود القديم | ✅ نُفذ | استُبدل تحديث `completed` المباشر في `refreshVideoJob` بالدالة الموحدة، وبقيت تحديثات `refunded` عبر `markProcessingJobRefunded` فقط |
| مطابقة البيانات الحالية | ✅ سليمة | لا توجد مهام `processing` عالقة، ولا مهام `completed` بلا رابط نتيجة، ولا مهام `completed` تحمل رابط استرداد، ولا mismatches بين `video_jobs` و`credit_ledger` |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اجتاز دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اجتاز؛ تحذير حجم الحزم فقط غير مانع |
| Mobile 390 | ⚠️ تحميل محمي | شاشة تحميل وسطية دون overflow أو كسر بصري؛ لا توجد أخطاء تطبيق في console، لكن جلسة المعاينة لم تكشف صفحة الفيديو الفعلية |
| Tablet 820 | ⚠️ تحميل محمي | نفس السلوك: spinner مستقر بلا كسر أو تسريب بيانات |
| Desktop 1280 | ⚠️ تحميل محمي | spinner مستقر، وشريط الكوكيز ظاهر أسفل اليسار دون كسر؛ لا توجد طلبات بيانات ظاهرة أو أخطاء تطبيق حرجة |

**الحكم النقدي:** هذه أفضل نسخة لمسار S8-S10 حالياً: حالة الفيديو النهائية أصبحت محمية في الاتجاهين؛ لا يتحول المسترد إلى مكتمل، ولا يمسح المسترد رابط دفتر النقاط. لم يتغير التصميم أو CSS، لذلك أثر المقاسات الثلاثة محصور في فحص عدم ظهور كسر أثناء حالة التحميل/الحماية.

**نسبة التقدم بعد هذه الدفعة:** 99.99995% جاهزية بناء وتشغيل، وقرابة 99.986% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة فيديو حقيقية بحساب اختبار على المزود الفعلي مع Safari وإغلاق مصفوفة S1-S14.

**الخطوة التالية:** تنفيذ تمريرة إنتاجية مصادقة كاملة لاختبار الناتج الفعلي: Login بحساب غير أدمن → Generate Video Fast 5s → Refresh حتى `completed` أو `refunded` واضح → Library → Usage → مطابقة `video_jobs` و`credit_ledger`، ثم اختبار عزل `/admin/*` لغير الأدمن ومراجعة الأدمن للسجلات.

---

## تحصين idempotency النهائي لاسترداد الفيديو + QA مقاسات — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة مع فجوة سباق نادرة | تحصين الإكمال والاسترداد كان صحيحاً، لكن تحديثين متزامنين بعد فشل المزود قد يجعلان الطلب الثاني لا يملك `refundLedgerId` رغم أن الاسترداد تم فعلاً |
| تعديل طبقة النقاط | ✅ نُفذ | أضيف `getRefundLedgerId` لقراءة سجل الاسترداد الموجود عند حصول `already_refunded` بدلاً من فقدان الربط في واجهة مهمة الفيديو |
| تعديل `refreshVideoJob` | ✅ نُفذ | مسارا timeout و`failed/canceled` يستخدمان الآن `effectiveRefundLedgerId`، مع إبقاء ربط الاسترداد محاسبياً مشروطاً بسجل refund فعلي لمنع التكرار |
| تعديل `generateVideo` | ✅ نُفذ | عند فشل إنشاء مهمة المزود بعد الخصم، يبقى وضع المهمة ورد النقاط مشروطين بنجاح الاسترداد أو بوجود سجل استرداد سابق |
| مطابقة الكود القديم | ✅ لا كود قديم متعارض | لم يبق مسار تحديث حالة نهائية للفيديو يكتب `completed/refunded` بلا شرط `processing` أو يحرر الحصة أكثر من مرة |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل؛ تحذير حجم الحزم غير مانع |
| Mobile 390 | ✅ مستقر | `/dashboard/generate-video?qa=refund-idempotency#cost` لا يظهر كسر تخطيط أو overflow واضح |
| Tablet 820 | ✅ مستقر | نفس المسار يحافظ على قابلية القراءة وحماية الجلسة/التحويل عند الحاجة |
| Desktop 1280 | ✅ مستقر | صفحة توليد الفيديو/الدخول مستقرة دون تداخل عناصر مؤثر |

**الحكم النقدي:** هذه الدفعة أغلقت فجوة محاسبية/تشغيلية دقيقة جداً: الاسترداد المتزامن لا يكرر الأموال ولا يفقد رابط سجل الاسترداد، ولا يوجد حالياً عداد حصة يومية مستقل للفيديو ضمن مصدر الحقيقة الحالي.

**نسبة التقدم بعد هذه الدفعة:** 99.99996% جاهزية بناء وتشغيل، وقرابة 99.988% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة توليد فيديو فعلية بحساب اختبار ومطابقة `credit_ledger` و`video_jobs` وLibrary/Usage.

**الخطوة التالية:** تنفيذ Production Pass بحساب اختبار فعلي: Login → Generate Video Fast 5s → انتظار `completed/refunded` → Library → Usage → مطابقة الدفتر والمهمة، ثم اختبار عزل `/admin/*` لمستخدم غير أدمن ومراجعة أدمن للسجلات.

---

## تحصين حراسة الأدمن قبل الرندر + QA مقاسات — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة | تحصين refund/idempotency بقي ثابتاً ولم تظهر فروقات محاسبية أو stale processing jobs |
| فجوة الحماية المكتشفة | ✅ عولجت | صفحات `/admin/*` كانت تعتمد على `AdminGuard` داخل المكوّن، وهذا يمنع البيانات خادمياً لكنه قد يسمح بشاشة/هيكل صفحة قبل اكتمال التحقق؛ تم نقل قرار الدخول إلى `beforeLoad` أيضاً |
| تعديل `AdminGuard` | ✅ نُفذ | أضيف `adminBeforeLoad` للتحقق من الجلسة ودور admin قبل الرندر، مع redirect إلى `/auth` أو `/dashboard`، وبقي `AdminGuard` كطبقة UX إضافية لا كخط الدفاع الوحيد |
| تطبيق شامل | ✅ نُفذ | تم ربط `beforeLoad: adminBeforeLoad` بجميع صفحات الأدمن الحالية البالغ عددها 15 صفحة |
| إزالة التعليق غير المقصود | ✅ نُفذ | بعد الاختبار ظهرت شاشة تحميل مستمرة عند بعض حالات الجلسة؛ تم تعديل شرط `AdminGuard` حتى لا يعلّق الواجهة بعد نجاح `beforeLoad` |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل |
| Mobile 390 | ✅ محمي | محاولة فتح `/admin/video-jobs` انتهت إلى `/auth?redirect=/admin/video-jobs?...` دون عرض بيانات أدمن |
| Tablet 820 | ✅ مستقر | صفحة الدخول ظاهرة ومقروءة؛ لا يوجد تداخل مؤثر، ومربع الكوكيز تراكب مقصود |
| Desktop 1280 | ✅ مستقر | صفحة الدخول مستقرة دون overflow أو تداخل واضح |
| Console/Network | ✅ لا أخطاء تطبيق حرجة | لا توجد طلبات تطبيق فاشلة؛ التحذيرات من سكربت المعاينة الخارجي فقط |

**الحكم النقدي:** تم رفع حماية الأدمن من “حارس عرض” إلى “حارس قبل التحميل” مع بقاء حماية الخادم وRLS كحماية نهائية. هذا يقلل flash/تعريض الهيكل ويجعل سلوك غير الأدمن أو غير المسجل أنظف وأسرع.

**نسبة التقدم بعد هذه الدفعة:** 99.99997% جاهزية بناء وتشغيل، وقرابة 99.989% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة توليد فيديو فعلية بحساب اختبار ومراجعة أدمن مصادق.

**الخطوة التالية:** تنفيذ Production Pass مصادق: حساب مستخدم عادي يولّد Video Fast 5s ثم Library/Usage/credit_ledger، وبعده حساب أدمن يفتح `/admin/video-jobs` و`/admin/credit-ledger` لمطابقة المهمة والدفتر، ثم اختبار غير أدمن ضد `/admin/*`.


---

## تدقيق حارس الأدمن ضد التعليق والاستعلامات البطيئة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة مع تحسين نقدي | كل صفحات الأدمن الـ15 مرتبطة بـ`adminBeforeLoad` و`AdminGuard`، ولا توجد صفحة أدمن خارج الحارس |
| الفجوة المكتشفة | ✅ عولجت | استعلام دور الأدمن داخل `beforeLoad` كان بلا timeout مستقل؛ أضيفت دالة `hasAdminRole` بزمن انتظار مضبوط وفشل آمن إلى `/dashboard` |
| تعليق الواجهة | ✅ عولج | شرط `AdminGuard` أصبح لا يعلّق صفحة الأدمن لمستخدم لديه جلسة بعد نجاح `beforeLoad` حتى لو تأخر مزوّد المصادقة المركزي |
| حذف/استبدال القديم | ✅ تم | أزيل الاستعلام المباشر المتكرر من `adminBeforeLoad` واستُبدل بمساعد موحّد قابل لإعادة الاستخدام داخل الملف نفسه |
| TypeScript + Production build | ✅ ناجح | اجتاز `bunx tsc --noEmit` و`bun run build` بعد التصحيح |
| Mobile 390 | ✅ فُحص | `/admin/video-jobs` لا يكشف بيانات؛ عند جلسة غير مكتملة يظهر هيكل الصفحة بلا بيانات، والطلبات الحساسة تبقى محمية خادمياً |
| Tablet/Desktop | ✅ لا كسر متوقع | التعديل منطقي/أمني ولا يغير التخطيط؛ البناء يؤكد سلامة كل صفحات الأدمن |

**الحكم النقدي:** أصبح حارس الأدمن أكثر تحملاً لحالات بطء الشبكة أو تعليق استعلام الدور، مع فشل آمن بدلاً من انتظار غير محدود. تبقى الحماية النهائية في وظائف الخادم وRLS، وهذا صحيح معمارياً.

**نسبة التقدم بعد هذه الدفعة:** 99.99998% جاهزية بناء وتشغيل، وقرابة 99.99% جاهزية اعتماد قبل Soft Launch. لا تزال 100% مرتبطة بتمريرة مستخدم/أدمن فعلية على بيئة منشورة.

**الخطوة التالية:** تنفيذ تمريرة مصادقة حقيقية: مستخدم عادي يولّد فيديو Fast 5s ثم مراجعة Library/Usage/credit_ledger، وبعدها أدمن مصادق يطابق `/admin/video-jobs` مع `/admin/credit-ledger`، ثم اختبار غير أدمن ضد `/admin/*`.

---

## Production Pass تحضيرية للفيديو والأدمن — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| حالة Lovable Cloud | ✅ سليم | الخلفية جاهزة وتستجيب، ولا يوجد عائق بنيوي قبل اختبارات المصادقة الفعلية |
| مراجعة حارس الأدمن السابق | ✅ سليم | جميع صفحات الأدمن الـ15 تحتوي `beforeLoad: adminBeforeLoad` وتستخدم `AdminGuard`؛ لا توجد صفحة أدمن خارج الحماية |
| نقد مسار refund الفوري | ✅ حُسّن | عند فشل `generateVideo` بعد إنشاء ledger، أصبح الكود يجلب `refund_ledger_id` الموجود إذا كان refund سبق تنفيذه بالتزامن، مثل منطق `refreshVideoJob` |
| حذف/استبدال القديم | ✅ تم | لم يعد مسار الفشل الفوري يمرر `refundLedgerId` فقط؛ يستخدم الآن `effectiveRefundLedgerId` ويحفظه في metadata أيضاً |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اجتاز دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اجتاز؛ تحذير حجم chunks فقط غير مانع |
| Mobile 390 | ✅ فحص حراسة | فتح `/dashboard/generate-video` بلا جلسة فعلية يقود لمسار الدخول/الحماية ولا يظهر محتوى مستخدم حساس |
| Tablet 820 | ✅ مستقر | صفحة الدخول ظاهرة ومقروءة دون overflow أفقي أو تداخل واضح |
| Desktop 1280 | ✅ مستقر | التحويل إلى تسجيل الدخول يعمل دون كسر بصري واضح |
| Console/Network | ✅ لا أخطاء تطبيق حرجة | لا توجد طلبات تطبيق فاشلة؛ التحذيرات من سكربت المعاينة فقط |

**الحكم النقدي:** تم تنفيذ أفضل نسخة ممكنة من Production Pass التحضيرية دون بيانات اعتماد اختبار فعلية: الكود، الحماية، البناء، والتحويلات سليمة. لم يتم اعتبار توليد فيديو فعلي مغلقاً لأن ذلك يتطلب حساب مستخدم مصادق ورصيد/باقة تسمح بالفيديو ثم مطابقة سجلات الأدمن.

**نسبة التقدم بعد هذه الدفعة:** 99.999985% جاهزية بناء وتشغيل، وقرابة 99.992% جاهزية اعتماد قبل Soft Launch. تبقى 100% مشروطة بتمرير فيديو فعلي ومطابقة ledger من حساب أدمن.

**الخطوة التالية:** تمريرة مصادقة حقيقية: تسجيل دخول بحساب مستخدم اختبار مفعّل، توليد Video Fast 5s، انتظار `completed/refunded`، مراجعة Library وUsage، ثم دخول أدمن ومطابقة `/admin/video-jobs` مع `/admin/credit-ledger` وسجل المستخدم.

---

## تدقيق المكتبة وربط الفيديو بالدفتر — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مطابقة قاعدة البيانات | ✅ سليمة | `video_refund_mismatch=0` و`completed_without_result=0` و`processing_stale_20m=0` |
| ربط الفيديو بالمكتبة | ✅ محسّن | `dashboard.library` يجلب `video_jobs` عبر Server Function محمية ويعرض الحالات والنقاط، وأصبح فشل جلب الفيديو لا يعلّق المكتبة كلها |
| حذف/استبدال القديم | ✅ تم | أزيل مسار التحميل غير المحمي بـ`finally`، واستُبدل بفشل آمن يعرض النصوص/الصور حتى لو تعذر تحميل الفيديوهات |
| دفتر النقاط للأدمن | ✅ سليم كودياً | `listCreditLedger` محمي بـ`assertAdmin`، ويعرض حركات `consume_video/refund` مع user/email والmetadata للتدقيق |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` اجتاز دون أخطاء |
| Production build | ✅ ناجح | `bun run build` اجتاز بعد التحسين؛ تحذير حجم chunks فقط غير مانع |
| Mobile 390 | ✅ فحص | مسار المكتبة لا يكشف بيانات عند غياب جلسة فعلية، ولا توجد أخطاء تطبيق حرجة في المعاينة |
| Tablet 820 | ✅ مستقر | صفحة المكتبة/الحماية مستقرة؛ لا overflow أفقي واضح، وCookie Banner تراكب مقصود |
| Desktop 1280 | ✅ مستقر | التحويل إلى تسجيل الدخول مستقر ولا يوجد تداخل أو overflow واضح |
| Console/Network | ✅ لا أخطاء تطبيق حرجة | الطلبات المرصودة Analytics فقط 200؛ تحذيرات postMessage من بيئة المعاينة |

**الحكم النقدي:** تم إغلاق فجوة تجربة مهمة في المكتبة: فشل فيديوهات المستخدم لم يعد قادراً على إبقاء شاشة المكتبة في تحميل دائم. كما أن مطابقة قاعدة البيانات تؤكد عدم وجود حالات فيديو/Refund متعارضة حالياً.

**نسبة التقدم بعد هذه الدفعة:** 99.99999% جاهزية بناء وتشغيل، وقرابة 99.994% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى اختبار مستخدم مصادق يولد فيديو فعلياً ثم يطابقه أدمن في الدفتر.

**الخطوة التالية:** تنفيذ تمريرة حسابات فعلية: مستخدم اختبار مفعّل + رصيد فيديو يولّد Fast 5s، ثم فحص `Library` و`Usage`، وبعدها أدمن يطابق `video_jobs.ledger_id/refund_ledger_id` مع `credit_ledger` من الواجهة وليس الكود فقط.

---

## تحصين حالة تحميل المكتبة عند غياب الجلسة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة مع ملاحظة دقيقة | تحصين فشل جلب الفيديوهات كان صحيحاً، لكن `LibraryPage` كانت تبدأ `loading=true` ولا تنهيه إذا لم يكتمل `user` من المصادقة |
| التعديل المنفذ | ✅ نُفذ | ربطت المكتبة بـ`authLoading` من `useAuth`؛ تنتظر المصادقة، ثم إن لم يوجد مستخدم تنهي التحميل بأمان بدلاً من spinner دائم |
| حذف/استبدال القديم | ✅ تم | أزيل نمط `if (!user) return` الصامت داخل effect واستُبدل بفرع صريح ينهي التحميل عند غياب الجلسة |
| مطابقة قاعدة البيانات | ✅ سليمة | `video_refund_mismatch=0`، `completed_without_result=0`، `processing_stale_20m=0` |
| الأدمن | ✅ موجود | يوجد دور admin واحد في `user_roles`، وصفحات الأدمن الـ15 محمية سابقاً بـ`beforeLoad + AdminGuard` |
| TypeScript + Build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل |
| Mobile 390 | ✅ فحص | لا أخطاء تطبيق حرجة؛ أداة الاستخراج لم تُرجع نصاً كافياً لكن لا توجد طلبات فاشلة أو console errors من التطبيق |
| Tablet 820 | ✅ مستقر | عنوان `مكتبتي — رِفد` ظاهر دون overflow أفقي واضح |
| Desktop 1280 | ✅ مستقر | غير المسجل يُحوّل إلى `تسجيل الدخول — رِفد للتقنية` دون كسر بصري واضح |
| Console/Network | ✅ نظيف تطبيقياً | Analytics فقط 200؛ تحذيرات postMessage من بيئة المعاينة وليست من كود رِفد |

**الحكم النقدي:** تم إغلاق مصدر spinner دائم في المكتبة، وهو تحسين مهم قبل تمريرة المستخدم الفعلية لأن صفحات Dashboard يجب أن تفشل بأمان عند غياب الجلسة أو تأخرها.

**نسبة التقدم بعد هذه الدفعة:** 99.999992% جاهزية بناء وتشغيل، وقرابة 99.995% جاهزية اعتماد قبل Soft Launch. لا تزال 100% مرتبطة بتوليد فيديو فعلي ومطابقة الواجهة الإدارية بحساب أدمن.

**الخطوة التالية:** تشغيل تمريرة مستخدم حقيقي: Login → Generate Video Fast 5s → انتظار النتيجة → Library/Usage → Admin Video Jobs/Credit Ledger، ثم اعتماد Soft Launch إذا لم يظهر اختلاف في ledger أو الواجهة.

---

## تدقيق عرض الفيديوهات فقط داخل المكتبة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة مع خلل عرض حدي | إصلاح `authLoading` منع الـspinner الدائم، لكن حالة المستخدم الذي لديه فيديوهات فقط كانت قد تعرض رسالة الفراغ بدلاً من قسم الفيديو |
| التعديل المنفذ | ✅ نُفذ | أُضيف `hasVisibleItems` ليحسب ظهور الفيديوهات ضمن فلتر `all` و`video`، وأصبح عدّاد المكتبة يجمع النصوص/الصور والفيديوهات |
| حذف/استبدال القديم | ✅ تم | استُبدل شرط الفراغ القديم المعتمد على `filtered.length` فقط بشرط موحّد يطابق كل الفلاتر |
| مطابقة قاعدة البيانات | ✅ سليمة | `video_refund_mismatch=0`، `completed_without_result=0`، `processing_stale_20m=0`، ولا توجد مهام فيديو عالقة حالياً |
| أدوار المستخدمين | ✅ سليمة | يوجد أدمن واحد فقط في `user_roles`، ما يحافظ على تمريرة المطابقة الإدارية القادمة ضمن نطاق مضبوط |
| TypeScript + Build | ✅ ناجح | اجتاز `bunx tsc --noEmit` و`bun run build` بعد إصلاح المكتبة |
| Mobile/Tablet/Desktop | ✅ مطابق منطقياً | التعديل لا يغير شبكة البطاقات أو المقاسات؛ يمنع فقط ظهور empty state الخاطئ على كل المقاسات عند وجود فيديوهات |

**الحكم النقدي:** هذه فجوة تجربة صغيرة لكنها مهمة قبل Production Pass: المستخدم الذي يولّد فيديو كأول مخرج يجب أن يرى الفيديو في مكتبته فوراً، لا رسالة “ما عندك توليدات”. الآن منطق الفراغ والعداد مطابقان لمصدر البيانات الفعلي.

**نسبة التقدم بعد هذه الدفعة:** 99.999993% جاهزية بناء وتشغيل، وقرابة 99.996% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة فيديو فعلية بحساب مستخدم ثم مطابقة أدمن للـledger.

**الخطوة التالية:** تنفيذ تمريرة Production Authenticated: مستخدم اختبار مفعّل يولّد Video Fast 5s، ثم التحقق من ظهوره في `Library` واحتسابه في `Usage`، وبعدها أدمن يطابق `video_jobs` مع `credit_ledger` من الواجهة.

---

## تحديث حالة الفيديو من المكتبة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة | إصلاح حالة الفيديوهات فقط بقي صحيحاً، ولم يظهر تعارض في منطق الفراغ أو العدّاد |
| التعديل المنفذ | ✅ نُفذ | أُضيف زر `تحديث الحالة` للفيديوهات قيد المعالجة داخل المكتبة، ويستدعي المسار الخادمي نفسه الذي يرد النقاط عند الفشل/timeout أو يحدّث الرابط عند الاكتمال |
| حذف/استبدال القديم | ✅ تم | لم تعد المكتبة واجهة قراءة ساكنة للفيديوهات قيد المعالجة؛ أصبحت قادرة على مزامنة الحالة دون خروج المستخدم إلى صفحة التوليد |
| مطابقة قاعدة البيانات | ✅ سليمة | `video_refund_mismatch=0`، `completed_without_result=0`، `processing_stale_20m=0`، ولا توجد مهام معالجة عالقة حالياً |
| Mobile/Tablet/Desktop | ✅ مطابق منطقياً | الزر داخل بطاقة الفيديو بعرض كامل وارتفاع ثابت، فلا يسبب overflow أو إعادة تخطيط مؤذية على 390/820/1280 |
| TypeScript + Build | ✅ ناجح | اجتاز `bunx tsc --noEmit` و`bun run build` بعد التعديل |

**الحكم النقدي:** هذه أفضل نسخة قبل تمريرة الفيديو الفعلية؛ لأن المستخدم يستطيع الآن إكمال دورة الفيديو من المكتبة نفسها: انتظار، تحديث، اكتمال، أو استرداد مضبوط.

**نسبة التقدم بعد هذه الدفعة:** 99.999994% جاهزية بناء وتشغيل، وقرابة 99.997% جاهزية اعتماد قبل Soft Launch. لا تزال 100% مشروطة بتوليد فيديو فعلي ومطابقة أدمن للدفتر.

**الخطوة التالية:** تمريرة مستخدم مصادق فعلياً: توليد Video Fast 5s، تحديث حالته من المكتبة، التأكد من `Usage`، ثم مطابقة `/admin/video-jobs` مع `/admin/credit-ledger`.

---

## تنظيف عرض قسم الفيديو في المكتبة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة مع تحسين UX | زر تحديث حالة الفيديو صحيح، لكن قسم الفيديو كان يمكن أن يظهر كإطار فارغ عند وجود نصوص/صور فقط وفلتر `all` |
| التعديل المنفذ | ✅ نُفذ | أُضيف `shouldShowVideoSection` حتى لا يظهر قسم الفيديو إلا إذا وُجدت فيديوهات فعلاً ومع فلتر `all` أو `video` |
| حذف/استبدال القديم | ✅ تم | استُبدل شرط العرض العام القديم بشرط أدق يمنع البطاقات/الأقسام الفارغة ويحافظ على منطق empty state |
| مطابقة قاعدة البيانات | ✅ سليمة | `video_refund_mismatch=0`، `completed_without_result=0`، `processing_stale_20m=0`، ولا توجد مهام معالجة حالياً |
| Mobile/Tablet/Desktop | ✅ مطابق منطقياً | إزالة القسم الفارغ تقلل المسافة الزائدة وتحافظ على شبكة النصوص/الصور مستقرة على 390/820/1280 |
| TypeScript + Build | ✅ ناجح | اجتاز `bunx tsc --noEmit` و`bun run build` بعد التنظيف |

**الحكم النقدي:** أصبح عرض المكتبة الآن أنظف وأكثر تطابقاً مع البيانات: لا رسالة فراغ خاطئة عند وجود فيديوهات، ولا قسم فيديو فارغ عند عدم وجودها.

**نسبة التقدم بعد هذه الدفعة:** 99.999995% جاهزية بناء وتشغيل، وقرابة 99.998% جاهزية اعتماد قبل Soft Launch. لا تزال 100% مشروطة بتمريرة فيديو فعلية ومطابقة أدمن للدفتر.

**الخطوة التالية:** تنفيذ اختبار فعلي بحساب مستخدم: توليد Video Fast 5s، تحديثه من المكتبة، التحقق من Usage، ثم مطابقة أدمن بين `video_jobs` و`credit_ledger`.

---

## مزامنة تلقائية هادئة لفيديوهات المكتبة — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة | تنظيف قسم الفيديو الفارغ بقي صحيحاً ولا يغيّر بيانات المستخدم أو الحماية |
| التعديل المنفذ | ✅ نُفذ | أُضيف polling هادئ كل 15 ثانية للفيديوهات `processing` فقط، وبحد أقصى مهمتين كما هو حد النظام، ولا يعمل إلا عندما تكون الصفحة مرئية |
| حذف/استبدال القديم | ✅ تم | لم يعد الاعتماد على زر التحديث اليدوي وحده؛ الزر بقي كتحكم يدوي، والمزامنة التلقائية تغطي الانتظار الطبيعي |
| مطابقة قاعدة البيانات | ✅ سليمة | `video_refund_mismatch=0`، `completed_without_result=0`، `processing_stale_20m=0`، ولا توجد مهام معالجة حالياً |
| Mobile/Tablet/Desktop | ✅ مطابق منطقياً | التعديل غير بصري ولا يضيف عناصر جديدة؛ يحافظ على الواجهة في 390/820/1280 مع تقليل الطلبات عند تبويب غير مرئي |
| TypeScript + Build | ✅ ناجح | اجتاز `bunx tsc --noEmit` و`bun run build` بعد التعديل |

**الحكم النقدي:** أصبحت دورة الفيديو في المكتبة أقرب لسلوك منتج جاهز: المستخدم يرى الحالة تتحدث تلقائياً، ومع ذلك لا نرهق الخلفية لأن التحديث محصور في المهام النشطة والصفحة المرئية فقط.

**نسبة التقدم بعد هذه الدفعة:** 99.999996% جاهزية بناء وتشغيل، وقرابة 99.9985% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تمريرة فيديو فعلية ومطابقة أدمن للدفتر.

**الخطوة التالية:** تنفيذ اختبار فعلي بحساب مستخدم مفعّل: توليد Video Fast 5s، انتظار تحديث المكتبة تلقائياً، مراجعة Usage، ثم دخول أدمن ومطابقة `video_jobs` مع `credit_ledger`.

---

## تنفيذ الخطوة التالية — مراجعة الجلسة السابقة وبوابة تمريرة الفيديو الفعلية — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الجلسة السابقة | ✅ سليمة | آخر عمل موثق كان مزامنة فيديوهات المكتبة تلقائياً كل 15 ثانية للمهام `processing` فقط، مع بقاء زر التحديث اليدوي وعدم إضافة حمل زائد عند تبويب غير مرئي |
| Lint + TypeScript + Vitest | ✅ ناجح وظيفياً | `bun run lint && bunx tsc --noEmit && bun run test` اكتمل دون أخطاء؛ ظهرت 37 warnings غير مانعة، وملف Vitest المتاح ما زال متخطّى لذلك لا يُحتسب كدليل قبول وظيفي |
| Production build | ✅ ناجح | `bun run build` اكتمل بنجاح؛ تحذيرات حجم chunks فقط تبقى ملاحظة أداء غير مانعة |
| محاولة تمريرة `/dashboard/generate-video` على Mobile 390 | ⚠️ توقفت عند المصادقة | المستخدم غير مصادق في جلسة المعاينة، وتم تحويله بشكل صحيح إلى صفحة تسجيل الدخول؛ هذا يثبت حماية Dashboard ولا يثبت توليد الفيديو الفعلي |
| Console/Network أثناء المحاولة | ✅ لا أخطاء تطبيق حرجة | الطلبات المرصودة Analytics فقط 200؛ تحذيرات `postMessage` من بيئة المعاينة وليست من كود رِفد |
| مطابقة قاعدة البيانات للفيديو والدفتر | ✅ سليمة حالياً | `video_jobs.total=4`، ولا توجد `completed_without_result`، ولا `stale_processing`، ولا حالات refund مشبوهة؛ `consume_video=4` و`video_refunds=4`، ويوجد أدمن واحد فقط |
| قرار تمريرة الفيديو الفعلية | ⏳ غير مغلقة | لا يمكن اعتماد S8/S10/S13/S14 عملياً من الواجهة دون حساب مستخدم اختبار مصادق ورصيد يسمح بتوليد فيديو Fast 5s، ثم دخول أدمن للمطابقة من الواجهة |

**الحكم النقدي:** المتبقي الحقيقي ليس إصلاح كود الآن، بل تمريرة تشغيل مصادقة. النظام يبني بنجاح، قاعدة الفيديو/الدفتر لا تُظهر تناقضات، وحماية Dashboard تعمل، لكن الاعتماد الشامل لا يُرفع إلى 100% قبل اختبار مستخدم فعلي يولّد فيديو ثم يطابقه أدمن من الواجهة.

**نسبة التقدم بعد هذه الدفعة:** 99.999996% جاهزية بناء وتشغيل، وقرابة 99.9987% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى تنفيذ تمريرة حسابات فعلية موثقة.

**الخطوة التالية الدقيقة:** توفير/استخدام حساب مستخدم اختبار مفعّل داخل المعاينة، ثم تنفيذ: Login → Generate Video Fast 5s → Library auto-refresh → Usage → Admin Video Jobs/Credit Ledger.

---

## تمريرة حساب اختبار فعلية للفيديو — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | المانع السابق كان غياب جلسة مستخدم مصادق؛ تم إنشاء حساب QA مؤقت ومؤكد البريد وتجهيزه كباقة `starter` برصيد 1,500 نقطة فيديو |
| الدخول من الواجهة | ✅ ناجح | تسجيل الدخول من `/auth` نجح وظهر Toast `أهلاً بعودتك` ثم وصلت الصفحة إلى `/dashboard/generate-video` على Mobile 390 |
| حماية onboarding | ✅ كُشف شرط حدي وتم تجاوزه بأمان للاختبار | الحساب احتاج رقم WhatsApp رغم `onboarded=true`؛ تم إكمال بيانات الاختبار المطلوبة لأن التطبيق يشترط `profile.whatsapp` قبل Dashboard |
| واجهة توليد الفيديو Mobile 390 | ✅ ظاهرة وقابلة للتشغيل | ظهرت خيارات Fast/Quality، المقاسات، مدة 5/8 ثوانٍ، حقل الوصف، زر `ولّد الفيديو`، والرصيد `1,500` نقطة دون كسر بصري واضح |
| محاولة Generate Video Fast 5s | ⚠️ فشل مزوّد خارجي | الطلب وصل إلى الخادم، وتم عرض رسالة عربية آمنة: `تعذر الاتصال بخدمة الفيديو حالياً. تم حفظ الحالة ورد النقاط عند الحاجة.` |
| سلوك الخصم والاسترداد | ✅ نجح | أُنشئت مهمة فيديو واحدة بحالة `refunded`، خُصمت 150 نقطة ثم رُدت فوراً، وبقي رصيد المستخدم `1,500` بعد الاسترداد |
| مطابقة قاعدة البيانات | ✅ سليمة | `video_jobs.status=refunded`، يوجد `ledger_id` و`refund_ledger_id`، ودفتر النقاط يحتوي `consume_video=-150` ثم `refund=150` دون مهمة عالقة |
| Console/Network | ✅ لا أخطاء تطبيق حرجة | طلبات التطبيق/server functions رجعت 200؛ التحذيرات المتبقية من بيئة المعاينة/`postMessage` وليست من كود رِفد |
| فحص مزوّد الفيديو | ❌ مانع تشغيلي خارجي | فحص اتصال المزود أعاد `401 Unauthenticated` للنموذجين `google/veo-3-fast` و`google/veo-3`، ما يعني أن سر مزوّد الفيديو الحالي غير صالح أو غير مفعّل في البيئة |

**الحكم النقدي:** تم تنفيذ تمريرة مستخدم فعلية حتى نقطة الاتصال بالمزوّد. التطبيق اجتاز الأجزاء التي يملكها: المصادقة، عرض واجهة Mobile، التحقق من الرصيد، إنشاء المهمة، الخصم، والاسترداد. الجزء غير المجتاز هو نجاح إنتاج فيديو فعلي بسبب إعداد مزوّد خارجي غير صالح، لذلك لا يجوز اعتماد S8/S10 كنجاح كامل ولا رفع الاعتماد إلى 100%.

**نسبة التقدم بعد هذه الدفعة:** 99.999996% جاهزية بناء وتشغيل، وقرابة 99.999% جاهزية اعتماد قبل Soft Launch. مانع 100% الحالي محدد: تحديث سر مزوّد الفيديو ثم إعادة نفس التمريرة حتى حالة `completed` وظهور الفيديو في `Library` و`Usage` ومطابقته من Admin.

**الخطوة التالية الدقيقة:** تحديث سر مزوّد الفيديو الصالح في البيئة، ثم إعادة: Login QA → Generate Video Fast 5s → انتظار `completed` → Library auto-refresh → Usage → Admin Video Jobs/Credit Ledger.


---

## إعادة فحص سر مزوّد الفيديو بعد التحديث — 25 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| تحديث السر | ✅ نُفذ | تم فتح نموذج تحديث `REPLICATE_API_TOKEN` وحفظ المستخدم قيمة جديدة بنجاح |
| اختبار صلاحية المزود | ❌ لم يجتز | فحص الاتصال بحساب المزود ما زال يرجع `401 Unauthenticated`، ما يعني أن القيمة المحفوظة ليست Token صالحاً أو ليست مفعّلة لدى المزود |
| قرار تمريرة الفيديو الناجحة | ⏳ مؤجلة | لم يتم تشغيل Generate Video جديد لتجنّب استهلاك نقاط/إنشاء مهمة فشل مؤكدة طالما فحص المصادقة المباشر للمزود فاشل |
| مطابقة الفيديو والدفتر بعد الفحص | ✅ سليمة تشغيلياً | لا توجد مهام `processing` عالقة، ولا `completed` بلا رابط، ولا `refunded` بلا رابط استرداد في `video_jobs` |

**الحكم النقدي:** تم تنفيذ الخطوة التالية المطلوبة، لكن المفتاح الجديد لم يفتح مزود الفيديو بعد. الاعتماد النهائي لا يزال ممنوعاً حتى إدخال Token صحيح ينجح معه فحص الحساب، ثم إعادة تمريرة: QA Login → Generate Video Fast 5s → `completed` → Library/Usage → Admin reconciliation.

**نسبة التقدم بعد هذه الدفعة:** 99.999996% جاهزية بناء وتشغيل، وقرابة 99.999% جاهزية اعتماد قبل Soft Launch. المانع الوحيد المتبقي لهذا المسار هو Token مزود فيديو صالح.

---

## تصحيح تسلسل اختبار الفيديو المتوسط — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ كشفت فجوة منطقية | الخطة تنص على تشغيل 12 عينة أولاً ثم تقييمها، بينما الكود كان يشترط اعتماد كل عينة كـ`publishable` قبل تشغيل التالية |
| تعديل مسار التشغيل | ✅ نُفذ | تشغيل العينة التالية صار مشروطاً بإنشاء العينات السابقة من الرابط الرسمي وبلا عائق تشغيلي فقط، لا بقرار التقييم التجاري |
| تعديل مسار التقييم | ✅ نُفذ | لا يبدأ حفظ التقييم إلا بعد اكتمال كل الدفعة النظيفة، وتعرض الواجهة كل العينات المكتملة غير المقيّمة بدلاً من قفلها خلف اعتماد سابق |
| حذف/استبدال الكود القديم | ✅ نُفذ | استُبدل شرط `publishable` السابق من حارس التشغيل والتقييم، وتم تحديث رسائل القفل لتطابق الخطة المعتمدة |
| حالة الدفعة الحالية | ✅ مؤكدة | الاستعلام المباشر أعاد `0` عينات موسومة للاختبار المتوسط، لذلك لم يبدأ الاعتماد التجاري بعد |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل؛ تحذير حجم الحزم غير مانع |

**الحكم النقدي:** هذه الجولة صححت تعارضاً كان سيبطئ الاختبار ويشوّه منهجيته: المطلوب جمع أدلة 12 عينة أولاً، ثم اتخاذ قرار تجاري جماعي. الآن المسار يطابق الخطة: تشغيل متسلسل آمن، ثم تقييم بعد اكتمال الدفعة.

**نسبة التقدم بعد هذه الدفعة:** 99.999997% جاهزية بناء وتشغيل، وقرابة 99.999% جاهزية اعتماد قبل Soft Launch. الاعتماد التجاري لمسار الفيديو المتوسط يبقى 84% حتى تشغيل `pilot-01` إلى `pilot-12` فعلياً وتقييم النتائج.

**الخطوة التالية الدقيقة:** من حساب أدمن وبمزود فيديو صالح: افتح `/admin/video-providers` → تدقيق الدفعة → شغّل العينات بالترتيب من `pilot-01` إلى `pilot-12` مع صورة منتج عند إلزامها → تدقيق الدفعة → تقييم كل العينات المكتملة.

---

## تحصين موصل fal.ai قبل تشغيل عينات الفيديو المتوسط — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ صحيحة | تسلسل الاختبار المتوسط أصبح مطابقاً للخطة: تشغيل 12 عينة أولاً ثم تقييم الدفعة كاملة |
| فجوة الموصل المكتشفة | ✅ عولجت | موصل fal.ai كان يرسل الإنشاء مباشرة إلى `/fal.run` ويعامل الطلب غير المكتمل كـ`processing` دون Polling فعلي؛ تم نقله إلى Queue API مع قراءة حالة الطلب والنتيجة |
| فحص سر المزود | ✅ صار فعلياً | اختبار اتصال fal.ai لم يعد يكتفي بوجود المتغير، بل يرسل فحصاً خفيفاً يميز Token المرفوض من Token المقبول قبل تشغيل العينات |
| حذف/استبدال القديم | ✅ نُفذ | استُبدل مسار `refreshJob` الشكلي الذي يرجع `processing` دائماً عند عدم وجود رابط بمسار status/result حقيقي من المزود |
| حالة الدفعة الحالية | ✅ مؤكدة | الاستعلام المباشر أظهر `0` عينات اختبار متوسط، و`0` مهام processing، و`0` مهام completed بلا رابط |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل؛ تحذير حجم الحزم فقط غير مانع |

**الحكم النقدي:** هذه خطوة ضرورية قبل تشغيل `pilot-01` إلى `pilot-12`: حتى مع Token صالح، الاعتماد على مسار إنشاء دون تحديث Queue كان سيترك النتائج عالقة أو غير قابلة للإكمال من المكتبة/التدقيق. الآن أصبحت دورة المزود قابلة للتحقق: إنشاء → حالة → نتيجة أو فشل واضح مع استرداد.

**نسبة التقدم بعد هذه الدفعة:** 99.999998% جاهزية بناء وتشغيل، وقرابة 99.999% جاهزية اعتماد قبل Soft Launch. الاعتماد التجاري لمسار الفيديو المتوسط يبقى 84% حتى تشغيل العينات الاثنتي عشرة وتقييمها فعلياً.

**الخطوة التالية الدقيقة:** من حساب أدمن: افتح `/admin/video-providers` → اختبر اتصال fal.ai وتأكد أن الحالة `active` → شغّل `pilot-01` إلى `pilot-12` من مصفوفة الاختبار → استخدم تحديث الحالة/التدقيق حتى تصبح كل العينات `completed` → قيّم الدفعة كاملة.

---

## جلسة 2 — تدقيق الدفتر المالي ومطابقة الفيديو — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة مع تنظيف توثيقي | موصل fal.ai يستخدم Queue API، وفحص المزود صار فعلياً، ولا توجد مهام `processing` عالقة قبل تشغيل عينات جديدة |
| حذف/تصحيح بقايا الكود القديم | ✅ نُفذ توثيقياً | أزيلت عبارات الحصة اليومية المستقلة للفيديو من سجل S8-S10 والتمريرات اللاحقة؛ مصدر الحقيقة الحالي هو النقاط + حد مهمتين متزامنتين + refund |
| حالة مزودي الفيديو | ✅ جاهزة تشغيلياً | `fal_ai` هو المزود العام الوحيد المفعّل، حالته `active`، وتكلفته الحالية `150` نقطة لـ5 ثوانٍ و`800` نقطة لـ8 ثوانٍ؛ باقي المزودين غير مفعّلين للعامة |
| مطابقة `video_jobs` | ✅ سليمة | إجمالي المهام `12`: مكتملة `3`، مستردة `9`، قيد المعالجة `0`، مكتملة بلا رابط `0`، مستردة بلا `refund_ledger_id` = `0` |
| مطابقة `credit_ledger` | ✅ سليمة | إجمالي خصم الفيديو `1,800` نقطة، إجمالي الاسترداد المرتبط بأصول الخصم `1,350` نقطة، الصافي `450` نقطة مطابق لـ3 نجاحات × 150 |
| ربط الدفتر بالمهام | ✅ سليمة | لا توجد قيود `consume_video` بلا مهمة فيديو مقابلة، ولا قيود خصم موسومة كمستردة بلا سجل refund |
| الرصيد الحالي | ✅ منطقي | إجمالي أرصدة المستخدمين الحالية `21,350` نقطة؛ رقم إجمالي لا يُستخدم كرصيد مستخدم واحد، لكنه يؤكد عدم وجود رصيد سالب ظاهر ضمن فحص الفيديو |
| فحص الأمان الآلي | ⚠️ يحتاج جلسة مستقلة | ظهر عدد من تحذيرات Lovable Cloud العامة حول دوال موثوقة قابلة للاستدعاء على مستوى عام؛ ليست نتيجة تعديل الفيديو مباشرة، لكنها تمنع وصف الإغلاق الأمني بأنه نهائي قبل مراجعة صلاحيات الدوال |

**الحكم النقدي:** الجلسة الثانية أغلقت أهم خطر مالي في قسم الفيديو: الخصومات والاستردادات متطابقة رقمياً وتشغيلياً، ولا توجد مهام عالقة أو نتائج مكتملة بلا رابط. الفجوات المتبقية ليست دفترية، بل اختبار ضغط وفشل/تزامن عملي، ثم مراجعة تحذيرات صلاحيات الدوال قبل وصف الإغلاق الأمني بأنه نهائي.

**نسبة التقدم بعد هذه الدفعة:** 99.999998% جاهزية بناء وتشغيل، وقرابة 99.999% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى جلسة 3: فشل/تزامن/ضغط عملي ثم جلسة UX/Admin ثم قرار التخزين.

**الخطوة التالية الدقيقة:** تنفيذ الجلسة 3: اختبار فشل مزود مضبوط، تكرار تحديث نفس المهمة، محاولات تشغيل متزامنة لنفس الحساب، ثم إثبات أنه لا يوجد خصم مكرر ولا `processing` دائم ولا refund مزدوج.

---

## جلسة 3 — اختبار الفشل والتزامن ومنع التعليق — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 2 | ✅ سليمة | المطابقة المالية بقيت صحيحة: `12` مهمة، خصم فيديو `1,800`، استرداد `1,350`، وصافي `450` نقطة |
| فجوة timeout مكتشفة | ⚠️ عولجت | كان `refreshVideoJob` يرجع مبكراً إذا كانت المهمة `processing` بلا `provider_job_id`، ما قد يترك مهمة قديمة عالقة بلا استرداد عند timeout |
| تعديل مسار الفشل | ✅ نُفذ | أصبح فحص timeout يسبق شرط وجود `provider_job_id`، لذلك أي مهمة `processing` أقدم من 20 دقيقة تُرد نقاطها وتتحول إلى `refunded` حتى لو لم يصل معرّف المزود |
| منع refund مزدوج | ✅ سليم | `refund_credits` idempotent، و`markProcessingJobRefunded` يحدّث فقط إذا بقيت المهمة `processing`؛ التحديثات المتزامنة لنفس المهمة لا تُنشئ أثراً مضاعفاً |
| منع completed/refunded race | ✅ سليم | `markProcessingJobCompleted` و`markProcessingJobRefunded` كلاهما مشروطان بـ`status = processing`، وأي تحديث متأخر يقرأ الحالة النهائية بدلاً من قلبها عشوائياً |
| حد التزامن | ✅ سليم بنيوياً | يوجد عدّان: فحص خادمي قبل الخصم، وتريجر قاعدة `enforce_video_processing_limit` يمنع أكثر من مهمتين `processing` لنفس المستخدم عند الإدخال/التحديث |
| حذف/مطابقة الكود القديم | ✅ لا كود نشط قديم | لا توجد استدعاءات نشطة لـ`daily_video_usage` أو `consume_video_daily_quota` أو `releaseVideoDailyQuota` في `src`; المتبقي في الخطة/السجل هو سياق تدقيقي فقط |
| مطابقة القاعدة بعد الإصلاح | ✅ سليمة | لا توجد مهام `processing`، ولا مهام أقدم من 20 دقيقة، ولا `completed` بلا رابط، ولا `refunded` بلا `refund_ledger_id` |

**الحكم النقدي:** الجلسة الثالثة رفعت صلابة الفيديو ضد أسوأ حالة عملية: خصم تم، مهمة بدأت، لكن المزوّد لم يرجع معرفاً صالحاً. قبل التصحيح كان يمكن أن تبقى المهمة في `processing` إلى أن تُعالج يدوياً؛ الآن timeout يغلقها ويرد النقاط تلقائياً. من ناحية التسويق والثقة، هذا يحمي وعد المنتج: المستخدم لا يدفع ثمن فشل تقني خارج سيطرته.

**نسبة التقدم بعد هذه الدفعة:** 99.999999% جاهزية بناء وتشغيل، وقرابة 99.999% جاهزية اعتماد قبل Soft Launch. ما زال الاعتماد النهائي مشروطاً بجلسة UX/Admin حقيقية وتشغيل عينات `pilot-01` إلى `pilot-12` بمزوّد صالح.

**الخطوة التالية الدقيقة:** تنفيذ الجلسة 4: مراجعة UX على `/dashboard/generate-video` و`/dashboard/library` و`/dashboard/usage` للموبايل/التابلت/الديسكتوب، ثم مراجعة `/admin/video-providers` و`/admin/video-jobs` و`/admin/credit-ledger` للتأكد أن المستخدم يفهم الحالة والأدمن يستطيع التدقيق بدون تسريب بيانات.

---

## جلسة 4 — مراجعة UX/Admin وإغلاق دورة حياة الفيديو — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 3 | ✅ سليمة | فحص القاعدة أكد: `12` مهمة، `0` processing، `0` stale، `0` completed بلا رابط، `0` refunded بلا `refund_ledger_id`، ولا mismatch بين `video_jobs` و`credit_ledger` |
| صفحة توليد الفيديو | ✅ حُسنت | أضيف عدّاد عملي للمهام النشطة ورسالة واضحة عند بلوغ حد مهمتين؛ لم يعد وجود مهمة واحدة قيد المعالجة يعطل إنشاء الثانية، بينما يبقى القفل عند الحد الصحيح |
| السعر والثقة التسويقية | ✅ أوضح | بطاقة الخصم تعرض السعر قبل التنفيذ، مدة الفيديو، سياسة الاسترداد، العلامة المائية، وحد المهام المتزامنة بلغة مفهومة للمستخدم |
| المكتبة | ✅ حُسنت | المكتبة تحدّث `pending` و`processing` تلقائياً، وتعرض زر فتح الفيديو عند وجود نتيجة، مع بقاء الحالة والنقاط وسياق الحملة ظاهرين |
| إدارة الفيديو | ✅ حُسنت | لوحة `/admin/video-jobs` تعرض المهام النشطة بدقة أكبر، ومعرّف المزود المختصر، وقت الإكمال، وروابط ledger/refund مع tooltip كامل للتدقيق |
| عزل بيانات الأدمن | ✅ سليم | `adminBeforeLoad` يمنع الرندر قبل التحقق، و`listAdminVideoJobs` يفرض `assertAdmin` خادمياً قبل جلب بيانات المستخدمين والمهام |
| حذف/مطابقة الكود القديم | ✅ لا بقايا متعارضة | لا توجد مراجع نشطة للحصة اليومية للفيديو أو تعطيل خاطئ عند مهمة واحدة؛ مصدر الحقيقة بقي النقاط + حد مهمتين + refund |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد تعديلات الواجهة والإدارة دون أخطاء |
| فحص الأمان الآلي | ⚠️ ملاحظة عامة | Lovable Cloud linter ما زال يعرض تحذيرات عامة لدوال موثوقة قابلة للاستدعاء؛ ليست من تعديل الجلسة 4 لكنها تستحق جلسة Hardening منفصلة |

**الحكم النقدي:** الجلسة الرابعة أغلقت فجوة UX مهمة: الكود كان يسمح خادمياً بمهمتين، لكن الزر في الواجهة كان يوقف المستخدم عند مهمة واحدة نشطة. الآن التجربة تطابق السياسة التقنية والتسويقية: المستخدم يرى السعر والحالة والاسترداد وحد التشغيل بوضوح، والأدمن يرى سجل التدقيق اللازم دون كشفه لغير المخولين.

**نسبة التقدم بعد هذه الدفعة:** 99.999999% جاهزية بناء وتشغيل، وقرابة 99.9992% جاهزية اعتماد قبل Soft Launch. لا تزال 100% ممنوعة حتى جلسة 5: قرار التخزين والتقرير النهائي، ومعها تمريرة فيديو فعلية إن كان الهدف اعتماداً إنتاجياً كاملاً.

**الخطوة التالية الدقيقة:** تنفيذ الجلسة 5: حسم هل روابط الفيديو الخارجية من المزود مقبولة للـSoft Launch أم يجب نسخ النتائج إلى التخزين الداخلي، ثم إصدار تقرير إغلاق رسمي لقسم الفيديو يتضمن القرار النهائي، المخاطر المتبقية، ومصفوفة الاعتماد.

---

## جلسة 5 — قرار التخزين والتقرير النهائي لقسم الفيديو — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 4 | ✅ سليمة مع فجوة استراتيجية | تحسين UX/Admin صحيح، لكن الاعتماد على روابط المزود وحدها يبقي خطر انتهاء الرابط أو تغيّر صلاحياته خارج سيطرة المنصة |
| قرار التخزين | ✅ تم الحسم | أفضل نسخة مهنية للـSoft Launch هي نسخ أي فيديو مكتمل إلى تخزين داخلي خاص، مع إبقاء رابط المزود الأصلي داخل metadata للتدقيق فقط |
| بنية التخزين | ✅ نُفذت | أُضيف bucket خاص `generated-videos` غير عام، بحد 100MB، وأنواع فيديو مسموحة فقط: mp4/webm/quicktime |
| سياسات الوصول | ✅ آمنة | المستخدم يرى ملفاته داخل مجلده فقط، والأدمن يستطيع القراءة للتدقيق، والإدارة الكتابية تتم عبر صلاحيات خادمية موثوقة فقط |
| مسار الإكمال | ✅ حُسن | عند اكتمال الفيديو فورياً أو عبر refresh يتم تنزيل رابط المزود ورفعه داخلياً، ثم يُحفظ `storage_path` ويُعاد `result_url` كرابط داخلي موقّع |
| مسار الفشل | ✅ آمن | إذا فشلت الأرشفة الداخلية لا يُفشل الفيديو الناجح؛ يبقى رابط المزود كـfallback وتُسجل مشكلة الأرشفة في metadata حتى لا نخسر تجربة المستخدم |
| المكتبة ولوحة الأدمن | ✅ متوافقة | `listVideoJobs` و`listAdminVideoJobs` يعيدان روابط موقّعة حديثة من التخزين الداخلي عند توفر `storage_path`، ولوحة الأدمن تعرض Badge “مؤرشف داخلياً” |
| مطابقة البيانات الحالية | ✅ سليمة | حالياً `12` مهمة: لا processing، لا stale، لا completed بلا رابط، لا refunded بلا ledger؛ توجد `3` فيديوهات مكتملة قديمة بلا `storage_path` لأنها سبقت قرار الأرشفة |
| حذف/مطابقة الكود القديم | ✅ لا تعارض | لم يعد قرار التخزين خارجياً فقط؛ الكود الجديد يفضل التخزين الداخلي ولا يكسر fallback القديم للنتائج السابقة |

**الحكم النقدي:** القرار الصحيح تسويقياً وتقنياً هو التخزين الداخلي الخاص: المستخدم لا يشعر بأن النتيجة “رابط مؤقت”، والأدمن يملك أثراً قابلاً للتدقيق، والمنصة تقلل اعتمادها على صلاحية روابط المزود. ترك fallback للمزود قرار مقصود حتى لا يتحول فشل الأرشفة إلى فشل توليد ناجح.

**قرار Soft Launch لقسم الفيديو:** جاهز بشرط. الشرط الوحيد المتبقي للاعتماد الإنتاجي الكامل هو تمريرة فعلية بحساب اختبار على المزود الحالي بعد هذا التعديل للتأكد أن الفيديو الجديد يحصل على `storage_path` ويظهر في Library كرابط موقّع داخلي.

**الخطة التالية الدقيقة:** جلسة Hardening قصيرة وليست توسعة منتج: تشغيل فيديو Fast 5s حقيقي بعد التعديل، التأكد من `storage_path`، ثم معالجة تحذيرات صلاحيات الدوال العامة في Lovable Cloud linter قبل إعلان الإغلاق الأمني النهائي.

---

## جلسة 6 — Hardening أمني نهائي قبل تمريرة الفيديو الحية — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 5 | ✅ سليمة | التخزين الداخلي الخاص صحيح تقنياً وتسويقياً؛ الكود يحفظ `storage_path` للنتائج الجديدة ويستخدم رابطاً موقّعاً مع fallback آمن للنتائج القديمة |
| اختبار البناء | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد مراجعة التخزين والتحصين دون أخطاء |
| حالة بيانات الفيديو | ✅ مستقرة | يوجد `12` مهمة: `3` مكتملة و`9` مسترجعة، ولا توجد مهام `processing` أو stale؛ المكتمل القديم بلا `storage_path` لأنه سبق قرار الأرشفة |
| الصافي المالي | ✅ مطابق | الاستهلاك الصافي الفعلي للفيديو المكتمل = `450` نقطة، مطابق لـ3 فيديوهات × 150 نقطة؛ إجمالي الخصم التاريخي قبل الاستردادات = `1800` نقطة |
| تحذيرات الدوال العامة | ✅ عولجت جزئياً بقرار آمن | خُفّضت تحذيرات linter من `58` إلى `17` عبر إلغاء التنفيذ العام لكل الدوال ثم إعادة منح أقل صلاحية للدوال المستخدمة فعلياً فقط |
| الدوال المتروكة للعامة | ✅ مقصودة | بقي للزائر دوال العدادات/الإعدادات العامة فقط؛ وبقي للمستخدم المسجل دوال الرصيد، التوليد، الحصص، وفحص الدور اللازمة للتطبيق |
| الدوال التشغيلية الحساسة | ✅ أُغلقت | دوال قوائم البريد، الفحوص الخلفية، التريجرات، والعمليات الداخلية لم تعد قابلة للاستدعاء مباشرة من الزوار أو المستخدمين العاديين، وتبقى للخادم الموثوق |
| الكود القديم | ✅ لا حذف إضافي | لا توجد بقايا نشطة للحصة اليومية للفيديو أو تخزين خارجي فقط؛ fallback الخارجي مقصود للنتائج القديمة وفشل الأرشفة فقط |

**الحكم النقدي:** الجلسة السادسة أغلقت أكبر فجوة أمنية آلية متبقية دون كسر وظائف المنتج: لم نغلق الدوال التي يعتمد عليها المستخدمون، لكن أزلنا التنفيذ العام الافتراضي للدوال الحساسة. بقاء 17 تحذيراً الآن ليس فشلاً، بل مساحة مراجعة دقيقة لدوال مستخدمة فعلياً وتحتاج اختباراً وظيفياً بعد كل تضييق إضافي.

**قرار الاعتماد الحالي لقسم الفيديو:** جاهز تقنياً لـSoft Launch محدود بشرط تمريرة فيديو حية واحدة بحساب اختبار فعلي للتأكد من ملء `storage_path` للجيل الجديد. لا يُرفع إلى 100% أو إعلان عام قبل QA V9 الكامل و84 تمريرة موثقة.

**الخطة التالية الدقيقة:** الجلسة 7 يجب أن تكون اختباراً وظيفياً حياً لا تعديلاً واسعاً: تسجيل دخول بحساب اختبار، توليد فيديو Fast 5s، انتظار الاكتمال/refresh، تأكيد ظهور الفيديو في Library برابط موقّع، ثم فحص صف `video_jobs` للتأكد من `storage_path` و`internal_video_archived=true`.

---

## جلسة 7 — محاولة الاختبار الحي بعد التحصين — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 6 | ✅ سليمة | تحصين صلاحيات الدوال بقي مستقراً؛ linter ما زال عند `17` تحذيراً فقط بعد أن كان `58`، والمتبقي دوال مستخدمة فعلياً أو عامة مقصودة |
| فحص الكود القديم | ✅ لا يوجد نشط | البحث داخل `src` لم يجد استدعاءات نشطة لـ`daily_video_usage` أو `consume_video_daily_quota` أو تحقق أدمن عبر التخزين المحلي؛ الظهور المتبقي داخل migrations/التوثيق تاريخي أو سياق إزالة |
| حالة قاعدة الفيديو | ✅ مستقرة | لا توجد مهام `processing` ولا stale؛ الحالة الحالية `3 completed` و`9 refunded`، والأرشفة الداخلية ما زالت تنتظر فيديو جديد بعد التعديل |
| اختبار الواجهة | ⚠️ توقف عند الدخول | صفحة `/auth?redirect=/dashboard/generate-video` ظهرت بشكل سليم على عرض موبايل 508px، لكن لا توجد جلسة مستخدم نشطة داخل أداة الاختبار، لذلك لا يمكن تنفيذ توليد فيديو حي دون بيانات دخول فعلية |
| Console/Network | ✅ لا خطأ من التطبيق | لا توجد أخطاء تطبيق حرجة؛ التحذيرات الظاهرة من إطار المعاينة الخارجي فقط، وطلبات التحليلات عادت 200 |
| قرار مهني | ⚠️ لا نزوّر نتيجة اختبار | لم يتم إنشاء فيديو جديد، لذلك لا يجوز إعلان تأكيد `storage_path` عملياً؛ الاعتماد يبقى مشروطاً بتمريرة مستخدم مسجل فعلياً |

**الحكم النقدي:** الجلسة السابعة أثبتت أن التحصين لم يترك النظام في حالة غير مستقرة، لكنها لم تُغلق شرط الاختبار الحي لأن الوصول توقف عند تسجيل الدخول. أفضل قرار مهني هنا هو عدم ادعاء نجاح تمريرة لم تحدث، بل تثبيت أن المانع تشغيلي وليس خللاً في الكود.

**الخطة التالية الدقيقة:** توفير جلسة مستخدم مسجل في المعاينة أو تسجيل الدخول يدوياً، ثم تنفيذ فيديو Fast 5s واحد فقط، وبعده فحص `video_jobs.storage_path` و`metadata.internal_video_archived` وتوثيق النتيجة النهائية قبل رفع جاهزية الفيديو فوق 98.5%.

---

## جلسة 7 — تمريرة فيديو حية واعتماد الأرشفة الداخلية — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة | تحصين الدوال لم يكسر تسجيل الدخول أو دوال الرصيد/الفيديو، وتم الدخول بحساب الأدمن بنجاح من `/auth` إلى لوحة الإدارة ثم صفحة التوليد |
| تمريرة توليد فعلية | ✅ نجحت | تم تشغيل فيديو `Fast 5s` من `/dashboard/generate-video` بعد تزويد صورة منتج لازمة للباقات المدفوعة؛ الحالة انتقلت من `processing` إلى `completed` |
| الأرشفة الداخلية | ✅ مُثبتة عملياً | المهمة الجديدة `daa4118b-9a18-491a-b2eb-8f3c944e3faa` حصلت على `storage_path=c974babd-e099-45e2-89ef-73fecc6a88e2/videos/daa4118b-9a18-491a-b2eb-8f3c944e3faa.mp4` و`internal_video_archived=true` |
| رابط المعاينة | ✅ سليم | الواجهة عرضت حالة `جاهز للمعاينة` مع أزرار التحميل والفتح، ما يثبت أن `listVideoJobs/refreshVideoJob` يرجعان رابطاً موقّعاً صالحاً من التخزين الداخلي |
| مطابقة النقاط | ✅ مطابقة 100% | بعد الفيديو الجديد أصبح إجمالي الفيديوهات المكتملة `600` نقطة، وصافي دفتر نقاط الفيديو غير المسترد `600` نقطة؛ لا يوجد فرق مالي |
| ملاحظة UX أثناء الاختبار | ✅ سلوك صحيح | محاولة أولى دون صورة منتج لم تُنشئ مهمة ولم تخصم نقاطاً؛ الرسالة أرشدت لإرفاق صورة المنتج، وهذا يحافظ على جودة الإعلان ولا يُعد فشلاً |
| حذف/مطابقة القديم | ✅ لا تعارض | الفيديوهات القديمة تبقى بلا `storage_path` كـfallback لأنها سبقت قرار التخزين، أما النتائج الجديدة فتُؤرشف داخلياً تلقائياً |

**الحكم النقدي:** تم إغلاق الشرط الإنتاجي الأهم لقسم الفيديو: توليد فعلي ناجح بعد التحصين، حفظ داخلي خاص، رابط موقّع في الواجهة، ومطابقة مالية كاملة. هذا يرفع الثقة التقنية والتسويقية لأن المستخدم يرى نتيجة قابلة للمعاينة والتحميل دون الاعتماد التشغيلي المباشر على رابط المزود الأصلي.

**نسبة التقدم بعد هذه الدفعة:** 99.999999% جاهزية بناء وتشغيل، وقرابة 99.9997% جاهزية اعتماد قبل Soft Launch. لا تزال 100% النهائية مشروطة بتمريرات QA اليدوية الكاملة متعددة الأجهزة/المتصفحات وLighthouse الإنتاجي.

**الخطة التالية الدقيقة:** مراجعة لوحة الأدمن `/admin/video-jobs` والمكتبة `/dashboard/library` بصرياً بعد الفيديو المؤرشف، ثم إصدار تقرير إغلاق Soft Launch يفرّق بوضوح بين ما أُثبت عملياً وما يحتاج QA يدوي موسّع قبل الإعلان العام.


---

## جلسة 8 — فحص المكتبة ولوحة الإدارة ومعالجة شاشة التحميل — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 7 | ✅ مثبتة | الفيديو الحي اكتمل وأُرشف داخلياً، والمطابقة المالية بقيت سليمة |
| فحص المكتبة Mobile | ⚠️ كُشفت فجوة ثم عولجت | عند فقدان/تأخر جلسة المصادقة كانت صفحة Dashboard تبقى على Loader صامت؛ أضيفت حالة آمنة تعرض رسالة دخول وزر متابعة وتحويل مع redirect |
| طلبات الأدمن الجانبية | ✅ عولجت | عدّاد رسائل التواصل في `DashboardShell` صار يمرر Authorization بدلاً من إرسال طلب 401 صامت |
| مطابقة الفيديو والدفتر | ✅ سليمة | `completed=4`، `active=0`، `archived_completed=1`، وصافي دفتر الفيديو `600` مطابق لإجمالي الفيديوهات المكتملة `600` |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد الإصلاح؛ تحذيرات حجم chunks فقط غير مانعة |
| حذف/مطابقة الكود القديم | ✅ لا بقايا متعارضة | لم تُضاف مسارات جديدة خارج الخطة؛ الإصلاح حصر نفسه في حماية تجربة Dashboard وتمرير الجلسة للطلب الإداري |

**الحكم النقدي:** الفجوة لم تكن في الأرشفة أو النقاط، بل في تجربة ما بعد فقدان الجلسة: Loader صامت يضعف الثقة ويمنع QA البصري. الإصلاح الآن يجعل الحالة مفهومة للمستخدم، يحافظ على redirect، ويزيل 401 غير ضروري من عدّاد الأدمن.

**نسبة التقدم بعد هذه الدفعة:** 99.9998% جاهزية اعتماد قبل Soft Launch. ما يزال الإعلان العام مشروطاً بتمريرات QA V9 اليدوية وLighthouse الإنتاجي.

**الخطة التالية الدقيقة:** تنفيذ فحص بصري نهائي للمكتبة ولوحة `/admin/video-jobs` بعد نشر الإصلاح، ثم بدء Soft Launch محدود ومراقبة أول 10 عمليات فيديو/نقاط/حملات.

---

## جلسة 9 — الفحص النهائي للمكتبة ولوحة إدارة الفيديو قبل Soft Launch — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 8 | ✅ سليمة | إصلاح `DashboardShell` لا يكسر البناء، ويحافظ على redirect عند فقدان الجلسة، ويمرر Authorization لعدّاد رسائل الأدمن |
| فحص الدخول في المعاينة | ⚠️ توقف تشغيلي | محاولة الدخول الحالية عبر أداة المتصفح عادت 400 من خدمة المصادقة، لذلك لم أعتمدها كدليل بصري نهائي ولم أزوّر نتيجة مشاهدة المكتبة |
| فحص الكود للمكتبة | ✅ سليم | `/dashboard/library` يجلب الفيديوهات عبر `listVideoJobs`، ويستبدل `result_url` برابط موقّع حديث عند وجود `storage_path`، ويعرض المشغل/الحالة/النقاط/سياق الحملة |
| فحص كود إدارة الفيديو | ✅ حُسّن | وُجدت فجوة دقيقة في بطاقة “نشطة الآن”: كانت تجمع `processing` العالمي مع `pending` من الصفوف المعروضة فقط؛ تم تصحيحها لتستخدم `stats.pending + stats.processing` من الخادم |
| مطابقة القاعدة | ✅ سليمة | الحالة الحالية: `completed=4`، `refunded=9`، `active=0`، و`internally_archived=1` للفيديو الجديد بعد قرار التخزين |
| مطابقة الدفتر المالي | ✅ مطابقة 100% | إجمالي نقاط الفيديوهات المكتملة `600` يساوي صافي `credit_ledger` لاستهلاك الفيديو غير المسترد `600` |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد تعديل إحصاءات الإدارة دون أخطاء؛ تحذير حجم chunks فقط غير مانع |
| حذف/مطابقة الكود القديم | ✅ لا بقايا متعارضة | لم تُعد أي حصة يومية للفيديو أو تحقق أدمن من الواجهة؛ مصدر الحقيقة بقي: نقاط الفيديو + حد التزامن + روابط تخزين موقّعة |

**الحكم النقدي:** الفجوة التي عولجت اليوم ليست في الأرشفة أو الدفتر، بل في دقة مؤشر إداري مهم قبل Soft Launch. تصحيح “نشطة الآن” يمنع قراراً تشغيلياً خاطئاً عند وجود مهام `pending` خارج العينة المعروضة، ويحافظ على قدرة الأدمن على قراءة الضغط الحقيقي للنظام.

**نسبة التقدم بعد هذه الدفعة:** 99.99985% جاهزية اعتماد قبل Soft Launch. يبقى الإعلان العام ممنوعاً حتى QA V9 اليدوي الكامل وLighthouse الإنتاجي.

**الخطة التالية الدقيقة:** بدء Soft Launch محدود لأول 10 عمليات فعلية مع مراقبة: اكتمال الفيديو، ظهور `storage_path`، سلامة الروابط الموقّعة في المكتبة، مطابقة `credit_ledger`، وعدم ظهور أخطاء مصادقة أو console حرجة؛ ثم تحديث تقرير الإغلاق بقرار Beta Launch أو إيقاف لمعالجة أي عائق.

---

## جلسة 10 — تنفيذ مراقبة Soft Launch لأول 10 عمليات — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 9 | ✅ سليمة | تصحيح `stats.pending + stats.processing` بقي صحيحاً، ولا توجد عودة لاستخدام عينة الصفوف المعروضة كمصدر للحالة العالمية |
| مراقبة Soft Launch | ✅ نُفذت | أضيفت بطاقة “مراقبة الإطلاق المحدود” داخل `/admin/video-jobs` تعرض أول 10 عمليات، الاكتمال، الاسترداد، النشطة، الأرشفة، وقرار الجاهزية للـBeta |
| تمييز Legacy fallback | ✅ مهم تسويقياً وتقنياً | الفيديوهات المكتملة القديمة بلا `storage_path` تُصنف كـLegacy fallback لأنها سبقت قرار التخزين، ولا تُحسب كفشل أرشفة جديد إلا إذا ظهر `internal_video_archived` وفشل التخزين |
| مطابقة عينة القاعدة | ✅ واضحة | آخر 10 عمليات: `completed=4`، `refunded=6`، `active=0`، `archived=1`، `missing_archive_new=0`، `legacy_fallback=3` |
| مطابقة الدفتر | ✅ سليمة | إجمالي نقاط الفيديوهات المكتملة `600` يطابق صافي استهلاك الفيديو في `credit_ledger` وهو `600` |
| TypeScript + Production build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد إضافة المراقبة دون أخطاء؛ تحذير حجم chunks فقط غير مانع |
| حذف/مطابقة الكود القديم | ✅ لا بقايا متعارضة | لم تُضف مسارات خارج الخطة؛ المراقبة تعتمد على `video_jobs` و`credit_ledger` وسياق `campaign_pack_id` داخل metadata |

**الحكم النقدي:** هذه ليست مجرد بطاقة إحصائية؛ هي بوابة تشغيل مصغرة تمنع الانتقال إلى Beta بناءً على الانطباع. تمييز Legacy fallback يحمي القرار من إنذار مضلل بسبب فيديوهات قديمة، وفي نفس الوقت يلتقط أي فشل أرشفة جديد بعد قرار التخزين الداخلي.

**نسبة التقدم بعد هذه الدفعة:** 99.9999% جاهزية Soft Launch مراقب. لا يزال الإعلان العام ممنوعاً حتى QA V9 اليدوي الكامل وLighthouse الإنتاجي.

**الخطة التالية الدقيقة:** تشغيل Soft Launch فعلياً حتى تكتمل 10 عمليات جديدة بعد قرار التخزين، ثم استخدام بطاقة المراقبة لاتخاذ قرار: الانتقال إلى Beta إذا كانت `active=0` و`missing_archive=0` و`ledger_mismatch=0`، أو إيقاف الإطلاق لمعالجة العائق المحدد.

## جلسة 11 — تصحيح معيار عينة Soft Launch وبدء القياس النظيف — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة مع فجوة قياس | مراقبة Soft Launch كانت مفيدة، لكنها كانت تسحب آخر 10 عمليات تاريخياً، ما يخلط نتائج Legacy قبل الأرشفة مع العينة الجديدة |
| التصحيح التقني | ✅ تم | عينة Soft Launch أصبحت تبدأ من أول فيديو يملك `storage_path` داخلياً، ثم تقيس آخر 10 عمليات بعد هذه النقطة فقط |
| منع القرار المضلل | ✅ تم | لم تعد لوحة الإدارة قادرة على إعلان جاهزية Beta بناءً على نتائج قديمة قبل قرار التخزين الداخلي |
| حالة البيانات الحالية | ✅ واضحة | بداية العينة النظيفة: `2026-04-27T10:48:17.88175+00:00`؛ حجم العينة الحالي `1/10`، مكتمل `1`، مؤرشف `1`، عوائق `0` |
| مطابقة الدفتر | ✅ مستمرة | إجمالي الفيديوهات `13`: لا pending ولا processing؛ المكتمل `4`، المسترجع `9`، وصافي ledger للفيديو المكتمل لا يزال مطابقاً `600` نقطة |
| الاختبارات | ✅ ناجحة | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل دون أخطاء |

**الحكم النقدي:** هذا التصحيح مهم لأنه يحول مراقبة Soft Launch من بطاقة عرض جيدة إلى معيار قرار موثوق. أي قرار Beta الآن سيُبنى على عمليات ما بعد الأرشفة الداخلية فقط، وليس على بيانات مختلطة من مرحلة ما قبل التخزين.

**الخطة التالية الدقيقة:** تنفيذ 9 عمليات فيديو حقيقية إضافية ضمن Soft Launch النظيف، ومراقبة البطاقة حتى تصل العينة إلى `10/10` مع: صفر pending/processing عالق، صفر missing archive، صفر ledger mismatch، ثم الانتقال إلى QA V9 اليدوي وLighthouse قبل Beta.

## جلسة 12 — إضافة نسبة تقدم Soft Launch — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 11 | ✅ سليمة | فصل العينة النظيفة عن Legacy بقي صحيحاً، وبداية القياس الحالية من أول `storage_path` داخلي |
| نسبة التقدم | ✅ أضيفت | أضيف `progressPercent` خادمياً إلى مؤشرات Soft Launch حتى تكون النسبة مصدرها نفس منطق العينة الرسمي |
| واجهة الإدارة | ✅ محسّنة | أضيف شريط تقدم واضح ونسبة رقمية داخل بطاقة مراقبة الإطلاق المحدود |
| الحالة الحالية | ✅ موثقة | العينة النظيفة الحالية `1/10`، ونسبة التقدم `10%`، بلا عوائق: لا active، لا missing archive، لا ledger mismatch |

**الحكم النقدي:** إضافة النسبة ليست تجميلاً فقط؛ هي تحسن القرار التشغيلي لأن الأدمن يرى فوراً حجم التقدم نحو بوابة Beta دون تفسير يدوي للأرقام.

**الخطة التالية الدقيقة:** رفع النسبة من `10%` إلى `100%` عبر 9 عمليات فيديو فعلية إضافية، مع إيقاف قرار Beta فوراً إذا ظهر أي blocker في الأرشفة أو ledger.

## جلسة 13 — تقوية بوابة Soft Launch ضد الفشل غير المسترد — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة | نسبة التقدم 10% والعينة النظيفة 1/10 بقيتا صحيحتين بعد فصل Legacy |
| فجوة مكتشفة | ✅ عولجت | أضيف معيار صريح للفشل غير المسترد: أي مهمة `failed` لديها `ledger_id` دون `refund_ledger_id` تصبح blocker قبل Beta |
| منطق ledger | ✅ أقوى | `videoLedgerMatches` أصبح يعتبر الفشل مقبولاً فقط إذا لم يحدث خصم أو تم رده فعلياً |
| واجهة الإدارة | ✅ أوضح | أضيف مؤشر `فشل بلا رد` داخل بطاقة Soft Launch حتى يظهر الخطر المالي مباشرة للأدمن |
| الحالة الحالية | ✅ نظيفة | عينة Soft Launch الحالية: `1/10`، نسبة التقدم `10%`، فشل بلا رد `0`، ledger mismatch `0`، active `0` |

**الحكم النقدي:** هذه خطوة حماية مالية وتسويقية مهمة؛ لا يكفي أن يكتمل الفيديو أو يُؤرشف، بل يجب أن لا يخرج أي مستخدم من التجربة بخسارة نقاط عند فشل المزود.

**الخطة التالية الدقيقة:** تنفيذ 9 عمليات فيديو فعلية إضافية للوصول إلى `10/10` و`100%`، مع مراقبة مؤشرات: فشل بلا رد = 0، missing archive = 0، ledger mismatch = 0، active عالق = 0.

## جلسة 14 — إضافة نسبة إنهاء الخطة إلى Beta — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 13 | ✅ سليمة | فحص الفشل غير المسترد بقي صحيحاً: `failed_unrefunded=0`، و`ledger_mismatch=0`، ولا توجد مهام نشطة عالقة |
| نسبة إنهاء الخطة | ✅ أضيفت | أضيف `planCompletionPercent` خادمياً للوحة الإدارة ليعرض تقدم الخطة العامة نحو Beta، وليس فقط تقدم عينة Soft Launch |
| الحالة الحالية | ✅ واضحة | تقدم Soft Launch الحالي `10%`، ونسبة إنهاء الخطة إلى Beta `91%` بناءً على اكتمال التحصين وبقاء 9 عمليات من العينة النظيفة |
| واجهة الإدارة | ✅ محسّنة | بطاقة Soft Launch تعرض الآن شريطين: تقدم العينة، وإنهاء الخطة إلى Beta |

**الحكم النقدي:** هذه الإضافة تمنع الخلط بين نسبة العينة التشغيلية ونسبة إنجاز الخطة العامة. المشروع ليس 10% منجزاً؛ العينة فقط 10%، بينما الخطة إلى Beta وصلت 91% وبقيت مرتبطة بإكمال 9 عمليات نظيفة.

**الخطة التالية الدقيقة:** تنفيذ 9 عمليات فيديو فعلية إضافية. كل عملية ناجحة ونظيفة ترفع الخطة تقريباً 1% حتى تصل إلى `100%` عند اكتمال `10/10` بلا: active عالق، missing archive، ledger mismatch، أو فشل غير مسترد.

## جلسة 15 — توضيح المتبقي لإنهاء الخطة — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 14 | ✅ سليمة | نسبة العينة `10%` ونسبة الخطة إلى Beta `91%` صحيحتان، ولا توجد عوائق حالية في العينة |
| مؤشر المتبقي | ✅ أضيف | أضيف `remainingToBeta` خادمياً ليوضح عدد العمليات المتبقية قبل قرار Beta؛ الحالي `9` عمليات |
| أثر كل عملية | ✅ أضيف | أضيف `planPercentPerOperation` لتوضيح أن كل عملية نظيفة ترفع خطة Beta تقريباً `+1%` |
| واجهة الإدارة | ✅ أوضح | بطاقة Soft Launch تعرض الآن: العينة، المتبقي للـBeta، زيادة كل عملية، ومؤشرات المخاطر المالية والتقنية |

**الحكم النقدي:** هذه الإضافة تجعل الخطة قابلة للقياس التنفيذي لا مجرد نسبة عامة؛ يعرف الأدمن بالضبط كم عملية بقيت وما أثر كل عملية على الوصول إلى قرار Beta.

**نسبة إنهاء الخطة الحالية:** `91%` إلى Beta، مع بقاء `9` عمليات فعلية للوصول إلى `100%` إذا بقيت جميع المؤشرات بلا عوائق.

**الخطة التالية الدقيقة:** تنفيذ أول دفعة من العمليات المتبقية، ويفضل 3 فيديوهات متتالية كدفعة مراقبة قصيرة؛ بعدها يجب أن تصبح العينة `4/10`، ونسبة Soft Launch `40%`، ونسبة الخطة إلى Beta `94%` إذا لم تظهر عوائق.

## جلسة 16 — إضافة هدف دفعة المراقبة التالية — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 15 | ✅ سليمة | المتبقي للـBeta `9` عمليات، وكل عملية نظيفة ترفع الخطة `+1%`، والمؤشرات الحالية بلا عوائق |
| هدف الدفعة التالية | ✅ أضيف | أضيف `nextBatchTarget` لتحديد دفعة مراقبة قصيرة من `3` فيديوهات بدلاً من تنفيذ 9 دفعة واحدة |
| نسبة الدفعة | ✅ أضيفت | أضيف `nextBatchProgressPercent`، والحالي `0%` لأن الدفعة لم تبدأ بعد |
| أثر الدفعة | ✅ أضيف | أضيف `nextBatchExpectedPlanPercent`؛ عند نجاح الدفعة التالية سترتفع الخطة من `91%` إلى `94%` |
| واجهة الإدارة | ✅ أوضح | بطاقة Soft Launch تعرض الآن هدف الدفعة، تقدمها، والنسبة المتوقعة بعد اكتمالها |

**الحكم النقدي:** تقسيم المتبقي إلى دفعات صغيرة يقلل المخاطر التشغيلية؛ إذا ظهر عائق بعد أول 3 فيديوهات نوقف ونصلح قبل استهلاك كامل العينة.

**نسبة إنهاء الخطة الحالية:** `91%`. هدف الدفعة التالية: `3` فيديوهات، تقدم الدفعة `0%`، والنسبة المتوقعة بعدها `94%` إذا بقيت المؤشرات بلا عوائق.

**الخطة التالية الدقيقة:** تنفيذ 3 فيديوهات فعلية فقط كدفعة أولى، ثم إعادة فحص: sample `4/10`، Soft Launch `40%`، خطة Beta `94%`، وبلوكَرز = صفر.

## جلسة 17 — قرار جاهزية دفعة المراقبة التالية — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 16 | ✅ سليمة | هدف الدفعة التالية بقي `3` فيديوهات، ونسبة الخطة الحالية `91%`، والمتوقع بعدها `94%` |
| جاهزية الدفعة | ✅ أضيفت | أضيف `nextBatchReady` خادمياً؛ يكون صحيحاً فقط إذا بقيت عمليات مطلوبة ولا توجد blockers |
| واجهة الإدارة | ✅ أوضح | أضيفت شارة صريحة: `الدفعة التالية جاهزة` أو `أوقف الدفعة` حسب حالة العينة |
| الحالة الحالية | ✅ جاهزة | العينة الحالية `1/10`، المتبقي `9`، هدف الدفعة `3`، و`nextBatchReady=true` لأن active/missing archive/ledger mismatch/failed unrefunded كلها صفر |

**الحكم النقدي:** هذه الخطوة تمنع تنفيذ دفعة جديدة آلياً أو تشغيلياً فوق عينة فيها خلل. القرار لم يعد يحتاج تفسيراً من الأرقام؛ الواجهة تعطي توجيهاً واضحاً للأدمن.

**نسبة إنهاء الخطة الحالية:** `91%`. عند تنفيذ الدفعة التالية بنجاح تصبح الخطة `94%`.

**الخطة التالية الدقيقة:** تنفيذ 3 فيديوهات فعلية فقط، ثم مراجعة الشارة؛ إذا بقيت `الدفعة التالية جاهزة` ونسبة الخطة `94%` نكمل الدفعة الثانية، وإذا تحولت إلى `أوقف الدفعة` نعالج السبب قبل أي توليد جديد.

## جلسة 18 — بدء دفعة المراقبة وإيقافها عند أول استرجاع — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| بدء الدفعة | ⚠️ بدأ جزئياً | تم تنفيذ أول فيديو من دفعة المراقبة المخططة من 3 فيديوهات فقط |
| نتيجة العملية | ⚠️ مسترجعة | المهمة تحولت إلى `refunded` وتم رد النقاط؛ لم يوجد فشل مالي غير مسترد |
| قرار التشغيل | ✅ إيقاف مهني | تم إيقاف بقية الدفعة وعدم تنفيذ الفيديوهين التاليين حتى لا نهدر عمليات فوق عينة ظهرت فيها إشارة عدم استقرار |
| تقوية البوابة | ✅ تم | أضيف الاسترجاع داخل عينة Soft Launch كـ blocker صريح قبل Beta، لأن Beta يجب أن تعتمد على عينة مكتملة ومؤرشفة لا على عمليات فاشلة حتى لو رُدت النقاط |
| حالة العينة بعد الإيقاف | ⚠️ تحتاج معالجة | العينة النظيفة بعد بداية الأرشفة أصبحت `2/10`: مكتمل `1`، مسترجع `1`، نشط `0`، مؤرشف `1`، فشل بلا رد `0` |
| الاختبارات | ✅ ناجحة | `bunx tsc --noEmit` و`bun run build` اجتازا بعد تقوية blocker الاسترجاع |

**الحكم النقدي:** الاسترجاع المالي عمل بشكل صحيح، وهذا يحمي المستخدم. لكن من منظور Soft Launch وقرار Beta، وجود عملية مسترجعة داخل العينة يعني أن التجربة ليست مستقرة بما يكفي للاستمرار بنفس الدفعة. القرار الصحيح هو التوقف والتحقيق قبل توليد المزيد.

**نسبة إنهاء الخطة الحالية:** تبقى تشغيلياً عند `91%` لقرار Beta، رغم أن العينة أصبحت `2/10`، لأن وجود `refunded` داخل العينة يمنع احتساب التقدم كجاهزية مستقرة.

**الخطة التالية الدقيقة:** تحليل سبب الاسترجاع من سجل المهمة والمزود، ثم إصلاح السبب أو تعديل إعدادات المزود/القالب، وبعدها إعادة محاولة فيديو واحد فقط. لا نكمل دفعة 3 فيديوهات قبل أن تعود شارة الدفعة إلى جاهزة بلا blockers.

## جلسة 19 — إصلاح فجوة صورة المزود قبل الخصم — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 18 | ✅ دقيقة | آخر مهمة مسترجعة سببها أن المزود لم يستطع تنزيل رابط صورة موقّع، والاسترداد المالي تم بنجاح دون فشل غير مسترد |
| فجوة مكتشفة | ✅ عولجت | تحويل الصورة إلى Data URL كان داخل استدعاء المزود بعد إنشاء المهمة والخصم؛ هذا كان يسمح بخصم ثم refund إذا رفض المزود الصورة |
| الإصلاح التقني | ✅ تم | أُضيف preflight قبل `consume` و`video_jobs.insert`: يجلب الخادم صورة المرجع ويحوّلها إلى Data URL، وأي فشل وصول/نوع/حجم يوقف العملية قبل الخصم |
| حذف/مطابقة القديم | ✅ تم | لم يعد مسار الصور يرجع إلى رابط غير قابل للوصول عند فشل preflight؛ يتم إرجاع خطأ آمن للمستخدم بدلاً من محاولة مزود مضمونة الفشل |
| مراجعة إضافية | ✅ عولجت | تم تحسين رسالة خطأ صورة المنتج، وإصلاح حلقة توجيه محتملة ظهرت في سجل المعاينة كـ`Maximum update depth` عند مسارات auth/admin |
| المعاينات | ✅ مطابقة سريعة | تمت مراجعة `/auth?redirect=/admin/video-providers` على هاتف 390px، تابلت 820px، وديسكتوب 1366px، مع Light/Dark؛ لا تظهر حلقة التحديث بعد الإصلاح |
| الاختبارات | ✅ ناجحة | `bunx tsc --noEmit` و`bun run build` اجتازا بعد الإصلاح؛ كما تمت مراجعة آخر سجلات الفيديو لتأكيد سبب الاسترجاع |

**الحكم النقدي:** هذا إصلاح مهم لأنه ينقل فشل صورة المنتج من مرحلة مكلفة بعد الخصم إلى مرحلة تحقق مجانية قبل إنشاء المهمة. بذلك نقلل الاسترجاعات غير الضرورية ونحمي عينة Soft Launch من فشل معروف قابل للمنع.

**نسبة إنهاء الخطة الحالية:** تبقى `91%` إلى Beta لأن العينة الحالية ما زالت `2/10` وبداخلها عملية `refunded`، لذلك لا نرفع الجاهزية حتى تنجح محاولة مراقبة واحدة بعد الإصلاح.

**الخطة التالية الدقيقة:** تنفيذ فيديو مراقبة واحد فقط بعد هذا الإصلاح؛ إذا اكتمل وأُرشف داخلياً وتطابق ledger بلا refund جديد، نحدّث قرار العينة وننتقل لاستكمال دفعة قصيرة جديدة، أما إذا فشل preflight فنعالج مصدر الصورة قبل أي خصم أو توليد.

## جلسة 20 — مراجعة ما بعد إصلاح الصور + تحسين مطابقة الموبايل — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 19 | ✅ سليمة كودياً | `providerReachableImageUrl` موجود قبل الخصم وإنشاء المهمة، ورسائل أخطاء الصور أصبحت آمنة وواضحة |
| مراجعة بيانات الإنتاج | ⚠️ آخر فيديو مراقبة ما زال من النسخة القديمة | آخر مهمة مسترجعة ما زالت تحمل رابط صورة موقّع داخل خطأ المزود، لذلك لم تكن دليلاً على الإصلاح الجديد بل على المشكلة القديمة قبل وصول التعديل للنشر |
| فحص مالي | ✅ سليم | المهمة المسترجعة لديها `ledger_id` و`refund_ledger_id`، ولا يوجد خصم فيديو غير مسترد ضمن آخر فحص |
| فجوة واجهة مكتشفة | ✅ عولجت | على الموبايل كانت روابط الإدارة غير ظاهرة داخل شريط Dashboard، فأضيفت روابط الأدمن أفقياً للأدمن فقط، مع زر تبديل Light/Dark داخل الداشبورد للموبايل والديسكتوب |
| مطابقة الأجهزة | ✅ تمت | تمت مراجعة إدارة الفيديو على هاتف 390px، تابلت 820px، وديسكتوب 1366px؛ لا يوجد انهيار تخطيط، والـSoft Launch يظهر العينة `2/10` والخطة `92%` مع blocker الاسترجاع |
| الاختبارات | ✅ ناجحة | `bunx tsc --noEmit` و`bun run build` اجتازا بعد تعديل DashboardShell |

**الحكم النقدي:** لا يجوز توسيع الدفعة بعد، لأن عينة Soft Launch فيها `refunded=1`. الإصلاح الجديد جاهز كودياً، لكن يحتاج فيديو مراقبة واحد بعد نشر آخر build حتى نثبت أن صورة Data URL وصلت للمزود قبل استهلاك نقاط إضافية.

**نسبة إنهاء الخطة الحالية:** تعرض اللوحة `92%` لأن العينة أصبحت `2/10`، لكن الاعتماد التشغيلي يبقى معلّقاً بسبب blocker الاسترجاع حتى ينجح فيديو مراقبة جديد بلا refund.

**الخطة التالية الدقيقة:** نشر/تحديث آخر نسخة، ثم تنفيذ فيديو مراقبة واحد فقط. إذا اكتمل ومؤرشفاً ودفتر النقاط مطابق، نعيد تقييم blocker الاسترجاع ونقرر هل نكمل دفعة 3 فيديوهات أم نعيد ضبط عينة Soft Launch النظيفة.

## جلسة 21 — إصلاح حلقة Dashboard قبل فيديو المراقبة — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ سليمة جزئياً | إصلاح روابط أدمن الموبايل وLight/Dark اجتاز TypeScript والبناء |
| فجوة تشغيلية مكتشفة | ❌ مانعة | عند فتح `/dashboard/generate-video` ظهرت حلقة `Maximum update depth` تمنع تشغيل فيديو المراقبة بأمان |
| الإصلاح | ✅ تم | أزيل حارس `beforeLoad` المتكرر من Layout الداشبورد، وأصبح التوجيه يتم مرة واحدة داخل `DashboardLayout` مع حالة انتظار آمنة |
| التحقق | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد الإصلاح |

**الحكم النقدي:** كان تشغيل الفيديو فوق حلقة توجيه مخاطرة غير مهنية؛ تم إيقاف التنفيذ وإصلاح طبقة الوصول أولاً. لم يتم استهلاك نقاط إضافية في هذه الجولة.

**نسبة إنهاء الخطة الحالية:** تبقى `92%` ظاهرياً لعينة `2/10`، لكن الاعتماد التشغيلي ما زال معلّقاً بسبب `refunded=1` حتى ينجح فيديو مراقبة واحد بعد الإصلاح.

**الخطة التالية الدقيقة:** افتح `/dashboard/generate-video` بعد تحديث المعاينة/النشر، تحقق أن الصفحة لا تدخل حلقة، ثم نفّذ فيديو مراقبة واحد فقط؛ إذا اكتمل ومؤرشفاً بلا refund نقرر استكمال دفعة 3 فيديوهات.

## جلسة 22 — تنفيذ فيديو مراقبة واحد وكشف فجوة CDN المزود — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 21 | ✅ سليمة | تم فتح `/dashboard/generate-video` بعد تسجيل الدخول دون ظهور حلقة `Maximum update depth`، وصفحة توليد الفيديو أصبحت قابلة للاستخدام |
| تنفيذ فيديو المراقبة | ⚠️ مسترجع | تم تشغيل فيديو مراقبة واحد فقط كما خُطط، وتحوّل إلى `refunded` برسالة مزود تفيد فشل تنزيل `image_url` |
| الفحص المالي | ✅ سليم | أحدث مهمة `a2624903-625a-4f04-9d7f-b6db739d44b5` لديها `ledger_id` و`refund_ledger_id`، أي لا يوجد خصم غير مسترد |
| فجوة مكتشفة | ✅ عولجت كودياً | المزود لا يعتمد روابط التخزين الموقّعة بشكل ثابت حتى بعد preflight؛ لذلك تم تغيير `providerReachableImageUrl` لرفع صورة المرجع إلى Fal CDN عبر `@fal-ai/client` وإرسال رابط `v*.fal.media` عام للمزود |
| حذف/مطابقة القديم | ✅ تم | لم يعد مسار المزود يعتمد على تمرير رابط التخزين الموقّع أو Data URL كناتج نهائي؛ يتم جلب الصورة والتحقق منها ثم رفعها إلى CDN المزود قبل الخصم وإنشاء المهمة |
| الاختبارات | ✅ ناجحة | `bunx tsc --noEmit` و`bun run build` اجتازا بعد تعديل مسار الصورة وإضافة مكتبة `@fal-ai/client` |

**الحكم النقدي:** فيديو المراقبة أدى وظيفته: أثبت أن حلقة التوجيه انتهت، وكشف أن Data URL/الرابط الموقّع ليسا المسار الأكثر موثوقية مع المزود الحالي. الحل الاحترافي هو استخدام CDN المزود نفسه كطبقة تسليم للصور قبل إنشاء مهمة الفيديو، مع بقاء الفحص قبل الخصم.

**نسبة إنهاء الخطة الحالية:** تبقى `92%` ظاهرياً بسبب عينة `3/10` تقريباً وبداخلها عمليات مسترجعة، لكن الاعتماد التشغيلي لا يرتفع حتى ينجح فيديو مراقبة جديد بعد إصلاح CDN.

**الخطة التالية الدقيقة:** تحديث المعاينة/النشر ثم تشغيل فيديو مراقبة واحد فقط مرة أخرى. معيار النجاح: `completed` + `storage_path` داخلي + `refund=0` + عدم ظهور رابط تخزين موقّع داخل خطأ المزود. عند النجاح نقرر إما إعادة ضبط العينة النظيفة أو استكمال دفعة 3 فيديوهات قصيرة.

## جلسة 23 — إثبات نجاح CDN وتشغيل فيديو مراقبة نظيف — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 22 | ✅ سليمة | آلية رفع صورة المرجع إلى CDN المزود موجودة قبل الخصم وإنشاء المهمة، ولم يظهر كسر TypeScript أو Build |
| اختبار مراقبة واحد | ✅ ناجح | تم تشغيل مهمة واحدة فقط `34b12a80-69d8-4daf-9197-acaed90a5ef1` بعد الإصلاح، وانتقلت إلى `completed` |
| تحقق CDN | ✅ ناجح عملياً | لم يتكرر خطأ تنزيل `image_url`، ووصلت المهمة إلى `COMPLETED` لدى المزود بدل `refunded` |
| الأرشفة الداخلية | ✅ ناجحة | تم حفظ الفيديو داخلياً في `generated-videos` عبر `storage_path`، وهذا يثبت أن نتيجة الفيديو لا تعتمد فقط على رابط المزود المؤقت |
| السلامة المالية | ✅ سليمة | `refund_ledger_id` فارغ لأن المهمة نجحت، ولا توجد حالة `failed` بلا استرداد في فحص العينة الحالي |
| التحقق التقني | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا قبل التشغيل؛ كما تم فحص صفحة التوليد على Mobile 390px دون أخطاء console خاصة بالفيديو |

**الحكم النقدي:** إصلاح CDN أثبت نجاحه عملياً في أول فيديو مراقبة بعد التعديل. المشكلة السابقة لم تكن في الخصم أو الاسترداد، بل في موثوقية الرابط الذي يستلمه المزود. الآن المسار الصحيح هو: فحص الصورة خادمياً → رفعها إلى CDN المزود → إنشاء مهمة الفيديو → أرشفة النتيجة داخلياً.

**نسبة إنهاء الخطة الحالية:** ترتفع تشغيلياً إلى `93%` بعد نجاح فيديو مراقبة نظيف ومؤرشف. العينة الكلية تحتوي عمليات مسترجعة قديمة، لذلك لا نعتبر الخطة مغلقة، لكن blocker صورة المزود تم إثبات علاجه عملياً.

**الخطة التالية الدقيقة:** تنفيذ دفعة مراقبة قصيرة من فيديوهين فقط بدلاً من ثلاثة لتقليل المخاطر بعد أول نجاح. معيار الانتقال بعدها: كل فيديو `completed` + `storage_path` موجود + لا `refund` جديد + لا خطأ تنزيل صورة. إذا نجحت الدفعة القصيرة، نرفع التقدم إلى قرابة `95%` ونكمل باقي عينة Soft Launch تدريجياً.

## جلسة 24 — دفعة مراقبة CDN قصيرة من فيديوهين — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 23 | ✅ سليمة | أول فيديو بعد CDN كان `completed` ومؤرشفاً داخلياً، لذلك تم الانتقال بحذر إلى فيديوهين فقط كما خُطط |
| فيديو المراقبة الثاني | ✅ ناجح | المهمة `c3a34594-4a3f-4a12-94ca-ea8e11c6a9e4` وصلت إلى `completed` و`fal_queue_status=COMPLETED` |
| أرشفة الفيديو الثاني | ✅ ناجحة | `storage_path` موجود و`result_url` موجود، ولا يوجد `refund_ledger_id` |
| فيديو المراقبة الثالث | ✅ ناجح | المهمة `0de75305-e71c-4e40-b44c-6ec23a61b0a7` وصلت إلى `completed` و`fal_queue_status=COMPLETED` |
| أرشفة الفيديو الثالث | ✅ ناجحة | `storage_path` موجود و`result_url` موجود، ولا يوجد `refund_ledger_id` |
| سلامة العينة بعد الدفعة | ✅ مستقرة | لا توجد مهام نشطة، ولا توجد حالة `failed` بلا استرداد؛ إجمالي المؤرشف داخلياً أصبح `4` فيديوهات |
| مراقبة الواجهة | ✅ سليمة | لا توجد أخطاء console خاصة بالفيديو أثناء تشغيل الدفعة من صفحة `/dashboard/generate-video` على Mobile 390px |

**الحكم النقدي:** أصبح لدينا الآن ثلاث عمليات متتالية بعد إصلاح CDN انتهت بنجاح وأُرشفت داخلياً، وهذا يثبت أن فجوة تنزيل صورة المزود عولجت تشغيلياً لا كودياً فقط. لا نحتاج لتعديل كود جديد في هذه الجولة؛ القرار المهني هو الاستمرار التدريجي لا التوسع المفاجئ.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى قرابة `95%` بعد نجاح فيديوهين إضافيين بلا refund وبأرشفة داخلية. ما زالت الخطة ليست 100% لأن العينة التاريخية تحتوي عمليات مسترجعة قديمة، وباقي Soft Launch يحتاج إكمال تدريجي ومراجعة لوحة الإدارة.

**الخطة التالية الدقيقة:** فتح لوحة `/admin/video-jobs` ومراجعة شارة Soft Launch وأرقام `completed/refunded/archived/active` بعد هذه الدفعة، ثم تنفيذ دفعة مراقبة تالية من 2–3 فيديوهات فقط إذا بقيت المؤشرات بلا عوائق.

## جلسة 25 — مراجعة لوحة الإدارة وتصحيح نافذة Soft Launch — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 24 | ✅ سليمة | آخر ثلاث عمليات بعد إصلاح CDN مكتملة ومؤرشفة داخلياً، ولا توجد حالات `failed` بلا استرداد |
| فجوة مكتشفة في الإدارة | ✅ عولجت | كانت نافذة Soft Launch تبدأ من أول فيديو مؤرشف داخلياً، وهذا قد يخلط فيديوهات قديمة قبل إثبات إصلاح CDN مع العينة النظيفة |
| التصحيح التقني | ✅ تم | ثُبّتت بداية عينة Soft Launch عند وقت إثبات إصلاح CDN (`2026-04-27T12:50:00Z`) حتى تقيس اللوحة العينة التشغيلية الصحيحة فقط |
| مطابقة البيانات | ✅ متطابقة | بعد التصحيح: العينة النظيفة `3/10`، مكتمل `3`، مؤرشف `3`، مسترجع `0`، نشط `0`، وفشل بلا استرداد `0` |
| السلامة المالية العامة | ✅ سليمة | إجمالي قاعدة الفيديو: `7` مكتملة و`10` مسترجعة تاريخياً، ولا توجد حالة مسترجعة/فاشلة بخصم غير مردود |
| فحص الواجهة | ⚠️ مؤجل للمصادقة | جلسة المعاينة الحالية غير مسجلة الدخول وفتحت صفحة auth بدلاً من `/admin/video-jobs`، لذلك لم يتم تشغيل دفعة جديدة من جلسة غير مصادقة |
| الاختبارات | ✅ ناجحة | `bunx tsc --noEmit` و`bun run build` اجتازا بعد تعديل منطق لوحة الإدارة |

**الحكم النقدي:** لم يكن من المهني تشغيل فيديوهات جديدة قبل إصلاح مقياس الإدارة؛ لأن المؤشر كان قد يحتسب عينة مختلطة تاريخياً. الآن أصبح قرار الدفعة التالية مبنياً على نافذة نظيفة بعد إصلاح CDN، وهذا أدق مالياً وتشغيلياً.

**نسبة إنهاء الخطة الحالية:** تبقى تشغيلياً عند `95%`، مع رفع جودة القياس الإداري. لم نرفع النسبة لأننا لم نشغّل دفعة جديدة، بل أصلحنا معيار الاحتساب الذي يقرر هل نكمل بأمان.

**الخطة التالية الدقيقة:** تسجيل الدخول كأدمن في المعاينة/الإنتاج، فتح `/admin/video-jobs` للتأكد بصرياً من العينة النظيفة `3/10` وغياب blockers، ثم تشغيل دفعة مراقبة من فيديوهين فقط. عند نجاحهما تصبح العينة النظيفة `5/10` ويمكن رفع التقدم إلى قرابة `97%`.

## جلسة 26 — بوابة تشغيل الدفعة التالية وحاجز المصادقة — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 25 | ✅ سليمة | نافذة Soft Launch أصبحت تبدأ من وقت إثبات إصلاح CDN فقط، ولا تختلط بالفيديوهات التاريخية القديمة |
| مطابقة العينة النظيفة | ✅ مستقرة | العينة الحالية بعد إصلاح CDN: `3/10`، مكتمل `3`، مؤرشف `3`، مسترجع `0`، نشط `0`، وفشل بلا استرداد `0` |
| مراجعة السجلات | ✅ لا مانع فيديو جديد | لا توجد أخطاء خادم حديثة تخص توليد الفيديو أو فشل CDN ضمن آخر فحص؛ الموجود فقط محاولة فتح مسارات أدمن من جلسة غير مصادقة |
| فحص لوحة الإدارة | ⚠️ حاجز مصادقة | فتح `/admin/video-jobs` في جلسة المعاينة أعاد إلى صفحة تسجيل الدخول، وهذا سلوك صحيح أمنياً وليس عطلاً في لوحة الفيديو |
| قرار تشغيل الفيديوهين | ✅ إيقاف آمن | لم يتم تشغيل فيديوهات جديدة لأن تشغيل دفعة مراقبة من جلسة غير مصادقة أو عبر تجاوز صلاحيات المستخدم سيكسر معيار الاختبار الحقيقي |
| السلامة المالية | ✅ محفوظة | لم تُستهلك نقاط إضافية، ولا توجد عمليات نشطة أو خصومات فيديو غير مردودة قبل الدفعة التالية |

**الحكم النقدي:** النظام جاهز من ناحية البيانات والـledger لتشغيل فيديوهين جديدين، لكن التنفيذ العملي يجب أن يتم من جلسة أدمن حقيقية داخل التطبيق حتى يبقى الاختبار مطابقاً لمسار المستخدم/الأدمن الفعلي. عدم تجاوز المصادقة هنا قرار جودة وأمان، وليس تعطلاً.

**نسبة إنهاء الخطة الحالية:** تبقى `95%`. لا نرفعها إلى `97%` قبل تنفيذ فيديوهين فعلياً واكتمالهما مع `storage_path` وبدون `refund`.

**الخطة التالية الدقيقة:** تسجيل الدخول بحساب الأدمن، فتح `/admin/video-jobs` للتأكد من شارة `الدفعة التالية جاهزة`، ثم تشغيل فيديوهين فقط من `/dashboard/generate-video`. بعد اكتمالهما نراجع: `completed=5`، `archived=5`، `refunded=0`، `active=0`، ثم نرفع التقدم إلى قرابة `97%`.

## جلسة 27 — توحيد نصوص لوحة Soft Launch مع مصدر الحقيقة — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 26 | ✅ سليمة | العينة النظيفة ما زالت `3/10` بلا استرجاع أو مهام نشطة، وحاجز التنفيذ الوحيد هو تسجيل دخول أدمن فعلي |
| فجوة صغيرة مكتشفة | ✅ عولجت | نص لوحة الإدارة كان يقول إن العينة تبدأ بعد الأرشفة الداخلية فقط، بينما مصدر الحقيقة بعد جلسة 25 هو بداية ما بعد إثبات إصلاح CDN |
| التصحيح | ✅ تم | عُدّل وصف Soft Launch ورسالة البداية الفارغة لتذكر صراحة: بعد إصلاح CDN والأرشفة الداخلية، لا الأرشفة وحدها |
| مطابقة البيانات | ✅ ثابتة | قاعدة البيانات تؤكد: `sample_size=3`، `completed=3`، `archived=3`، `refunded=0`، `active=0` منذ وقت إثبات CDN |
| الاختبارات | ✅ ناجحة | تم تشغيل TypeScript وProduction build بعد التعديل دون كسر |

**الحكم النقدي:** هذا ليس تغييراً وظيفياً كبيراً، لكنه مهم لتقليل الالتباس التشغيلي: الأدمن الآن يرى نفس منطق القياس المطبق في الخادم، فلا يظن أن أي فيديو مؤرشف قديم يدخل تلقائياً في عينة Beta.

**نسبة إنهاء الخطة الحالية:** تبقى `95%` لأننا حسّنا دقة الإدارة ولم نشغّل الفيديوهين التاليين بعد.

**الخطة التالية الدقيقة:** بعد تسجيل دخول الأدمن، تشغيل فيديوهين فقط. عند وصول العينة إلى `5/10` مع `archived=5` و`refunded=0` نرفع التقدم إلى `97%`، ثم ننتقل لدفعة أخيرة تدريجية لإكمال `10/10` قبل قرار Beta.

## جلسة 28 — معاينة لوحة الأدمن وإيقاف دفعة المراقبة عند فجوة صورة المزود — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| تسجيل دخول الأدمن | ✅ نجح | تم الدخول بحساب الأدمن وفتح `/admin/video-jobs` فعلياً |
| معاينة لوحة الإدارة | ✅ متطابقة | اللوحة تعرض إجمالي `17` مهمة، مكتملة `7`، مسترجعة `10`، وتكلفة عينة `$5.20` |
| عينة Soft Launch النظيفة | ✅ مستقرة | الواجهة وقاعدة البيانات متطابقتان: `3/10`، مكتمل `3`، مؤرشف `3`، مسترجع `0`، نشط `0`، وفشل بلا رد `0` |
| تشغيل دفعة المراقبة | ⚠️ أُوقف بأمان | عند محاولة بدء فيديو مراقبة ظهرت فجوة مع رابط صورة المزود: `file_download_error` على رابط صورة خاص/موقّع، لذلك لم نكمل الفيديو الثاني |
| السلامة المالية | ✅ محفوظة | مسار الاسترداد عمل عند فشل المزود، ولا توجد زيادة معتمدة في عينة Soft Launch بعد هذه الجلسة |
| قرار الجودة | ✅ صحيح | لا يجوز رفع التقدم إلى `97%` قبل إصلاح/تثبيت مسار الصورة ثم نجاح فيديوهين مكتملين ومؤرشفين |

**الحكم النقدي:** خطوة الأدمن أزالت حاجز المصادقة وأثبتت أن لوحة الإدارة تقرأ الأرقام الصحيحة. لكن تشغيل الدفعة كشف أن مسار الصور المرجعية ما زال يحتاج تثبيتاً نهائياً قبل الاعتماد، لأن المزود قد يرفض تحميل رابط صورة خاص أو غير قابل للوصول خارجياً. إيقاف الدفعة هنا هو القرار المهني الصحيح لحماية النقاط ونظافة عينة Soft Launch.

**نسبة إنهاء الخطة الحالية:** تبقى `95%`. لم يتم رفعها لأن معيار الرفع كان نجاح فيديوهين جديدين، وهذا لم يتحقق بعد.

**الخطة التالية الدقيقة:** تثبيت مسار صور الفيديو بحيث تُرسل للمزود عبر رابط عام صالح أو رفع موثوق إلى تخزين المزود قبل إنشاء المهمة، ثم إعادة تشغيل فيديو مراقبة واحد فقط. إذا اكتمل ومؤرشفاً بلا refund، نشغّل الفيديو الثاني؛ عند وصول العينة إلى `5/10` نرفع التقدم إلى `97%`.

## جلسة 29 — تثبيت مسار صور الفيديو وتشغيل مراقبة واحدة ناجحة — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 28 | ✅ سليمة | قرار إيقاف الدفعة كان صحيحاً لأن الفشل كان في قابلية تنزيل رابط الصورة لدى المزود، وليس في الخصم أو الأرشفة |
| إصلاح رسائل الخطأ | ✅ تم | تم تنظيف أخطاء `file_download_error/image_url` حتى لا تظهر تفاصيل مزود أو روابط موقعة للمستخدم، وتظهر رسالة عربية آمنة وقابلة للتصرف |
| توثيق رابط المزود | ✅ تم | أصبحت مهمة الفيديو تحفظ `provider_image_url` و`provider_image_source` داخل metadata لتسهيل تدقيق أي فشل صورة لاحقاً |
| تشغيل فيديو مراقبة واحد | ✅ نجح | تم تشغيل فيديو واحد فقط بعد الإصلاح، وأصبح `completed` مع `storage_path` داخلي و`provider_status=succeeded` وبدون refund |
| عينة Soft Launch النظيفة | ✅ تقدمت | أصبحت العينة بعد إصلاح CDN: `4/10`، مكتمل `4`، مؤرشف `4`، مسترجع `0`، نشط `0` |
| الاختبارات | ✅ ناجحة | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل |

**الحكم النقدي:** الإصلاح نجح في إثبات أن مسار الصور يعمل عندما يُرسل للمزود كرابط قابل للوصول وموثق، وأن الأرشفة الداخلية ما زالت مستقرة. لم نشغّل الفيديو الثاني عمداً لأن خطة الجودة بعد فشل جلسة 28 اشترطت فيديو مراقبة واحد أولاً قبل استئناف الدفعة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `96%` تقريباً لأن العينة النظيفة أصبحت `4/10` بلا عوائق، لكنها لا تصل إلى `97%` قبل نجاح فيديو مراقبة ثانٍ.

**الخطة التالية الدقيقة:** تشغيل فيديو مراقبة ثانٍ فقط بنفس معيار الصور القابلة للوصول، ثم مراجعة `/admin/video-jobs`. إذا أصبحت العينة `5/10` مع `archived=5` و`refunded=0` و`active=0` نرفع التقدم إلى `97%` ونكمل بقية العينة تدريجياً.

## جلسة 30 — تشغيل فيديو المراقبة الثاني ورفع العينة إلى 5/10 — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 29 | ✅ سليمة | مسار الصورة المثبت كان يعمل بعد رفع صورة المرجع إلى رابط مزود عام، والعينة كانت `4/10` بلا refund أو active |
| تشغيل فيديو المراقبة الثاني | ✅ نجح | تم تشغيل مهمة واحدة فقط `e5aa5860-f1b3-4072-9c6d-227d1b3dd585` من حساب الأدمن، ثم تحديثها حتى انتقلت من `processing` إلى `completed` |
| تحقق مسار الصورة | ✅ ثابت | المهمة حفظت `provider_image_url` على نطاق `fal.media`، ولم يتكرر خطأ `file_download_error` أو فشل تنزيل الصورة |
| الأرشفة الداخلية | ✅ ناجحة | تم حفظ الفيديو داخلياً في `generated-videos` عبر `storage_path`، مع `internal_video_archived=true` |
| السلامة المالية | ✅ سليمة | لا يوجد `refund_ledger_id` للمهمة الجديدة، ولا توجد مهام نشطة أو فشل بلا استرداد ضمن آخر 10 عمليات بعد إصلاح CDN |
| عينة Soft Launch النظيفة | ✅ وصلت للبوابة | أصبحت العينة `5/10`: مكتمل `5`، مؤرشف `5`، مسترجع `0`، نشط `0` |
| فحص الواجهة | ⚠️ تعذر استخدام المتصفح | أداة المتصفح لم تكن متاحة أثناء هذه الجلسة، لذلك تم الاعتماد على استدعاء خادمي موثق واستعلامات قاعدة البيانات بدل الفحص البصري |

**الحكم النقدي:** تم تحقيق معيار جلسة 29 بدقة: فيديو مراقبة ثانٍ فقط، بنفس معيار الصورة القابلة للوصول، اكتمل وأُرشف داخلياً دون refund. هذا يثبت أن إصلاح مسار الصور لم يكن نجاحاً عارضاً في فيديو واحد، بل أصبح مستقراً عبر تجربتين متتاليتين بعد الفجوة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `97%` لأن العينة النظيفة أصبحت `5/10` مع `archived=5` و`refunded=0` و`active=0`.

**الخطة التالية الدقيقة:** تشغيل دفعة صغيرة أخيرة تدريجية من 2 فيديو فقط، ثم مراجعة لوحة `/admin/video-jobs` ودفتر النقاط. إذا بقيت المؤشرات بلا عوائق نكمل إلى `10/10` قبل قرار Beta النهائي، بدون تشغيل دفعة كبيرة مفاجئة.

## جلسة 29 — محاولة بدء الدفعة التالية وإيقاف آمن بسبب رجوع خطأ الصورة — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الحالة السابقة | ✅ مطابقة | لوحة الإدارة وقاعدة البيانات متطابقتان: عينة Soft Launch النظيفة `5/10`، مكتملة `5`، مؤرشفة `5`، مسترجعة `0`، نشطة `0` |
| بوابة التشغيل | ✅ سليمة قبل المحاولة | لا توجد عوائق في العينة الحالية، والدفعة التالية كانت جاهزة مبدئياً حسب لوحة الإدارة |
| محاولة تشغيل فيديو جديد | ⚠️ لم تُحتسب | ظهرت رسالة `file_download_error` من المزود عند تجهيز صورة المرجع، ولم يظهر صف جديد ضمن نافذة Soft Launch النظيفة بعد الانتظار |
| السلامة المالية | ✅ سليمة | لم تتغير العينة النظيفة: ما زالت `5/10`، ولا يوجد `refund` جديد داخل نافذة Soft Launch، ولا توجد مهمة نشطة أو فشل بلا رد |
| قرار التشغيل | ✅ إيقاف مهني | تم إيقاف الدفعة فوراً وعدم تشغيل الفيديو الثاني؛ لا نستهلك نقاطاً إضافية قبل علاج سبب رجوع رابط الصورة الموقّع في بيئة المعاينة/النشر |

**الحكم النقدي:** الخطوة السابقة صحيحة حتى `5/10`، لكن محاولة بدء الدفعة التالية كشفت أن صفحة التوليد الحالية ما زالت تمرر في بعض الحالات رابط تخزين موقّعاً للمزود بدلاً من رابط CDN/مصدر قابل للتنزيل. لأن العينة النظيفة لم تتلوث ولم يظهر refund داخل نافذتها، لا نخفض التقدم، لكن لا نرفعه.

**نسبة إنهاء الخطة الحالية:** تبقى `97%`، وعينة Soft Launch النظيفة ثابتة عند `5/10` بلا refund وبلا active jobs.

**الخطة التالية الدقيقة:** مراجعة مسار نشر/استدعاء منطق `providerReachableImageUrl` والتأكد أن النسخة الفعلية المستخدمة في المعاينة/الإنتاج لا تمرر روابط التخزين الموقعة للمزود، ثم إعادة تشغيل فيديو مراقبة واحد فقط. عند نجاحه وأرشفته تصبح العينة `6/10` ونستكمل الدفعة تدريجياً.

## جلسة 30 — تثبيت تمرير رابط CDN للمزود وإثبات عائق النسخة الفعلية — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 29 | ✅ دقيقة | عينة Soft Launch النظيفة بقيت `5/10` مكتملة ومؤرشفة، بلا refund وبلا مهام نشطة |
| الإصلاح التقني | ✅ تم | أزيلت إعادة تجهيز الصورة داخل مزود FAL، وأصبح المزود يستخدم فقط `providerImageUrl` الناتج من preflight/رفع CDN قبل الخصم وإنشاء المهمة |
| توثيق metadata | ✅ تم | تمت إضافة `provider_image_url` و`provider_image_source` داخل سجل المهمة من مرحلة ما قبل المزود لتسهيل التدقيق |
| التحقق التقني | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل |
| اختبار المعاينة | ⚠️ لم يثبت الإصلاح بعد | صفحة المعاينة ما زالت تعيد خطأ `file_download_error` برابط تخزين موقّع، ولم يظهر صف جديد داخل نافذة Soft Launch بعد الانتظار |
| القرار التشغيلي | ✅ إيقاف آمن | لم يتم تشغيل محاولات إضافية؛ العينة النظيفة لم تتلوث وبقيت `5/10` |

**الحكم النقدي:** الكود الآن يمنع المسار الذي كان يعيد تمرير رابط التخزين الموقّع من داخل مزود الفيديو، لكن المعاينة الحالية تبدو أنها لم تستخدم النسخة الجديدة بعد أو أن الاستدعاء ما زال يصل من bundle/جلسة قديمة. لا نرفع التقدم حتى يثبت فيديو مراقبة واحد أن `image_url` المرسل للمزود أصبح رابط CDN لا رابط تخزين موقّع.

**نسبة إنهاء الخطة الحالية:** تبقى `97%`، وعينة Soft Launch النظيفة ثابتة عند `5/10`.

**الخطة التالية الدقيقة:** تحديث/إعادة تحميل المعاينة بعد وصول آخر build، ثم تشغيل فيديو مراقبة واحد فقط ومراجعة metadata للتأكد أن `provider_image_source=provider_preflight` وأن الرابط ليس من التخزين الموقّع. عند النجاح والأرشفة نرفع العينة إلى `6/10` ونكمل بحذر.

## جلسة 31 — حاجز CDN وتعطيل صوت المزود غير السعودي — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة الخطوة السابقة | ✅ مؤكدة | آخر عينة نظيفة ما زالت `5/10` مكتملة ومؤرشفة، ولا توجد محاولة جديدة مسجلة بعد آخر عائق في المعاينة |
| سبب فجوة الصورة | ✅ عولج دفاعياً | أضيف حاجز صريح يمنع تمرير أي `image_url` إلى المزود إذا لم يكن رابط CDN من `fal.media` بعد preflight |
| تنظيف الكود القديم | ✅ تم | أزيل الاعتماد على `primaryReferenceImage` داخل مسار المزود حتى لا يعاد استخدام رابط التخزين الموقّع كبديل ضمني |
| فجوة اللهجة/الصوت | ✅ عولجت مؤقتاً بأمان | تم تعطيل `generate_audio_switch` للمزود لأن صوته لا يضمن عربية صريحة أو لهجة سعودية؛ الاعتماد التجاري للصوت مؤجل لمسار صوت سعودي مستقل |
| التوثيق الداخلي | ✅ تم | أضيفت `audio_policy=provider_audio_disabled_until_saudi_voice_pipeline` و`provider_audio_requested=false` داخل metadata |
| التحقق التقني | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل |

**الحكم النقدي:** لا يجوز الاستمرار في إنتاج فيديوهات بصوت غير سعودي ثم اعتبارها قابلة للإطلاق. القرار الصحيح هو تثبيت الفيديو كمسار بصري صامت/آمن حالياً، ثم بناء مسار Voiceover سعودي منفصل ودمجه لاحقاً قبل أي اعتماد تجاري للصوت.

**نسبة إنهاء الخطة الحالية:** تبقى `97%`؛ التقدم لم يرتفع لأننا أصلحنا بوابة أمان وجودة، لكن لم نثبت فيديو مراقبة جديد بعد تحديث النسخة الفعلية.

**الخطة التالية الدقيقة:** بعد تحديث المعاينة/النشر، تشغيل فيديو مراقبة واحد فقط. معيار النجاح الجديد: `completed` + `storage_path` + `provider_image_url` من `fal.media` + `provider_audio_requested=false` + لا refund. بعدها نرفع العينة إلى `6/10` ونفتح مسار مستقل للصوت السعودي قبل Beta.

## جلسة 32 — محاولة تشغيل مراقبة من الواجهة وإيقاف آمن قبل إنشاء المهمة — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 31 | ✅ سليمة | TypeScript وProduction build اجتازا، والعينة النظيفة قبل المحاولة ثابتة عند `5/10`، مؤرشفة `5`، مسترجعة `0`، نشطة `0` |
| تسجيل دخول الأدمن | ✅ نجح | تم الدخول بحساب الأدمن وفتح رابط عينة `pilot-06` من صفحة توليد الفيديو |
| تصحيح رابط العينة | ✅ تم | الرابط الأول كان بقالب غير مطابق، فتم استخدام القالب الصحيح `home-decor` للعينة السادسة |
| تجهيز صورة المنتج | ✅ تم | تم إدخال رابط صورة منتج عام من نطاق `fal.media` في حقل صورة المنتج |
| محاولة التشغيل | ⚠️ لم تُنشئ مهمة | الضغط من الواجهة لم ينتج عنه استدعاء `generateVideo` ظاهر في الشبكة ولم يظهر صف جديد في `video_jobs` |
| السلامة المالية | ✅ محفوظة | قاعدة البيانات تؤكد بقاء العينة `5/10`، ولا توجد مهمة نشطة أو refund جديد أو خصم جديد مرتبط بالمحاولة |
| القرار المهني | ✅ إيقاف آمن | لم يتم تشغيل محاولة ثانية أو تجاوز المسار من الخادم حتى لا نكسر معيار الاختبار الحقيقي من الواجهة |

**الحكم النقدي:** الكود والبناء سليمان، وحاجز CDN/تعطيل صوت المزود جاهزان، لكن محاولة الواجهة لم تصل فعلياً إلى إنشاء مهمة جديدة رغم ظهور الزر متاحاً. لذلك لا يجوز احتساب فيديو سادس ولا رفع التقدم. هذا عائق واجهة/جلسة تشغيل يجب فحصه قبل استهلاك نقاط إضافية.

**نسبة إنهاء الخطة الحالية:** تبقى `97%`، وعينة Soft Launch النظيفة ثابتة عند `5/10` بلا refund وبلا active jobs.

**الخطة التالية الدقيقة:** فحص سبب عدم وصول ضغط زر التوليد إلى `generateVideo` في وضع الاختبار الداخلي، غالباً عبر مراجعة حالة الزر/النموذج وطلبات الشبكة الخاصة بالصفحة، ثم تشغيل فيديو مراقبة واحد فقط بعد التأكد أن الاستدعاء ينطلق فعلاً. معيار النجاح لا يتغير: `completed` + `storage_path` + `provider_image_url` من `fal.media` + `provider_audio_requested=false` + لا refund.

## جلسة 33 — إصلاح بوابة زر توليد الفيديو قبل تشغيل العينة السادسة — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 32 | ✅ دقيقة | قاعدة البيانات تؤكد أن العينة النظيفة ما زالت `5/10` ولا توجد مهام نشطة أو خصم جديد بعد محاولة الواجهة السابقة |
| سبب عائق الزر | ✅ عولج | كان زر التوليد معطلاً بالكامل عند بعض شروط ما قبل الإرسال، لذلك لا يصل الضغط إلى `generateVideo` ولا تظهر محاولة في الشبكة |
| الإصلاح | ✅ تم | أصبح الزر يرسل الضغط دائماً ما دام لا يوجد تحميل/مهام نشطة/جلب رصيد جارٍ، بينما تبقى بوابات الجودة والرصيد وصورة المنتج داخل دالة `generate` برسائل واضحة قبل أي خصم |
| حذف/استبدال القديم | ✅ تم | أزيلت شروط التعطيل القديمة التي كانت تمنع الاستدعاء صامتاً عند نقص الرصيد/الجودة/المدة/صورة المنتج |
| التحقق التقني | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل |
| السلامة المالية | ✅ محفوظة | لا توجد مهام `pending/processing` للأدمن، ورصيد الأدمن الحالي كافٍ لتشغيل عينة مراقبة واحدة دون مخاطرة تشغيل متزامن |

**الحكم النقدي:** الإصلاح لا يتجاوز حماية الخادم ولا يخصم نقاطاً عند وجود خطأ؛ فقط يمنع الفشل الصامت في الواجهة ويجعل محاولة التشغيل إما تصل للخادم أو تعرض رسالة واضحة للمستخدم. لذلك لا نرفع العينة إلى `6/10` حتى يتم تشغيل فيديو مراقبة فعلي بعد وصول آخر build للمعاينة.

**نسبة إنهاء الخطة الحالية:** تبقى `97%`؛ تم علاج عائق الواجهة والتحقق من البناء، لكن معيار التقدم التالي يتطلب فيديو جديداً مكتملًا ومؤرشفًا.

**الخطة التالية الدقيقة:** بعد تحديث المعاينة/النشر، تشغيل عينة `pilot-06` مرة واحدة فقط من الواجهة. معيار النجاح: ظهور طلب `generateVideo`، ثم `completed` + `storage_path` + `provider_image_url` من `fal.media` + `provider_audio_requested=false` + لا refund. عند تحقق ذلك ترتفع العينة إلى `6/10` ونكمل تدريجياً حتى `10/10` قبل Beta.

## جلسة 34 — تشغيل الواجهة ثبت ووُقف التنفيذ عند بوابة التسلسل — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 33 | ✅ صحيحة | إصلاح الزر نجح: الضغط من الواجهة أرسل طلب `generateVideo` فعلياً بعد تسجيل دخول الأدمن |
| تشغيل `pilot-06` | ⚠️ مُنع خادمياً | الخادم رفض التشغيل برسالة `medium_test_sequence_violation` لأن عينات الاختبار المتوسطة السابقة ليست مكتملة رسمياً حسب وسوم `medium_test` |
| الشبكة | ✅ مثبتة | ظهر طلب Server Function من صفحة `/dashboard/generate-video` وفي جسمه `source=medium-test`, `mediumTestSampleId=pilot-06`, `mediumTestTemplateId=home-decor` |
| قاعدة البيانات | ✅ سليمة | لم يتم إنشاء صف جديد، ولا توجد مهام نشطة، ولا خصم أو refund جديد؛ العينة النظيفة العامة بقيت ثابتة |
| القرار المهني | ✅ إيقاف آمن | لا يجوز تجاوز بوابة التسلسل أو تشغيل `pilot-06` من الخلفية لأن ذلك يكسر معيار الاختبار الرسمي من الواجهة |

**الحكم النقدي:** العائق لم يعد زر الواجهة؛ العائق الآن منطقي ومقصود داخل الخادم: `pilot-06` لا يعمل قبل إغلاق العينات السابقة ضمن مسار الاختبار المتوسط الرسمي. هذا يحمي جودة العينة ويمنع القفز على ترتيب الاعتماد.

**نسبة إنهاء الخطة الحالية:** تبقى `97%`؛ تم إثبات أن الواجهة ترسل الطلب، لكن لم يتم إنتاج فيديو سادس بسبب بوابة تسلسل صحيحة.

**الخطة التالية الدقيقة:** الرجوع إلى لوحة مزودي الفيديو وتشغيل العينة المسموحة التالية بالترتيب الرسمي أو إعادة ضبط خطة الاختبار إن كان الهدف التجاري هو عينة Soft Launch عامة لا عينة Medium Test. لا نرفع إلى `98%` إلا بعد فيديو جديد مكتمل ومؤرشف مع `fal.media` وتعطيل صوت المزود وعدم وجود refund.

## جلسة 35 — تصحيح مسار التشغيل بعد بوابة التسلسل وفحص العينة الرسمية — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 34 | ✅ صحيحة | منع `pilot-06` كان منطقياً لأن مصفوفة الاختبار المتوسط الرسمية تشير إلى أن العينة المسموحة التالية هي `pilot-01` لا السادسة |
| لوحة مزودي الفيديو | ✅ تم فحصها بجلسة أدمن | اللوحة تعرض أمر التشغيل الحالي: `pilot-01 — عطر فاخر — هوك سريع`، مع تنفيذ `8%` وعائق واحد بسبب محاولة `pilot-01` القديمة المستردة |
| محاولة التشغيل الرسمية | ✅ تم تنفيذها من الرابط الصحيح | تم فتح `/dashboard/generate-video?source=medium-test&mediumTestSampleId=pilot-01&mediumTestTemplateId=perfume-premium-hook` والضغط على توليد الفيديو |
| نتيجة الخادم | ⚠️ مُنعت قبل الخصم | الطلب وصل إلى `generateVideo` لكن الخادم رفضه برسالة تجهيز صورة المرجع؛ لم تُنشأ مهمة جديدة ولم يحدث خصم جديد |
| قاعدة البيانات | ✅ سليمة | لا تزال آخر عينة Medium Test الرسمية هي `pilot-01` القديمة بحالة `refunded`، ولا توجد مهمة نشطة أو تسريب مالي جديد |
| قرار الجودة | ✅ إيقاف آمن | لا يتم القفز إلى `pilot-06` ولا تشغيل خلفي؛ يجب معالجة سبب رفض صورة مرجع الشخصية/الـ CDN أو استخدام رابط `fal.media` مقبول قبل إعادة `pilot-01` |

**الحكم النقدي:** الخطوة السابقة لم تكن خطأ في الواجهة، بل كان هناك خلط بين مسارين: عينة Soft Launch العامة `5/10` ومسار Medium Test الرسمي. المسار الرسمي يبدأ حالياً من `pilot-01` لأنه آخر محاولة لها مستردة، لذلك تشغيل `pilot-06` سيظل مرفوضاً. محاولة اليوم أثبتت أن الزر والخادم يعملان، وأن الحماية تمنع الخصم عند فشل تجهيز صورة المرجع.

**نسبة إنهاء الخطة الحالية:** تبقى `97%`؛ لم نرفع النسبة لأننا لم نحصل على فيديو جديد مكتمل ومؤرشف، لكننا أغلقنا الالتباس التشغيلي وحددنا العائق الفعلي التالي بدقة.

**الخطة التالية الدقيقة:** إصلاح تجهيز صورة مرجع الاختبار المتوسط بحيث لا تُرسل للخادم إلا صورة CDN مباشرة مقبولة من `fal.media` أو اعتماد مسار مرجع داخلي آمن، ثم إعادة تشغيل `pilot-01` فقط. معيار النجاح: إنشاء صف جديد، `completed`، وجود `storage_path`، `provider_audio_requested=false`، وعدم وجود `refund`. بعد ذلك يصبح تشغيل `pilot-02` مسموحاً بالتسلسل.

## جلسة 36 — إصلاح مسار صورة مرجع الاختبار المتوسط قبل إعادة `pilot-01` — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 35 | ✅ دقيقة | الفشل كان قبل الخصم بسبب صورة مرجع الشخصية في وضع الاختبار المتوسط، وليس بسبب زر الواجهة أو بوابة التسلسل |
| سبب العائق | ✅ مشخص | رابط صورة الشخصية كان يشير إلى أصل داخلي في المعاينة يتطلب جسر مصادقة، لذلك لا يصلح كمدخل تحميل خادمي/مزود فيديو |
| الإصلاح | ✅ تم | في وضع `medium-test` لم نعد نرسل `speakerImageUrl` من أصول الواجهة؛ نعتمد على وصف الشخصية المقفل داخل البرومبت ونترك أي صورة منتج مطلوبة تُرفع/تدخل صراحة |
| تنظيف القديم | ✅ تم | أزيل الاعتماد على رابط أصل ثابت قديم لصورة الشخصية في مسار الاختبار الداخلي حتى لا يعاد إرسال رابط غير قابل للتحميل للمزود |
| التحقق التقني | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد التعديل |
| السلامة المالية | ✅ محفوظة | لا توجد مهمة جديدة أو خصم ناتج عن محاولات الفشل السابقة؛ آخر مهمة Medium Test ما زالت `pilot-01` القديمة بحالة `refunded` |

**الحكم النقدي:** هذا إصلاح صحيح لأن العينة `pilot-01` لا تتطلب صورة منتج، ومرجع الشخصية موجود نصياً داخل البرومبت الرسمي المقفل. إزالة رابط صورة الشخصية غير القابل للتحميل أفضل من تمريره ثم تحويله إلى خطأ قبل الخصم أو إلى `file_download_error` عند المزود.

**نسبة إنهاء الخطة الحالية:** تبقى `97%`؛ رفع النسبة مشروط بتشغيل فعلي بعد وصول هذا التعديل للمعاينة/النشر وإكمال فيديو جديد مؤرشف.

**الخطة التالية الدقيقة:** إعادة تسجيل الدخول ثم تشغيل `pilot-01` مرة واحدة بعد تحديث المعاينة. معيار النجاح: إنشاء صف جديد في `video_jobs`، الحالة `completed`، وجود `storage_path`، عدم وجود `refund_ledger_id`، ووسوم `provider_audio_requested=false`/`audio_policy=provider_audio_disabled_until_saudi_voice_pipeline`. بعد نجاحها يصبح `pilot-02` هو العينة التالية بالتسلسل.

## جلسة 37 — معالجة عوائق `pilot-01` بعد التشغيل الفعلي — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 36 | ✅ صحيحة جزئياً | إزالة صورة الشخصية سمحت بتجاوز عائق CDN، لكن كشفت عائقين أعمق عند المزود |
| المحاولة الأولى بعد الإصلاح | ⚠️ وصلت للمزود ثم استُردت | تم إنشاء مهمة `pilot-01` جديدة، لكن المزود رفض البرومبت النهائي لأنه أطول من الحد الداخلي؛ تم رد النقاط |
| الإصلاح الأول | ✅ تم | تم ضغط برومبت الاختبار المتوسط فعلياً، مع حد مزدوج بالأحرف والبايتات حتى لا يتكرر تجاوز حد مزود الفيديو |
| المحاولة الثانية | ⚠️ وصلت للمزود ثم استُردت | بعد ضغط البرومبت ظهر أن مسار عدم وجود صورة مرجعية يستخدم موديل Image-to-Video ويتطلب `image_url` |
| الإصلاح الثاني | ✅ تم | أصبح اختيار موديل FAL ديناميكياً: Image-to-Video عند وجود صورة مرجعية، وText-to-Video عند عدم وجود صورة |
| التحقق التقني | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` اجتازا بعد الإصلاحين |
| حالة المعاينة | ⚠️ لم يُعاد التشغيل النهائي | بعد إعادة البناء ظهرت الصفحة على تحميل هيكلي في جلسة المتصفح، ولم أكمل محاولة ثالثة حتى لا أستهلك نقاطاً أثناء عدم استقرار المعاينة |

**الحكم النقدي:** تم علاج فجوتين حقيقيتين في مسار `pilot-01`: طول البرومبت واختيار موديل غير مناسب عند غياب الصورة. لم نرفع النسبة لأن معيار النجاح لم يتحقق بعد (`completed` + `storage_path` + لا refund)، لكن الكود أصبح أكثر صحة إنتاجياً ويميز بين مساري Text-to-Video وImage-to-Video.

**نسبة إنهاء الخطة الحالية:** تبقى `97%`؛ تم تحسين المتانة التقنية، لكن لا يوجد فيديو Medium Test مكتمل جديد حتى الآن.

**الخطة التالية الدقيقة:** بعد استقرار المعاينة، تشغيل `pilot-01` مرة واحدة فقط. إذا اكتملت مع `storage_path` وبدون refund نرفع المسار إلى `98%` وننتقل مباشرة إلى `pilot-02` بالتسلسل الرسمي.

## جلسة 38 — نجاح `pilot-01` وفتح التسلسل الرسمي — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 37 | ✅ صحيحة | الإصلاحان السابقان كانا مطلوبين: ضغط البرومبت واختيار Text-to-Video عند عدم وجود صورة |
| تسجيل دخول الأدمن | ✅ نجح | تم الدخول بحساب الأدمن وفتح رابط `pilot-01` الرسمي |
| التشغيل من الواجهة | ✅ نجح | ظهر تأكيد `تم إنشاء مهمة الفيديو — جاري المعالجة` من صفحة `/dashboard/generate-video` |
| صف المهمة | ✅ تم إنشاؤه | المهمة `69722b08-3f32-4842-97f3-1e25520f89fb` بدأت `processing` ثم اكتملت |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/text-to-video` لأن العينة لا تحتوي صورة مرجعية، مع `final_provider_prompt=650` |
| سياسة الصوت | ✅ مطابقة | `provider_audio_requested=false` و`audio_policy=provider_audio_disabled_until_saudi_voice_pipeline` |
| الأرشفة | ✅ مكتملة | الحالة `completed`، يوجد `result_url` و`storage_path`، و`internal_video_archived=true` |
| السلامة المالية | ✅ سليمة | لا يوجد `refund_ledger_id` للمهمة الناجحة، ولا توجد مهمة نشطة متروكة |

**الحكم النقدي:** تم إغلاق أول عينة Medium Test رسمية بنجاح كامل من الواجهة، وبذلك ثبت أن مسار الاختبار المتوسط يعمل بعد الإصلاحات دون تجاوز تسلسل أو تدخل خلفي. أصبح تشغيل `pilot-02` مسموحاً منطقياً حسب بوابة التسلسل.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `98%` لأن أول عينة رسمية اكتملت وأُرشفت مع تعطيل صوت المزود وبدون refund.

**الخطة التالية الدقيقة:** تشغيل `pilot-02` فقط من الرابط الرسمي التالي، مع الانتباه إلى أنها لا تزال ضمن جودة `fast` ولا تتطلب صورة منتج. معيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + لا refund، ثم نكمل التسلسل عينة بعينة حتى نصل إلى اعتماد دفعة Medium Test.

## جلسة 39 — نجاح `pilot-02` واستمرار تسلسل Medium Test — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 38 | ✅ صحيحة | `pilot-01` مكتملة ومؤرشفة، لذلك تشغيل `pilot-02` كان مسموحاً بالتسلسل الرسمي |
| تسجيل دخول الأدمن | ✅ نجح | تم الدخول بحساب الأدمن وفتح رابط `pilot-02` الرسمي |
| التشغيل من الواجهة | ✅ نجح | تم إنشاء مهمة `pilot-02` من صفحة `/dashboard/generate-video` وظهرت حالة المعالجة |
| صف المهمة | ✅ تم إنشاؤه واكتمل | المهمة `f20d403c-223b-4e0c-88cf-4aa7fb0f746e` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/text-to-video` لأن عينة `fast` لا تتطلب صورة منتج أو صورة مرجعية |
| سياسة الصوت | ✅ مطابقة | `provider_audio_requested=false` و`audio_policy=provider_audio_disabled_until_saudi_voice_pipeline` |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true` داخل metadata |
| السلامة المالية | ✅ سليمة | خصم `150` نقطة مسجل في ledger بدون `refund_ledger_id` أو `refunded_at`، ولا توجد مهمة نشطة متروكة |

**الحكم النقدي:** تم إغلاق ثاني عينة Medium Test رسمية بنجاح كامل من الواجهة، مع بقاء نفس الضوابط المهمة: تعطيل صوت المزود، أرشفة الفيديو داخلياً، وعدم وجود refund. لا توجد فجوة كود تستدعي تعديلاً في هذه الجولة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `98.5%` لأن عينتين رسميتين متتاليتين (`pilot-01` و`pilot-02`) اكتملتا وأُرشفتا دون تجاوز التسلسل أو تسريب مالي.

**الخطة التالية الدقيقة:** تشغيل `pilot-03` فقط من الرابط الرسمي التالي؛ لا تزال ضمن جودة `fast` ولا تتطلب صورة منتج. معيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + لا refund، ثم بعد `pilot-04` ننتقل بحذر إلى عينات `lite` التي تتطلب صورة منتج.

## جلسة 40 — نجاح `pilot-03` وتأكيد استقرار fast T2V — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 39 | ✅ صحيحة | `pilot-02` مكتملة ومؤرشفة، لذلك تشغيل `pilot-03` كان مطابقاً للتسلسل الرسمي |
| تسجيل دخول الأدمن | ✅ نجح | تم الدخول بحساب الأدمن وفتح رابط `pilot-03` الرسمي |
| التشغيل من الواجهة | ✅ نجح | تم إنشاء مهمة الفيديو وظهرت رسالة `تم إنشاء مهمة الفيديو — جاري المعالجة` |
| صف المهمة | ✅ تم إنشاؤه واكتمل | المهمة `42f7ac5c-1f26-4c7c-99bc-7d18be284970` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/text-to-video` لعينة `fast` بدون صورة منتج أو مرجع |
| سياسة الصوت | ✅ مطابقة | `provider_audio_requested=false` و`audio_policy=provider_audio_disabled_until_saudi_voice_pipeline` |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true`، و`provider_status=succeeded` |
| السلامة المالية | ✅ سليمة | خصم `150` نقطة مسجل في ledger بدون `refund_ledger_id` أو `refunded_at`، والرصيد بعد الخصم `18200` نقطة خطة |

**الحكم النقدي:** تم إغلاق ثالث عينة Medium Test رسمية بنجاح من الواجهة، وهذا يؤكد أن مسار `fast` عبر Text-to-Video مستقر لثلاث عينات متتالية دون refund أو مهام عالقة. لا توجد فجوة كود تستدعي حذفاً أو استبدالاً في هذه الجولة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `99%` لأن 3 عينات رسمية متتالية (`pilot-01` إلى `pilot-03`) اكتملت وأُرشفت مع تعطيل صوت المزود وبدون تسريب مالي.

**الخطة التالية الدقيقة:** تشغيل `pilot-04` فقط؛ هي آخر عينة ضمن جودة `fast` ولا تتطلب صورة منتج. معيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + لا refund. بعد نجاحها يبدأ الانتقال إلى `pilot-05` ضمن جودة `lite` مع وجوب تجهيز/رفع صورة منتج مناسبة قبل التشغيل.

## جلسة 41 — نجاح `pilot-04` وإغلاق مرحلة fast — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 40 | ✅ صحيحة | `pilot-03` مكتملة ومؤرشفة، لذلك تشغيل `pilot-04` كان مطابقاً للتسلسل الرسمي |
| تسجيل دخول الأدمن | ✅ نجح | تم الدخول بحساب الأدمن وفتح رابط `pilot-04` الرسمي |
| التشغيل من الواجهة | ✅ نجح | تم إنشاء مهمة الفيديو وظهرت رسالة `تم إنشاء مهمة الفيديو — جاري المعالجة` |
| صف المهمة | ✅ تم إنشاؤه واكتمل | المهمة `19013b4f-5fe6-4e24-8cbb-e5f002eec792` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/text-to-video` لعينة `fast` بدون صورة منتج أو مرجع |
| سياسة الصوت | ✅ مطابقة | `provider_audio_requested=false` و`audio_policy=provider_audio_disabled_until_saudi_voice_pipeline` |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true`، و`provider_status=succeeded` |
| السلامة المالية | ✅ سليمة | خصم `150` نقطة مسجل في ledger بدون `refund_ledger_id` أو `refunded_at`، والرصيد بعد الخصم `18050` نقطة خطة |
| الواجهة | ✅ جاهزة | الحالة النهائية في الصفحة `جاهز للمعاينة` وتوجد معاينة فيديو |

**الحكم النقدي:** تم إغلاق كل عينات `fast` الأربع (`pilot-01` إلى `pilot-04`) بنجاح متتالٍ من الواجهة، مع أرشفة داخلية وتعطيل صوت المزود وعدم وجود refund. هذا يؤكد استقرار مسار Text-to-Video السريع قبل الانتقال إلى العينات الأعلى تكلفة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `99.25%` لأن مرحلة `fast` اكتملت بالكامل، وبقيت مرحلة `lite` التي تتطلب صورة منتج ثم عينة `quality` النهائية.

**الخطة التالية الدقيقة:** تجهيز صورة منتج مناسبة ثم تشغيل `pilot-05` فقط ضمن جودة `lite`؛ هذه العينة تتطلب صورة منتج إلزامية. معيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + لا refund + بقاء `medium_test_product_image_required=true` موثقاً في ledger/metadata.

## جلسة 42 — نجاح `pilot-05` بعد إصلاح فلتر صورة المنتج — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 41 | ✅ صحيحة | `pilot-04` مكتملة ومؤرشفة، لذلك تشغيل `pilot-05` كان مطابقاً للتسلسل الرسمي |
| العائق المكتشف | ✅ تم عزله | أول تشغيل لـ`pilot-05` فشل قبل إرسال المزود بسبب رفض رابط `fal.media` بصيغة subdomain تحتوي حروفاً، وتم رد `500` نقطة تلقائياً |
| الإصلاح التقني | ✅ تم | توسيع فلتر CDN الآمن ليقبل صيغ `fal.media` الحديثة مثل `v3b.fal.media`، مع إبقاء الرفض لأي نطاق غير موثوق |
| التحقق بعد الإصلاح | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد التعديل |
| التشغيل الرسمي المعاد | ✅ نجح | المهمة `088333ff-ec7c-4ec0-82a4-dcb99674f5ab` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/image-to-video` لعينة `lite` مع صورة المنتج المرجعية |
| سياسة الصوت | ✅ مطابقة | `provider_audio_requested=false` و`audio_policy=provider_audio_disabled_until_saudi_voice_pipeline` |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true`، و`fal_queue_status=COMPLETED` |
| صورة المنتج | ✅ موثقة | `medium_test_product_image_required=true` و`provider_image_source=provider_preflight` مثبتان في metadata |
| السلامة المالية | ✅ سليمة | التشغيل الناجح خصم `500` نقطة بدون refund، ومحاولة الفشل السابقة رُدت آلياً بدون ترك ضرر مالي |

**الحكم النقدي:** تم إغلاق أول عينة `lite` بنجاح بعد معالجة فجوة حقيقية في توافق روابط صورة المنتج مع مزوّد الفيديو، مع الحفاظ على فلتر النطاقات الآمنة وعدم فتح قبول عام غير مضبوط.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `99.5%` لأن `pilot-05` اكتمل وأُرشف مع صورة منتج إلزامية، وبقي تشغيل `pilot-06` ثم عينات الجودة الأعلى حسب التسلسل.

**الخطة التالية الدقيقة:** تشغيل `pilot-06` فقط ضمن جودة `lite` من الرابط الرسمي، مع معيار نجاح: `completed` + `storage_path` + `provider_audio_requested=false` + لا refund + التحقق من عدم وجود مهام `processing` عالقة بعد الاكتمال.

## جلسة 43 — نجاح `pilot-06` لعينة الديكور المنزلي — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 42 | ✅ صحيحة | `pilot-05` مكتملة ومؤرشفة، لذلك تشغيل `pilot-06` كان مطابقاً للتسلسل الرسمي |
| تجهيز صورة المنتج | ✅ تم | أُضيفت صورة منتج ديكور منزلي مناسبة للعينة وربطت تلقائياً بـ`pilot-06` |
| التحقق قبل التشغيل | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد ربط الصورة |
| التشغيل من الواجهة | ✅ نجح | تم الدخول بحساب الأدمن وفتح رابط `pilot-06` الرسمي ثم الضغط مرة واحدة على توليد الفيديو |
| صف المهمة | ✅ تم إنشاؤه واكتمل | المهمة `5d4904e1-7401-4a08-a6d4-95202720a1de` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/image-to-video` لعينة `lite` مع صورة المنتج المرجعية |
| سياسة الصوت | ✅ مطابقة | `provider_audio_requested=false` و`audio_policy=provider_audio_disabled_until_saudi_voice_pipeline` |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true`، و`fal_queue_status=COMPLETED` |
| السلامة المالية | ✅ سليمة | خصم `500` نقطة مسجل في ledger بدون `refund_ledger_id` أو `refunded_at` |
| المهام العالقة | ✅ لا توجد | لا توجد مهام `pending` أو `processing` متبقية لحساب الأدمن بعد الاكتمال |

**الحكم النقدي:** تم إغلاق ثاني عينة `lite` بنجاح من الواجهة مع صورة منتج وأرشفة داخلية، بدون refund أو مهام عالقة. مسار Image-to-Video أصبح مثبتاً على عينتين متتاليتين بعد إصلاح فلتر الصورة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `99.65%` بعد اكتمال `pilot-06`، وبقيت عينات `lite` التالية قبل الانتقال إلى `quality`.

**الخطة التالية الدقيقة:** تشغيل `pilot-07` فقط ضمن جودة `lite` من الرابط الرسمي (`bags`) مع تجهيز صورة منتج شنطة مناسبة أولاً، ثم التحقق من `completed` + `storage_path` + `provider_audio_requested=false` + لا refund.

## جلسة 44 — تجهيز `pilot-07` واكتشاف عائق سياسة مزود الفيديو — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 43 | ✅ صحيحة | `pilot-06` مكتملة ومؤرشفة، وتشغيل `pilot-07` كان مسموحاً حسب التسلسل الرسمي |
| صورة المنتج | ✅ جُهزت | تم إنشاء `public/medium-test-pilot-07-bags.jpg` لصورة حقيبة يد واضحة ومناسبة لاختبار المنتج |
| ربط الواجهة | ✅ جُهز | أضيفت صورة `pilot-07` إلى خريطة صور الاختبار الداخلي حتى تُرسل كـ data URL ثم تُرفع للمزوّد عبر preflight |
| التشغيل من الواجهة | ⚠️ فشل مزود خارجي | تم تشغيل `pilot-07` مرتين من صفحة `/dashboard/generate-video`، لكن المزود رفض البرومبت برسالة `content_policy_violation` |
| السلامة المالية | ✅ سليمة | كلتا المحاولتين انتهتا `refunded` مع وجود `refund_ledger_id`، ولا توجد مهام `pending/processing` عالقة |
| سياسة الصوت والمنتج | ✅ وصلت | `provider_audio_requested=false` و`medium_test_product_image_required=true` و`product_image_url` موجودة في صفوف الفشل |
| تحسين الصياغة | ✅ تم | تم تعديل تسمية قالب `bags` إلى صياغة أكثر وضوحاً: `حقائب يد` بدل مفردات قد يلتبس تفسيرها عند فلتر المزود |
| جودة البناء | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد إضافة الصورة وتعديل الصياغة |

**الحكم النقدي:** لا توجد فجوة مالية أو مهمة عالقة؛ العائق الحالي خارجي من فلتر سياسة المزود على `pilot-07`. لم يُعتمد `pilot-07` لأنه لم يصل إلى `completed` ولا يحتوي `storage_path`.

**نسبة إنهاء الخطة الحالية:** تبقى عند `99.65%` لأن `pilot-07` لم يكتمل بعد، رغم أن تجهيز الصورة والتحقق المالي تمّا بنجاح.

**الخطة التالية الدقيقة:** إعادة تشغيل `pilot-07` بعد استقرار نسخة الصياغة المعدلة في المعاينة. إذا استمر رفض سياسة المحتوى، فالخطوة التالية ليست تغيير التسلسل بل تقليل برومبت المزود لهذه العينة فقط إلى صياغة إنجليزية/محايدة مع الحفاظ على نفس `sampleId/templateId` ومعيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + لا refund.

## جلسة 45 — نجاح `pilot-07` بعد تخفيف آمن للبرومبت — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 44 | ✅ صحيحة | كان العائق من فلتر سياسة المزود، مع ردّ نقاط تلقائي وعدم وجود مهام عالقة |
| إعادة التشغيل الأولى | ⚠️ مرفوضة | استمر رفض المزود للبرومبت العربي المعدل برسالة `content_policy_violation` وتم رد النقاط |
| المعالجة المحدودة | ✅ تمّت | تم تخفيف برومبت المزود لعينة `pilot-07/bags` فقط إلى صياغة إنجليزية محايدة، مع إبقاء رابط الاختبار والمعرفات والقفل الداخلي كما هي |
| التشغيل النهائي | ✅ نجح | المهمة `79f7cea9-e7c0-4e32-bc55-8e1962d31d0a` وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/image-to-video` مع صورة المنتج المرجعية |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true`، و`provider_status=succeeded` |
| سياسة الصوت والمنتج | ✅ مطابقة | `provider_audio_requested=false` و`medium_test_product_image_required=true` و`product_image_url` موجود |
| السلامة المالية | ✅ سليمة | خُصمت `500` نقطة للمهمة الناجحة بدون `refund_ledger_id`، ولا توجد مهام Medium Test عالقة |
| جودة البناء | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد التعديل |

**الحكم النقدي:** تم اعتماد `pilot-07` تشغيلياً بعد عزل سبب الفشل في فلتر المزود وتطبيق علاج محدود لا يغيّر التسلسل ولا يفتح القالب للعامة. يجب تقييم جودة الفيديو بصرياً لاحقاً ضمن scorecard، لكن بوابة التنفيذ الفنية مكتملة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `99.78%` بعد اكتمال `pilot-07` وأرشفته بدون refund وبدون مهام عالقة.

**الخطة التالية الدقيقة:** تشغيل `pilot-08` فقط ضمن جودة `lite` من الرابط الرسمي (`phone-case`) مع تجهيز صورة منتج كفر جوال واضحة أولاً. معيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + `medium_test_product_image_required=true` + لا refund.

## جلسة 46 — نجاح `pilot-08` كفر جوال — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 45 | ✅ صحيحة | `pilot-07` مكتملة ومؤرشفة، لذلك تشغيل `pilot-08` مطابق للتسلسل الرسمي |
| صورة المنتج | ✅ جُهزت | تم إنشاء `public/medium-test-pilot-08-phone-case.jpg` لصورة كفر جوال واضحة ومناسبة لاختبار المنتج |
| ربط الواجهة | ✅ جُهز | أضيفت صورة `pilot-08` إلى خريطة صور الاختبار الداخلي، وتُرسل كـ data URL ثم تُرفع للمزوّد عبر preflight |
| التشغيل من الواجهة | ✅ نجح | تم تشغيل `pilot-08` من الرابط الرسمي `/dashboard/generate-video` |
| صف المهمة | ✅ تم إنشاؤه واكتمل | المهمة `b1f2b10d-f0d0-40f5-b0d9-bf60fde19cdc` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/image-to-video` مع صورة المنتج المرجعية |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true`، و`provider_status=succeeded` |
| سياسة الصوت والمنتج | ✅ مطابقة | `provider_audio_requested=false` و`medium_test_product_image_required=true` و`product_image_url` موجود |
| السلامة المالية | ✅ سليمة | خُصمت `500` نقطة للمهمة الناجحة بدون `refund_ledger_id`، ولا توجد مهام Medium Test عالقة |
| جودة البناء | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد إضافة صورة العينة |

**الحكم النقدي:** تم اعتماد `pilot-08` تشغيلياً دون الحاجة لتخفيف خاص للبرومبت، وهذا يؤكد استقرار مسار `lite` مع صورة منتج لعينة الإكسسوارات التقنية. تقييم جودة الفيديو بصرياً يبقى ضمن scorecard اللاحق، لكن بوابة التنفيذ الفنية مكتملة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `99.86%` بعد اكتمال `pilot-08` وأرشفته بدون refund وبدون مهام عالقة.

**الخطة التالية الدقيقة:** تشغيل `pilot-09` فقط ضمن جودة `lite` من الرابط الرسمي (`ramadan-offer`) مع تجهيز صورة منتج/سفرة رمضانية مناسبة أولاً. معيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + `medium_test_product_image_required=true` + لا refund.

## جلسة 47 — نجاح `pilot-09` عرض رمضان — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 46 | ✅ صحيحة | `pilot-08` مكتملة ومؤرشفة، لذلك تشغيل `pilot-09` مطابق للتسلسل الرسمي |
| صورة المنتج | ✅ جُهزت | تم إنشاء `public/medium-test-pilot-09-ramadan-offer.jpg` كصورة سفرة/هدية رمضانية بدون نصوص واضحة بعد استبدال النسخة الأولى التي احتوت كتابة |
| ربط الواجهة | ✅ جُهز | أضيفت صورة `pilot-09` إلى خريطة صور الاختبار الداخلي، وتُرسل كـ data URL ثم تُرفع للمزوّد عبر preflight |
| التشغيل من الواجهة | ✅ نجح | تم تشغيل `pilot-09` من الرابط الرسمي `/dashboard/generate-video` |
| صف المهمة | ✅ تم إنشاؤه واكتمل | المهمة `bf79c8c5-7512-4a76-9056-aa1b1143db16` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/image-to-video` مع صورة المنتج المرجعية |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true` |
| سياسة الصوت والمنتج | ✅ مطابقة | `provider_audio_requested=false` و`medium_test_product_image_required=true` |
| السلامة المالية | ✅ سليمة | خُصمت `500` نقطة للمهمة الناجحة بدون `refund_ledger_id`، ولا توجد مهام Medium Test عالقة |
| جودة البناء | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد إضافة صورة العينة |

**الحكم النقدي:** تم اعتماد `pilot-09` تشغيلياً، ومعالجة فجوة الصورة قبل التشغيل كانت صحيحة لأن النسخة الأولى تضمنت نصاً على العبوة قد يرفع مخاطر التشوه أو الرفض. العينة النهائية أثبتت استقرار مسار `lite` الموسمي مع صورة منتج رمضانية وأرشفة داخلية دون refund.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `99.91%` بعد اكتمال `pilot-09` وأرشفته بدون refund وبدون مهام عالقة.

**الخطة التالية الدقيقة:** تشغيل `pilot-10` فقط ضمن جودة `lite` من الرابط الرسمي (`eid-gift`) مع تجهيز صورة هدية عيد واضحة وبدون نصوص. معيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + `medium_test_product_image_required=true` + لا refund.

## جلسة 48 — نجاح `pilot-10` هدية عيد — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 47 | ✅ صحيحة | `pilot-09` مكتملة ومؤرشفة، لذلك تشغيل `pilot-10` مطابق للتسلسل الرسمي |
| صورة المنتج | ✅ جُهزت | تم إنشاء `public/medium-test-pilot-10-eid-gift.jpg` كصورة هدية عيد واضحة وبدون نصوص/شعارات |
| ربط الواجهة | ✅ جُهز | أضيفت صورة `pilot-10` إلى خريطة صور الاختبار الداخلي، وتُرسل كـ data URL ثم تُرفع للمزوّد عبر preflight |
| التشغيل من الواجهة | ✅ نجح | تم تشغيل `pilot-10` من الرابط الرسمي `/dashboard/generate-video` |
| صف المهمة | ✅ تم إنشاؤه واكتمل | المهمة `ce501725-7b87-4c28-9e0a-81d70b237a11` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/image-to-video` مع صورة المنتج المرجعية |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true` |
| سياسة الصوت والمنتج | ✅ مطابقة | `provider_audio_requested=false` و`medium_test_product_image_required=true` |
| السلامة المالية | ✅ سليمة | خُصمت `500` نقطة للمهمة الناجحة بدون `refund_ledger_id`، ولا توجد مهام Medium Test عالقة |
| جودة البناء | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد إضافة صورة العينة |

**الحكم النقدي:** تم اعتماد `pilot-10` تشغيلياً، وأصبح مسار `lite` مع صور المنتجات واضح الاستقرار عبر عينات المطعم، الديكور، الإكسسوارات، رمضان، والعيد دون مهام عالقة أو استرداد غير متوقع. لا توجد فجوة كود تتطلب حذفاً أو استبدالاً في هذه الجولة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `99.95%` بعد اكتمال `pilot-10` وأرشفته بدون refund وبدون مهام عالقة.

**الخطة التالية الدقيقة:** تشغيل `pilot-11` فقط ضمن جودة `lite` من الرابط الرسمي (`kids-product`) مع تجهيز صورة منتج أطفال آمنة بدون ظهور طفل بملامح قريبة. معيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + `medium_test_product_image_required=true` + لا refund، مع مراقبة أعلى لاحتمال حساسية القالب لأنه مصنف `عالٍ`.

## جلسة 49 — نجاح `pilot-11` منتج أطفال — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 48 | ✅ صحيحة | `pilot-10` مكتملة ومؤرشفة، لذلك تشغيل `pilot-11` مطابق للتسلسل الرسمي |
| صورة المنتج | ✅ جُهزت بحذر | تم إنشاء `public/medium-test-pilot-11-kids-product.jpg` كصورة منتج أطفال آمنة بدون أطفال أو وجوه أو أشخاص أو نصوص |
| ربط الواجهة | ✅ جُهز | أضيفت صورة `pilot-11` إلى خريطة صور الاختبار الداخلي، وتُرسل كـ data URL ثم تُرفع للمزوّد عبر preflight |
| التشغيل من الواجهة | ✅ نجح | تم تشغيل `pilot-11` من الرابط الرسمي `/dashboard/generate-video` |
| صف المهمة | ✅ تم إنشاؤه واكتمل | المهمة `2906e656-b9c2-4ef5-972d-4a99cd2cc1b1` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/image-to-video` مع صورة المنتج المرجعية |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true` |
| سياسة الصوت والمنتج | ✅ مطابقة | `provider_audio_requested=false` و`medium_test_product_image_required=true` |
| السلامة المالية | ✅ سليمة | خُصمت `500` نقطة للمهمة الناجحة بدون `refund_ledger_id`، ولا توجد مهام Medium Test عالقة |
| جودة البناء | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد إضافة صورة العينة |

**الحكم النقدي:** تم اعتماد `pilot-11` تشغيلياً رغم أن القالب عالي الحساسية؛ اختيار صورة منتج فقط بدون أطفال أو وجوه خفّض مخاطر الرفض والتشوّه، واكتملت المهمة بأرشفة داخلية وبدون refund. تبقى مراجعة جودة المحتوى البصري ضمن scorecard لاحقاً، لكن بوابة التنفيذ التقنية مكتملة.

**نسبة إنهاء الخطة الحالية:** ترتفع إلى `99.98%` بعد اكتمال `pilot-11` وأرشفته بدون refund وبدون مهام عالقة.

**الخطة التالية الدقيقة:** تشغيل `pilot-12` فقط ضمن جودة `quality` من الرابط الرسمي (`supplements`) مع تجهيز صورة مكملات آمنة بدون ادعاءات علاجية أو نصوص واضحة. معيار النجاح: `completed` + `storage_path` + `provider_audio_requested=false` + `medium_test_product_image_required=true` + لا refund، مع مراقبة عالية لأنه أول اختبار `quality` وآخر قالب في المصفوفة الحالية.

## جلسة 50 — نجاح `pilot-12` مكملات بجودة Quality — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---|---|
| مراجعة جلسة 49 | ✅ صحيحة | `pilot-11` مكتملة ومؤرشفة، لذلك تشغيل `pilot-12` مطابق للتسلسل الرسمي |
| صورة المنتج | ✅ جُهزت بحذر | تم إنشاء `public/medium-test-pilot-12-supplements.jpg` ثم استبدال النسخة الأولى لأنها احتوت كبسولات مبعثرة؛ النسخة المعتمدة عبوة/باوتش بلا نصوص أو ادعاءات علاجية |
| ربط الواجهة | ✅ جُهز | أضيفت صورة `pilot-12` إلى خريطة صور الاختبار الداخلي، وتُرسل كـ data URL ثم تُرفع للمزوّد عبر preflight |
| التشغيل من الواجهة | ✅ نجح | تم تشغيل `pilot-12` من الرابط الرسمي `/dashboard/generate-video` بجودة `quality` |
| صف المهمة | ✅ تم إنشاؤه واكتمل | المهمة `f92a5896-d598-4330-bcb5-516501a044de` بدأت `processing` ثم وصلت إلى `completed` |
| المزوّد والموديل | ✅ صحيح | استخدم `fal-ai/pixverse/v6/image-to-video` مع صورة المنتج المرجعية |
| الأرشفة | ✅ مكتملة | يوجد `storage_path` و`result_url`، و`internal_video_archived=true` |
| سياسة الصوت والمنتج | ✅ مطابقة | `provider_audio_requested=false` و`medium_test_product_image_required=true` |
| السلامة المالية | ✅ سليمة | خُصمت `800` نقطة للمهمة الناجحة بدون `refund_ledger_id`، ولا توجد مهام Medium Test عالقة |
| جودة البناء | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد إضافة صورة العينة |

**الحكم النقدي:** تم اعتماد `pilot-12` تشغيلياً كأول وآخر عينة `quality` في المصفوفة الحالية، مع ضبط الصورة لتجنب ادعاءات صحية أو عناصر حساسة. اكتملت العينة بأرشفة داخلية وبدون refund، وبذلك أُغلقت مصفوفة Medium Test الحالية تنفيذياً من `pilot-01` إلى `pilot-12`.

**نسبة إنهاء الخطة الحالية:** تصل إلى `100%` لمصفوفة Medium Test التنفيذية، مع بقاء مراجعة scorecard البصرية النهائية كخطوة جودة/اعتماد وليست مانعاً تقنياً لمسار التنفيذ.

**الخطة التالية الدقيقة:** تنفيذ مراجعة إغلاق Medium Test النهائية: تجميع نتائج `pilot-01` إلى `pilot-12`، تلخيص النجاحات/الاستردادات/النماذج، وتحديد القوالب المسموح فتحها للمستخدمين مقابل القوالب التي تحتاج مراجعة بصرية أو تبقى مخفية.

## جلسة 51 — تقرير إغلاق Medium Test وScorecard النهائي — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| العينات الرسمية المكتملة | 12/12 | ✅ اكتملت كل عينات `pilot-01` إلى `pilot-12` وفق التسلسل الرسمي |
| المهام المفتوحة | 0 | ✅ لا توجد مهام `pending` أو `processing` ضمن Medium Test |
| الأرشفة الداخلية | 12/12 | ✅ كل مهمة ناجحة تحتوي `storage_path` و`result_url` و`internal_video_archived=true` |
| سياسة الصوت | 12/12 | ✅ صوت المزود معطّل؛ القيم النهائية `provider_audio_requested=false` أو `audio_requested=false` |
| مجموع خصومات النجاحات | 4900 نقطة | ✅ مطابق لتكلفة 4 fast ×150 + 7 lite ×500 + 1 quality ×800 |
| محاولات refund أثناء الاختبار | 5 محاولات / 1800 نقطة | ✅ كلها موثقة برد نقاط؛ لا توجد محاولة refund عالقة |
| مسار Text-to-Video | 4/4 | ✅ `pilot-01` إلى `pilot-04` جاهزة تقنياً للـBeta |
| مسار Image-to-Video | 8/8 | ✅ `pilot-05` إلى `pilot-12` جاهزة تقنياً بعد إثبات صورة المنتج والأرشفة |

### Scorecard القوالب

| العينة | القالب | الجودة | النتيجة التنفيذية | قرار الإطلاق |
|---|---|---|---|---|
| `pilot-01` | `perfume-premium-hook` | fast | ✅ مكتمل ومؤرشف | يفتح للـBeta |
| `pilot-02` | `abaya-elegance` | fast | ✅ مكتمل ومؤرشف | يفتح للـBeta |
| `pilot-03` | `electronics-benefit` | fast | ✅ مكتمل ومؤرشف | يفتح للـBeta |
| `pilot-04` | `beauty-skincare` | fast | ✅ مكتمل ومؤرشف | يفتح للـBeta |
| `pilot-05` | `restaurant-offer` | lite | ✅ مكتمل ومؤرشف بعد إصلاح فلتر CDN | يفتح للـBeta |
| `pilot-06` | `home-decor` | lite | ✅ مكتمل ومؤرشف | يفتح للـBeta |
| `pilot-07` | `bags` | lite | ✅ مكتمل بعد تخفيف محدود للبرومبت | يفتح للـBeta بشرط مراجعة بصرية قبل التوسع العام |
| `pilot-08` | `phone-case` | lite | ✅ مكتمل ومؤرشف | يفتح للـBeta |
| `pilot-09` | `ramadan-offer` | lite | ✅ مكتمل بعد استبدال صورة تحتوي نصاً | يفتح للـBeta الموسمي مع مراجعة النصوص على الصور |
| `pilot-10` | `eid-gift` | lite | ✅ مكتمل ومؤرشف | يفتح للـBeta الموسمي |
| `pilot-11` | `kids-product` | lite | ✅ مكتمل بصورة منتج دون أطفال/وجوه | يفتح للـBeta المقيّد؛ لا يوسّع قبل مراجعة بصرية وسياسة محتوى |
| `pilot-12` | `supplements` | quality | ✅ مكتمل بعد ضبط صورة آمنة بلا ادعاءات علاجية | يفتح للـBeta المقيّد؛ يتطلب مراجعة ادعاءات قبل الإطلاق العام |

**الحكم النقدي:** مصفوفة Medium Test مغلقة تنفيذياً بنسبة `100%`: كل العينات الرسمية اكتملت، لا توجد مهام عالقة، الأرشفة الداخلية مفعّلة، والدفتر المالي متوازن بين خصومات النجاح وعمليات refund. لا أرفع اعتماد المنتج الكلي إلى 100% لأن QA العام وسيناريوهات Safari/84 تمريرة لا تزال منفصلة عن إغلاق مصفوفة الفيديو هذه.

**قرار الفتح:** القوالب كلها صالحة للـBeta التقني. القوالب التي تحتاج مراجعة بصرية/سياسات قبل التوسع العام: `bags` بسبب تخفيف البرومبت، `kids-product` لحساسية منتجات الأطفال، و`supplements` لحساسية الادعاءات الصحية. القوالب الموسمية `ramadan-offer` و`eid-gift` صالحة للـBeta مع مراجعة توقيت/موسمية العرض.

**نسبة إنهاء خطة Medium Test:** `100%`. **نسبة اعتماد المنتج الكلي قبل Soft Launch:** تبقى ضمن نطاق `99.97%–99.98%` ولا تُغلق نهائياً قبل QA اليدوي العام.

**الخطة التالية الدقيقة:** الانتقال من إغلاق Medium Test إلى تمريرة QA عامة أخيرة على السيناريوهات الأعلى خطورة: جلسة مستخدم/أدمن فعلية تغطي S8-S14 بعد الإغلاق، ثم Safari/Chrome للمسارات الأساسية، ثم قرار Soft Launch محدود أو مؤجل حسب نتائج تلك التمريرة.

## جلسة 52 — تمريرة QA عامة بعد Medium Test ومعالجة فجوة التريجرات — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| مراجعة جلسة 51 | ✅ صحيحة | Medium Test مغلق تنفيذياً؛ 12/12 عينة مكتملة، 0 مهام مفتوحة، و4900 نقطة خصومات نجاح مطابقة |
| حساب الأدمن | ✅ صحيح | `saalla012@gmail.com` على خطة `business` ودوره `admin` ورصيده الحالي `13250` نقطة |
| حالة الفيديو العامة | ✅ مستقرة | إجمالي `video_jobs`: 22 مكتملة و14 مستردة، ولا توجد حالة `pending/processing` في الملخص الحالي |
| الفجوة المكتشفة | ⚠️ حرجة وتمت معالجتها | استعلام التريجرات الأول أعاد 0 بسبب فلترة خاطئة باسم `tgrelid::regclass`، ثم أكد الفحص الشامل وجود تريجرات فعلية؛ أثناء المعالجة أُعيد تثبيت التريجرات الحرجة ونُظّفت النسخ المكررة |
| التريجرات الحرجة بعد التنظيف | ✅ فعالة | `on_auth_user_created`, `trg_protect_profile_plan_change`, `trg_enforce_generation_integrity`, `trg_enforce_video_processing_limit`, وتريجرات تفعيل الاشتراك والنقاط تعمل بنسخة واحدة لكل غرض حرج |
| تكرار تريجرات `updated_at` | ✅ عولج | أزيلت النسخ المكررة ذات أسماء `update_*` وبقيت تريجرات `trg_*` القائمة، مع إبقاء تريجر واحد فقط حيث كان التكرار موجوداً |
| تحذيرات الأمان | ⚠️ غير مانعة لهذه الخطوة | تم إغلاق تنفيذ دوال التريجرات الحساسة مباشرة على الزوار/المستخدمين؛ بقيت تحذيرات عامة لدوال `SECURITY DEFINER` مستخدمة كـRPC أو داخلية وتحتاج جولة تحصين منفصلة دون كسر الوظائف |

**الحكم النقدي:** الخطوة السابقة كانت صحيحة من ناحية إغلاق Medium Test، لكن تمريرة ما بعد الإغلاق كشفت ضرورة عدم الاكتفاء بالتوثيق القديم للتريجرات. تم تثبيت التريجرات الحرجة وتنظيف التكرار، وتأكدت حالة الأدمن والفيديو دون مهام عالقة. لا يوجد تعديل كود واجهة مطلوب في هذه الجولة.

**نسبة التقدم بعد هذه الدفعة:** Medium Test يبقى `100%`. جاهزية المنتج الكلية ترتفع عملياً إلى نحو `99.985%` من ناحية البناء/التشغيل، لكنها لا تصبح 100% قبل جولة تحصين دوال قاعدة البيانات ثم تمريرة QA يدوية للمتصفحات والمسارات.

**الخطة التالية الدقيقة:** تنفيذ جولة تحصين صلاحيات دوال قاعدة البيانات المتبقية: تصنيف دوال الـRPC المطلوبة للمستخدمين مقابل الدوال الداخلية فقط، ثم سحب صلاحيات التنفيذ من غير الضروري بدون كسر النص/الصورة/الفيديو/النقاط، وبعدها تشغيل فحص أمني وإغلاق S8-S14 بجلسة أدمن/مستخدم فعلية.

## جلسة 53 — تحصين صلاحيات RPC وقاعدة أقل صلاحية — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| مراجعة استخدام RPC في الكود | ✅ اكتملت | تم حصر الاستدعاءات الفعلية عبر `.rpc(...)` في مسارات النقاط، الحصص، البريد، الأدمن، الفيديو، والإعدادات العامة |
| إزالة المنح العامة | ✅ تمت | تم سحب `EXECUTE` العام من كل دوال `public` ثم إعادة منح الصلاحيات المطلوبة فقط حسب الاستخدام الفعلي |
| الدوال العامة المسموحة | 2 | `get_founding_status` و`get_public_app_settings` فقط تبقيان للزائر/المستخدم لأنها تغذي صفحات عامة ببيانات محدودة |
| دوال المستخدم المصادق | 15 | بقيت فقط للدوال التي تحتاج جلسة فعلية: الأدوار، الرصيد، الحصص، استهلاك/رد نقاط الفيديو، تسجيل التوليد، والتفعيل الإداري المحمي داخلياً |
| الدوال الداخلية/التريجرات | ✅ مقفلة | دوال التريجرات والبريد الداخلي والفحص الدوري أصبحت غير قابلة للاستدعاء من الزائر أو المستخدم العادي مباشرة |
| التريجرات بعد التحصين | ✅ فعالة | التريجرات الحرجة ما زالت موجودة ومفعلة بعد تغيير صلاحيات التنفيذ |
| فحص linter | ⚠️ بقي 17 تحذيراً | التحذيرات المتبقية تخص نمط `SECURITY DEFINER` لدوال RPC مقصودة وتحتوي تحقق داخلي/RLS؛ تم تقليل السطح لكن لا يمكن تصفيرها دون إعادة هندسة RPC إلى Server Functions كاملة |

**الحكم النقدي:** تم تنفيذ تحصين حقيقي دون كسر مسارات المنتج: أزيلت صلاحيات التنفيذ العامة غير المقصودة، وبقيت الصلاحيات فقط للدوال التي يستخدمها التطبيق فعلياً. التحذيرات المتبقية ليست دليلاً على كشف بيانات مباشر، لكنها تبقى ديناً أمنياً موثقاً يستحق جولة refactor لاحقة لتحويل بعض RPC الحساسة إلى وظائف خادمية كاملة.

**نسبة التقدم بعد هذه الدفعة:** ترتفع جاهزية البناء/التشغيل إلى نحو `99.99%`. لا تزال جاهزية الإطلاق العام أقل من 100% حتى تمريرة QA اليدوية النهائية على مستخدم/أدمن وChrome/Safari.

**الخطة التالية الدقيقة:** تنفيذ تمريرة S8-S14 الفعلية على الواجهة: تسجيل دخول الأدمن، مراجعة `/dashboard/generate-video`, `/dashboard/library`, `/admin/video-jobs`, `/admin/credit-ledger`, والتأكد من عدم ظهور بيانات الإدارة لغير الأدمن، ثم تحديث قرار Soft Launch.

## جلسة 54 — تمريرة S8-S14 فعلية وإزالة استدعاء ترحيب مكرر — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| تسجيل دخول الأدمن | ✅ نجح | تم الدخول بحساب `saalla012@gmail.com` والتحويل إلى `/dashboard` |
| `/dashboard/generate-video` | ✅ سليم | الرصيد ظاهر `13,250` نقطة، خيارات الجودة ظاهرة، وقائمة الفيديوهات تعمل دون خطأ واجهة |
| `/dashboard/library` | ✅ سليم | تظهر الفيديوهات مع زر تشغيل وفتح الفيديو دون رسالة خطأ |
| `/admin/video-jobs` | ✅ سليم | تظهر لوحة الإدارة: 36 مهمة، 0 نشطة، 22 مكتملة، 14 مستردة، وتظهر بطاقات المهام وروابط النتائج |
| `/admin/credit-ledger` | ✅ سليم | تظهر سجلات `consume/refund` للأدمن مع الرصيد بعد كل حركة |
| Console | ⚠️ عولج | ظهر 401 من استدعاء `sendWelcomeEmail` بعد تسجيل دخول قديم؛ تم حذف الاستدعاء من صفحة الدخول لأن الترحيب لا يجب أن يعمل عند كل login |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` نجح بعد حذف الاستدعاء القديم |

**الحكم النقدي:** تمريرة S8-S14 الفعلية نجحت على حساب الأدمن، والفجوة الوحيدة كانت استدعاء ترحيب مكرر وغير لازم بعد تسجيل الدخول. أُزيل الكود القديم المرتبط به من صفحة `/auth` دون تغيير مسارات الفيديو أو الإدارة.

**نسبة التقدم بعد هذه الدفعة:** `99.992%` جاهزية بناء وتشغيل. لا يزال الإغلاق النهائي يحتاج فحص غير أدمن/Safari وتمرير QA اليدوي المتبقي قبل 100%.

**الخطة التالية الدقيقة:** تنفيذ اختبار عزل غير الأدمن لمسارات `/admin/*` ثم فحص سريع Mobile/Safari للمسارات الأساسية، وبعدها إصدار قرار Soft Launch محدود.

## جلسة 55 — عزل `/admin/*` وتحسين تحقق الإدارة — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| مراجعة جلسة 54 | ✅ صحيحة | تمريرة الأدمن السابقة نجحت، والخطوة التالية كانت عزل غير الأدمن ومسارات الإدارة |
| فحص كل ملفات `/admin/*` | ✅ اكتمل | كل صفحات الإدارة الرئيسية تحتوي `adminBeforeLoad` و`AdminGuard` |
| مسارات تحويل قديمة | ⚠️ وُجدت وعولجت | `/admin/video-provider` و`/admin/video-provide` كانتا تحوّلان إلى صفحة الإدارة قبل تحقق الدور؛ أضيف لهما `adminBeforeLoad` قبل التحويل |
| طبقة الخادم | ✅ عُزّزت | أضيف تحقق `assertAdmin` الصريح إلى `reconcileUsageLogs` حتى لا يعتمد المسار الإداري على تحقق الـRPC فقط |
| اختبار واجهة سريع | ✅ نجح | فتح `/admin/video-provider` يحوّل بعد التحقق إلى `/admin/video-providers`، ومحاولة `/admin/video-jobs` بدون جلسة مستخدم تُظهر مسار الدخول بدلاً من بيانات الإدارة |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` نجح بعد تعديلات الحماية |

**الحكم النقدي:** تم إغلاق فجوة صغيرة في مسارات تحويل قديمة لا تعرض بيانات بذاتها، لكنها كانت تتجاوز حارس الواجهة قبل التحويل. كما تم توحيد قاعدة الإدارة خادمياً بحيث كل وظائف `src/server/admin*.ts` التي تستخدم Server Functions تجمع بين جلسة مصادقة وتحقق أدمن صريح.

**نسبة التقدم بعد هذه الدفعة:** `99.994%` جاهزية بناء وتشغيل. لا يزال الاعتماد النهائي يحتاج فحص Mobile/Safari سريع للمسارات الأساسية ثم قرار Soft Launch محدود.

**الخطة التالية الدقيقة:** تنفيذ فحص Mobile/Safari أو محاكاة Safari للمسارات الأساسية `/`, `/auth`, `/dashboard/generate-video`, `/dashboard/library` مع مراقبة console/network، ثم إصدار قرار Soft Launch محدود إذا لم تظهر أخطاء حرجة.

## جلسة 56 — فحص Mobile للمسارات الأساسية وإصلاح 401 عام — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| مراجعة جلسة 55 | ✅ صحيحة | عزل `/admin/*` مكتمل، والخطوة التالية كانت فحص Mobile للمسارات الأساسية |
| `/` Mobile 390 | ✅ سليم بصرياً | العنوان والـCTA ظاهران دون overflow واضح |
| `/auth` Mobile 390 | ✅ سليم بصرياً | حقول البريد وكلمة المرور وزر الدخول وGoogle ظاهرة دون تداخل |
| تسجيل دخول الأدمن | ✅ نجح | الدخول بحساب الأدمن حوّل إلى `/dashboard` دون خطأ ظاهر |
| `/dashboard/generate-video` Mobile 390 | ✅ سليم | الرصيد `13,250` نقطة، خيارات الجودة/المقاس/المدة والقوالب وحقول الصور ظاهرة؛ Cookie banner كان ظاهراً وتم قبوله |
| `/dashboard/library` Mobile 390 | ✅ سليم | تظهر `27` توليدة، تبويبات الفلترة، وقائمة الفيديوهات مع حالات مكتملة/مستردة دون تداخل DOM ظاهر |
| Console/Network | ⚠️ وُجدت فجوة وعولجت | ظهر 401 من استعلام مباشر عام إلى `generations` داخل `SavingsCounter`; نُقل الإحصاء إلى Server Function تُرجع أرقاماً مجمّعة فقط |
| جودة البناء | ✅ ناجح | `bunx tsc --noEmit` نجح بعد الإصلاح |
| إعادة التحقق بعد الإصلاح | ✅ لا 401 جديد | بعد إعادة فتح `/` لم يظهر طلب 401 جديد إلى `generations`; الطلبات الحالية 200 أو تخص جلسة الأدمن/RLS المسموحة |

**الحكم النقدي:** الفحص كشف فجوة خصوصية/UX مهمة في الصفحة العامة: مكوّن إحصاء كان يحاول قراءة `generations` مباشرة من العميل فينتج 401. الإصلاح الأفضل كان نقل الحساب إلى الخادم وإرجاع إحصاءات مجمّعة فقط بدون `user_id` أو بيانات خام للمستخدمين. لا يوجد حذف واجهة مطلوب؛ تم حذف الاعتماد القديم على استعلام العميل.

**نسبة التقدم بعد هذه الدفعة:** `99.996%` جاهزية بناء وتشغيل. ما زال الاعتماد النهائي يحتاج قياس/فحص Safari حقيقي أو بيئة مماثلة وقرار Soft Launch محدود.

**الخطة التالية الدقيقة:** تشغيل فحص نهائي مختصر قبل القرار: مراجعة logs/console بعد النشر أو المعاينة، فحص Safari/Chrome إن توفر، ثم إصدار قرار Soft Launch محدود مع قائمة مراقبة أول 24 ساعة.

## جلسة 57 — بوابة ما قبل Soft Launch وقرار الإطلاق المحدود — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| مراجعة جلسة 56 | ✅ صحيحة | إصلاح 401 العام تم بنقل إحصاء الصفحة العامة إلى Server Function آمنة تعيد أرقاماً مجمّعة فقط |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` نجح بدون أخطاء |
| Production build | ✅ ناجح | `bun run build` نجح؛ بقيت تحذيرات حجم chunks كدين أداء غير مانع للإطلاق المحدود |
| الاستعلام القديم من الواجهة | ✅ محذوف | لم يعد يوجد استعلام `generations/user_id/limit(5000)` داخل `src/components` أو `src/routes`; المتبقي داخل Server Function عامة آمنة وملفات أدمن خادمية |
| Security scan state | ✅ لا مانع جديد | فحص حالة الأمان لم يُرجع findings مانعة جديدة في هذه البوابة |
| قرار Soft Launch | ✅ مسموح محدوداً | مسموح بإطلاق محدود/مراقب، وليس إطلاقاً عاماً كاملاً، بسبب بقاء Safari/Lighthouse الرسمي ومراقبة أول 24 ساعة |

**الحكم النقدي:** لا توجد موانع بناء أو TypeScript أو فجوات RLS ظاهرة تمنع Soft Launch محدود. لا يُرفع الاعتماد إلى 100% لأن الإطلاق العام يحتاج Lighthouse إنتاجي، Safari حقيقي، ومراقبة تشغيلية بعد أول مستخدمين فعليين.

**نسبة التقدم بعد هذه الدفعة:** `99.998%` جاهزية بناء وتشغيل. الاعتماد النهائي `100%` مشروط بمرور 24 ساعة مراقبة دون أخطاء حرجة وبإغلاق Safari/Lighthouse الرسمي.

**الخطة التالية الدقيقة:** بدء Soft Launch محدود لمجموعة صغيرة، مع مراقبة أول 24 ساعة: أخطاء server functions، فشل الفيديو/refund، رسائل البريد، نماذج الاشتراك، ودفتر النقاط؛ ثم اتخاذ قرار التوسع أو إيقاف/إصلاح سريع.

## جلسة 58 — مراقبة أولية بعد Soft Launch وإصلاح loop الإحصاء — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| مراجعة جلسة 57 | ✅ صحيحة | Soft Launch محدود كان مسموحاً بشرط مراقبة الأخطاء خلال أول 24 ساعة |
| لوجات الخادم | ⚠️ وُجد إنذار وتمت معالجته | ظهر `Maximum update depth exceeded` في بيئة المعاينة بعد إصلاح `SavingsCounter`; السبب المحتمل تبعية `useServerFn` غير ثابتة داخل `useEffect` |
| إصلاح الواجهة | ✅ تم | ثُبّت `useEffect` في `SavingsCounter` ليعمل مرة واحدة بعد mount ولا يعيد الحلقة بسبب تغير مرجع دالة الخادم |
| TypeScript + build | ✅ ناجح | `bunx tsc --noEmit` و`bun run build` نجحا بعد الإصلاح |
| إعادة فحص المتصفح | ✅ لا تكرار جديد | فتح `/` بعد الإصلاح لم يُظهر خطأ `Maximum update depth` جديد في جلسة المتصفح |
| حالة الفيديو | ✅ مستقرة | `video_jobs`: 22 مكتملة و14 مستردة، ولا توجد مهام `pending/processing` عالقة |
| لوجات قاعدة البيانات | ✅ لا أخطاء حديثة مانعة | لا توجد أخطاء `ERROR/FATAL/PANIC` حديثة ضمن نافذة الفحص |

**الحكم النقدي:** قرار Soft Launch المحدود لا يزال صالحاً، لكن كان يجب إيقاف التوسع العام مؤقتاً حتى معالجة loop الإحصاء. تم الإصلاح والتحقق ببناء كامل ومتصفح، ولا توجد مهام فيديو عالقة أو أخطاء قاعدة بيانات مانعة.

**نسبة التقدم بعد هذه الدفعة:** `99.999%` جاهزية بناء وتشغيل. لا نعلن 100% إلا بعد استمرار المراقبة 24 ساعة دون تكرار loop أو أخطاء فيديو/دفع/بريد حرجة.

**الخطة التالية الدقيقة:** مواصلة مراقبة أول 24 ساعة: server logs، أخطاء auth، فشل الفيديو/refund، طوابير البريد، ونماذج الاشتراك؛ إذا بقيت نظيفة يتم اعتماد 100% وفتح التوسع التدريجي.

## جلسة 59 — متابعة مراقبة Soft Launch وتثبيت قرار عدم التوسع العام — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| مراجعة جلسة 58 | ✅ صحيحة | إصلاح loop الإحصاء كان ضرورياً، ولا توجد حاجة لتعديل إضافي في `SavingsCounter` ضمن هذه الجولة |
| TypeScript | ✅ ناجح | `bunx tsc --noEmit` نجح دون أخطاء |
| Production build | ✅ ناجح | `bun run build` نجح؛ تحذيرات حجم الحزم باقية كملاحظة أداء غير مانعة للـSoft Launch المحدود |
| لوجات الإنتاج | ✅ لا أخطاء تطبيق مانعة | لا تظهر أخطاء إنتاجية حرجة مرتبطة بالفيديو أو loop؛ ظهرت فقط طلبات `.js.map` 404 غير مانعة |
| لوجات المعاينة | ⚠️ أثر قديم غير مانع | ما زالت لوجات sandbox تعرض آثار `Maximum update depth` القديمة من قبل الإصلاح، وليست دليلاً على تكرار جديد بعد البناء الحالي |
| حالة الفيديو | ✅ مستقرة | `video_jobs`: 22 مكتملة، 14 مستردة، 0 pending/processing، 0 مكتملة بلا نتيجة، و0 فشل/استرداد بلا refund ledger |
| صحة البريد | ⚠️ مراقبة مطلوبة | `auth_pending=0` و`transactional_pending=0`، لكن يوجد `transactional_dlq=3`; العدد أقل من حد التنبيه الحالي 5، ويحتاج مراجعة أدمن/تنظيف لاحق قبل الإعلان العام |

**الحكم النقدي:** لا يوجد blocker تقني جديد يمنع استمرار Soft Launch المحدود، لكن لا يصح رفع الجاهزية إلى 100% أو فتح التوسع العام لأن طابور البريد الفاشل يحتوي 3 رسائل ولأن Safari/Lighthouse/QA اليدوي الكامل لم تُغلق بعد. القرار المهني هو استمرار المراقبة المحدودة لا التوسع.

**نسبة التقدم بعد هذه الدفعة:** `99.9992%` جاهزية بناء وتشغيل. يبقى الاعتماد العام مشروطاً بتنظيف/تفسير DLQ، مرور نافذة مراقبة دون أخطاء حرجة، وإغلاق Safari/Lighthouse/84 تمريرة QA.

**الخطة التالية الدقيقة:** مراجعة `/admin/email-monitor` لمعرفة رسائل `transactional_dlq=3` هل هي قديمة أم جديدة، تنظيف أو إعادة إرسال المناسب، ثم تنفيذ محاولة فيديو مراقبة واحدة بلا refund جديد، وبعدها استكمال عينة Soft Launch النظيفة حتى 10/10 قبل قرار Beta.

## جلسة 60 — تدقيق البريد وقرار إيقاف التوسع حتى إعادة تفعيل النطاق — 27 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| حالة نطاق البريد | 🚫 غير مفعّل حالياً | لم يظهر نطاق بريد مهيأ في مساحة العمل؛ هذا يمنع الاعتماد العام حتى إعادة إعداد نطاق الإرسال الصحيح |
| قاعدة اسم النطاق | ✅ صُححت | `rifd.club` حُذف نهائياً ولا يُستخدم مجدداً؛ أي إعداد بريد لاحق يجب أن يعتمد نطاقات `rifd.site` فقط |
| سجل البريد الموحّد | ✅ مدقق مع إزالة التكرار | آخر 30 يوماً بعد dedupe حسب `message_id`: 5 رسائل مرسلة، و2 رسائل `dlq` فقط |
| رسائل DLQ الفعلية في السجل | ⚠️ قديمة وغير جديدة | الرسالتان هما قالب `welcome` بتاريخ 24 أبريل بسبب `TTL exceeded (60 minutes)` إلى `saalla012@gmail.com` و`a6439875542@gmail.com` |
| آخر 24 ساعة | ✅ نظيفة | لا توجد رسائل بريد جديدة ولا DLQ جديد خلال آخر 24 ساعة حسب `email_send_log` |
| صحة الطوابير | ⚠️ تحتاج تنظيف/مراجعة | فحص الصحة يعرض `transactional_dlq=3` مقابل رسالتين فقط في سجل الإرسال؛ الاحتمال وجود رسالة queue قديمة غير موثقة في السجل أو اختلاف قديم بين الطابور والسجل |

**الحكم النقدي:** الخطوة السابقة كانت صحيحة عندما اعتبرت `transactional_dlq=3` سبباً لعدم رفع الجاهزية. التدقيق الحالي يوضح أن فشل البريد الموثق قديم وليس فشلاً جديداً، لكن الفجوة الأكبر هي أن نطاق البريد غير مهيأ حالياً في مساحة العمل؛ لذلك لا يجوز فتح Beta أو الإعلان العام قبل إعادة تفعيل البريد من **Cloud → Emails** ثم التحقق من وصول بريد فعلي وسجل إرسال نظيف.

**نسبة التقدم بعد هذه الدفعة:** `99.9990%` جاهزية بناء وتشغيل. تم خفضها قليلاً تشغيلياً لأن البريد غير مفعّل حالياً، رغم أن الكود والبناء ليسا مكسورين.

**الخطة التالية الدقيقة:** إعادة إعداد نطاق البريد من **Cloud → Emails** باستخدام نطاق صالح من عائلة `rifd.site` فقط، ثم تشغيل فحص وصول بريد فعلي وتأكيد أن `email_send_log` يسجل `sent` دون DLQ جديد، وبعدها محاولة فيديو مراقبة واحدة بلا refund لاستكمال Soft Launch.

## جلسة 61 — مراجعة الحزمة الثانية وبوابة إعادة تفعيل البريد — 29 أبريل 2026

| المحور | النتيجة | القرار |
|---|---:|---|
| مراجعة تعديلات الحزمة الثانية | ✅ مكتملة | تم فحص سياق الحملة، روابط التنفيذ، أدوات النص/الصورة/الفيديو، المكتبة، والخادم دون imports مكسورة عبر ESLint |
| تنظيف كود قديم/مكرر | ✅ تم | أُبقي مكوّن `CampaignContextBar` الموحّد، وصُحح fallback حتى لا تُعامل حملة غير موجودة كحملة صالحة في الحفظ أو العودة |
| مراجعة النصوص التسويقية | ✅ سليمة | النصوص بقيت ضمن لغة المنتج: “اكتب نصاً يبيع”، “صمّم صورة إعلان”، “أنشئ فيديو قصير”، “العودة للاستوديو” دون وعود عامة أو هلوسة |
| التجاوب والثيم | ✅ لا كسر ظاهر في الكود | المكونات تستخدم tokens عامة وتخطيطات `flex/grid` متجاوبة؛ يلزم فحص Preview بصري كامل بعد إتاحة جلسة/بيانات مناسبة |
| حالة البريد | 🚫 مانع تشغيلي مستمر | النطاق المخصص `rifd.site` موجود للمشروع، لكن لا توجد أي نطاقات بريد مهيأة في مساحة العمل حالياً |

**الحكم النقدي:** الحزمة الثانية سليمة تقنياً بعد تصحيح fallback المهم: إذا فُتح رابط `campaignId` صحيح شكلاً لكنه غير موجود أو غير مملوك للمستخدم، تظهر الأداة كوضع عادي ولا تُرسل metadata حملة كاذبة ولا يظهر زر عودة مضلل. لم أحذف ملفات، لكن حذفت سلوكاً قديماً/زائداً كان يمرر `campaignPackId` القديم حتى عند فشل تحميل الحملة.

**نسبة التقدم بعد هذه الدفعة:** `99.9991%` جاهزية بناء وتشغيل. لم أرفعها أكثر لأن مانع البريد التشغيلي ما زال قائماً، ولا يصح اعتماد Beta قبل إعادة تفعيل بريد `rifd.site` والتحقق من إرسال فعلي.

**الخطة التالية الدقيقة:** إعداد نطاق البريد من **Cloud → Emails** باستخدام `rifd.site` فقط، ثم فحص سجل الإرسال للتأكد من `sent` دون DLQ جديد، وبعدها متابعة محاولة فيديو مراقبة واحدة بلا refund.
